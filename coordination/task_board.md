# Task Board

## Current Priority

1. T086 has deployed and independently accepted the T085 cache-stable unified query-planning release on top of the existing `v4-hybrid-fixed20-p1fix-v4` production runtime. The v4 corpus, FTS, 23,250 durable vectors and governed fixed20 retrieval flow remain active; ANN and knowledge graph are not used.
2. Registration, login, invitation, account, quota, frontend and public API architecture remain on the existing application layer. Production retains 7 users, 6 invitations and 27 sessions; application integrity is `ok`, registration and email verification remain enabled, and the private `/knowledge/*` route still returns public HTTP 404.
3. v3 is retained byte-for-byte only as a rollback asset and is no longer opened by the active KB process. Mutable candidate staging remains in an isolated v4 SQLite file. The latest code-deployment rollback point is `20260803T004333Z`; all 855 tests and the post-deployment simple and multi-evidence cloud smoke tests pass.
4. T075 remains `review_needed`; relation-layer/knowledge-graph construction is still a separate later decision and was not activated by the v4 cutover. `cloud_sync_required=false` now means no additional pending sync after the completed authorized deployment.

## Tasks

| ID | Owner | Status | Task | Output |
| --- | --- | --- | --- | --- |
| T001 | KB agent | done | Implement knowledge-base schema for standards, chunks, tables, candidates, and ingest runs. | `src/mining_qa/knowledge_store.py` |
| T002 | KB agent | done | Ingest all governed standards into the first local/internal KB. | `data/knowledge_base/` |
| T003 | KB agent | done | Implement `/knowledge/search` and `/knowledge/standards`. | `src/mining_qa/knowledge_service.py` |
| T004 | KB agent | done | Report OCR/table governance and KB MVP status. | `coordination/kb_agent_status.md` |
| T005 | PRD agent | done | Review implemented KB MVP behavior and docs alignment. | `docs/API_SPEC.md`, `docs/ARCHITECTURE.md`, API regression updates |
| T006 | KB agent | done | Add real-KB regression tests for search/catalog/candidate endpoints. | `scripts/run_kb_regression.py` |
| T007 | KB agent | done | Rebuild clause-level chunks for standards, especially mineral-body extrapolation clauses such as 5.4, 8.2.3, 8.2.6, 9.2.6 and G.1; preserve accurate `clause_no`, section path, page, and 1-3 sentence evidence quotes. | `scripts/rebuild_clause_chunks.py`, refreshed `data/knowledge_base/` |
| T008 | KB agent | done | Tighten raw `/knowledge/search` evidence output: return only 1-3 directly relevant sentences in `quote`, avoid long surrounding OCR text, and preserve `include_full_text=true` only for trusted internal review. | `src/mining_qa/knowledge_store.py`, updated regression checks |
| T009 | KB agent | done | Improve clause metadata completeness: reduce empty `clause_no` for clause chunks, especially standards/specifications where section numbers are present in OCR text; keep section path, page range, and source URL aligned. | `scripts/rebuild_clause_chunks.py`, refreshed `data/knowledge_base/` |
| T010 | KB agent | done | Expand `/knowledge/health` with operational index counts for validation: `vector_count`, `kg_entity_count`, `kg_relation_count`, plus existing document/chunk/candidate counts. | `src/mining_qa/knowledge_store.py`, `src/mining_qa/knowledge_service.py` |
| T011 | KB agent | done | Build an intent-enhanced retrieval layer for high-value policy authority questions. Start with `储量评审备案权限/负责机构` cases: map user wording such as `采矿证`, `去哪个机构`, `谁负责`, `哪一级部门` to canonical terms such as `采矿许可证`, `评审备案范围和权限`, `自然资源主管部门负责`. | synonym/intent rules in KB retrieval code or config, updated regression |
| T012 | KB agent | done | Extract and store high-value authority relations from policy clauses into the lightweight KG. Required first relation: `自然资规〔2023〕6号` 第十条: `自然资源部 -> 负责 -> 本级已颁发勘查许可证或采矿许可证的矿产资源储量评审备案`; `省级自然资源主管部门 -> 负责 -> 其他矿产资源储量评审备案`; include clause id, source URL/document id, and quote. | `kg_entities`, `kg_relations`, build script updates |
| T013 | KB agent | done | Add regression coverage for policy authority retrieval: the question `我的采矿证是自然资源部颁发的，我的储量评审应该去哪个机构` must rank `自然资规〔2023〕6号` 第十条 in the top 3 `/knowledge/search` results, with a short quote containing `自然资源部负责本级已颁发勘查许可证或采矿许可证`. | `scripts/run_kb_regression.py` |
| T014 | KB agent | done | Hydrate official source URLs for policy/system documents, not only standards. At minimum, `自然资规〔2023〕6号《自然资源部关于深化矿产资源管理改革若干事项的意见》` and all MNR `official_fulltext` policy documents returned by `/knowledge/search` must include stable official `url` and `source_platform`; answers citing policy clauses should render clickable source links just like standards. Add regression coverage for policy authority questions to assert the top cited policy source has a non-empty official URL. | `src/mining_qa/knowledge_store.py`, MNR policy manifests/DB hydration, `scripts/run_kb_regression.py` |
| T015 | KB agent | done | Add a maintainable `domain_lexicon` capability for intent-aware retrieval. First stage may be a SQLite table, JSON/YAML config, or equivalent build artifact, but it must preserve user expression, canonical term, intent label, positive expansions, negative terms, evidence-required patterns, priority, and status. Seed at least the high-value entries documented in `docs/KNOWLEDGE_BASE_REQUIREMENTS.md` section 6.8, especially `采矿证/采矿许可证`, `储量报告评审/矿产资源储量评审备案`, `大型金矿` as background not authority basis, `沙金/砂金/金属砂矿类`, `工程距离/勘查工程间距`, and `矿体外推/资源量类型与矿体外推`. Add regression cases showing that authority questions are routed to policy authority evidence and unrelated oil/gas or coalbed methane evidence is downranked for solid-mineral questions. | `src/mining_qa/domain_lexicon.json`, retrieval normalization in `knowledge_store.py`, `scripts/run_kb_regression.py`, runbook/status docs |
| T016 | KB agent | done | Treat `/home/nalanmading/下载/0. 继续有效文件.xls` as the authoritative allowlist for previously web-crawled MNR files published before 2026. Completely remove every in-scope pre-2026 crawled document whose normalized document number is absent from the workbook, remove all active derived data, and prevent future re-ingestion. | `scripts/govern_mnr_policy_allowlist.py`, refreshed private `data/knowledge_base/`, deletion manifest, regression results, `coordination/kb_agent_status.md` |
| T017 | KB agent | done | Ingest the 40 structured MNR mineral-service guides under `/home/nalanmading/下载/2. geowiki` as a distinct official service-guide corpus, preserving sections, application-material tables, official source links, attachment links, and flowchart links. | idempotent ingest script, refreshed private `data/knowledge_base/`, service-guide manifest, regression results, `coordination/kb_agent_status.md` |
| T018 | KB agent | done | Parse and index `自然资规〔2023〕4号` attachment 4, `采矿权申请资料清单及要求.doc`, as structured evidence linked to the parent policy and relevant service guides. | attachment parser, structured material chunks/tables, refreshed private indexes, regressions, `coordination/kb_agent_status.md` |
| T019 | KB agent | pending | Reconcile the five `effective_status='unverified'` documents that remain eligible by review and answerability. Verify each against its official source, record `status_source`/`status_evidence`/`status_checked_at`, then set `effective_status` to `current`, `repealed`, or `governance_conflict` only with evidence. Do not promote by title or model memory. The immediate regression blocker is `DZ/T 0430-2023《固体矿产资源储量核实报告编写规范》`: its 291 existing chunks must remain unavailable to positive answers until status verification completes. Rebuild only affected retrieval artifacts if document IDs/chunks change; status-only changes do not require content re-ingestion. | refreshed private SQLite KB and metadata-only verification report in `coordination/kb_agent_status.md`; `PRAGMA integrity_check`; update `scripts/run_kb_regression.py` so report-limit assertions require a `current` document rather than silently accepting `unverified`; `cloud_sync_required=true` |
| T020 | KB agent | done | For the isolated `data/knowledge_base_v4/` rebuild, reconcile identity and effective status for all 81 technical-standard documents. The initial audit found 73 compilation documents with no status evidence, 5 previously unverified documents, and 9 technical documents with no standard number. Verify against an official standard source, preserve status source/evidence/check date and official URL, and classify as `current`, `repealed`, or `governance_conflict`. Do not infer status from title, do not alter the v3 production DB, and do not activate a v4 document without evidence. Amendments and no-code materials may retain no `standard_no` only when their parent/version relation is explicitly recorded. | metadata-only status/identity reconciliation report under private `data/knowledge_base_v4/reports/`; updated private v4 corpus; `PRAGMA integrity_check`; completion entry in `coordination/kb_agent_status.md` |
| T021 | KB agent | done | Triage v4 source-quality findings before any retrieval experiment: 151 low-confidence OCR pages, 11 empty source pages, 9 documents missing standard numbers, 3 oversized structural units, and the structured policy attachment currently preserved from a legacy governed parser. Produce a priority-ranked metadata-only remediation manifest. Correct text only from an official original or controlled re-OCR/structured parser; retain raw text, page mapping, original hashes and every repair decision. No silent text replacement and no vector/FTS/KG build in this task. | private quality remediation manifest and audit report under `data/knowledge_base_v4/reports/`; source/repair evidence; `PRAGMA integrity_check`; completion entry in `coordination/kb_agent_status.md` |
| T022 | KB agent | done | After all P0 findings have an explicit decision recorded in `coordination/decisions.md`, T023/T024/T025 have completed, and a separate activation approval is present, perform one unified v4 governance/remediation run. Apply only approved repairs, exclusions, boundary corrections, attachment page-model repairs, source-role rules, and official-content verification outcomes; preserve raw source files, page/unit mappings, original and revised hashes, and a decision-level audit trail. `《矿产资源储量技术标准解读300问》` may remain citable only as explicitly labelled interpretive material and may not override current normative sources. Do not build FTS/vectors/embeddings/ANN/KG until the unified content mutation has passed validation. | private remediation run report and manifest; pre/post counts and hashes; source-to-page/unit mapping; `PRAGMA integrity_check`; P0 exclusion/repair assertions; `cloud_sync_required=false` until a later retrieval activation decision |
| T023 | KB agent | done | Audit source boundaries for every v4 document derived from a compilation PDF before unified remediation. For each document, verify the first and last included global pages against the original compilation PDF and adjacent pages. Detect missing own cover/title pages, category dividers, blank pages, table-of-contents pages, duplicate pages, and content from the preceding/following standard. Produce a metadata-only boundary manifest with proposed corrected inclusive ranges and evidence; do not alter v4 content or build retrieval artifacts. | `data/knowledge_base_v4/reports/t023_compilation_boundary_audit.md` and machine-readable manifest; one row per compilation-derived document; source-page evidence for each anomaly; coverage/count reconciliation; `PRAGMA integrity_check`; completion entry in `coordination/kb_agent_status.md` |
| T024 | KB agent | done | Acquire the published official full text for `DZ/T 0479-2024《压覆矿产资源调查评估规范》` from the user-provided NRSIS PDF URL; preserve it as the only prospective answer source, create a governed Markdown/JSON page artifact from it, and perform an auditable page/section alignment against the existing deprecated report draft. Do not activate or mutate v4 corpus content before T022. | private official-PDF artifact, official Markdown/JSON with raw page mapping, page/section comparison manifest, draft-deprecation record, integrity result, and completion entry in `coordination/kb_agent_status.md` |
| T025 | KB agent | done | Verify the P0-affected standards, attachment, and interpretive source against their authoritative official websites and official full-text files where accessible. Metadata-only search results do not verify body content. Produce a source-by-source content-verification ledger that identifies verified page/section scope, exact source, hashes, access limitations, and any new discrepancy; do not mutate v4 before T022. | `data/knowledge_base_v4/reports/t025_p0_official_content_verification.md` and machine-readable ledger; official source artifacts/URLs/hashes; blockers/questions; integrity result; completion entry in `coordination/kb_agent_status.md` |
| T026 | KB agent | closed | Perform semantic source-quality governance on the post-T022 isolated v4 corpus. Review the 142 low-confidence OCR findings attached to current documents as risk indicators rather than automatic errors, correct only text demonstrably inconsistent with the visible source, govern the two active oversized interpretive units, retire the one archived oversized finding, and then apply a document-level quality gate to the 70 current technical documents still pending quality review. Defer the four low-confidence pages belonging to a repealed standard as P2. | private T026 triage/remediation manifests and evidence; controlled mutation script with rollback; document promotion/hold ledger; validation and test results; completion entry in `coordination/kb_agent_status.md`; main-agent acceptance recorded below |
| T027 | Main agent | done | Build a reproducible retrieval-only derived layer on the frozen T026 corpus: supplement context for all 3,944 short source units, inspect all 156 long source units and split only at visible safe boundaries or structured rows, stitch all 1,224 cross-page units, reconstruct the three wide two-page-imposition standards, build document/section/table parent-child relations, isolate review-required leaves from direct citation, and run a controlled full-scan pre/post A/B with fixed questions, rewrites, and scorer. Do not create final chunks, FTS, embeddings, ANN, or a knowledge graph. | `scripts/build_v4_retrieval_preprocessing.py`; `data/knowledge_base_v4/retrieval_preprocessing_v1/`; `scripts/run_v4_fullscan_ab.py`; `scripts/compare_v4_retrieval_preprocessing_ab.py`; `data/knowledge_base_v4/evaluation/retrieval_preprocessing_ab_v1.md`; T030 refresh: 19,356 eligible source units / 23,250 leaves / 21 scored Gold cases; database hash `9f23c72...1d97` unchanged |
| T028 | Main agent | done | Run a controlled flat-leaf versus conservative parent-child full-scan A/B on the frozen T027 artifacts. Parent nodes are navigation/ranking signals only and cannot be cited. Expand the exact attachment-4 application branch for explicit new-establishment/extension questions, allow bounded expansion for condition-bundle questions, and leave ordinary single-point questions on their unchanged flat ranking. Record complete-list recall and wrong-branch contamination separately from the unchanged Gold metrics. This experiment does not adopt a production retrieval strategy. | `scripts/run_v4_parent_child_ab.py`; `tests/test_v4_parent_child_ab.py`; `data/knowledge_base_v4/evaluation/parent_child_ab_results_v1.json`; `data/knowledge_base_v4/evaluation/parent_child_ab_report_v1.md`; refreshed on T030 hash with Q22 ranked 3rd and 100% Coverage@20 |
| T029 | Main agent | done | Isolate the contribution of an FTS inverted index and BM25 ranking on the current 23,250-leaf T027 corpus. Compare A: exhaustive current lexical scoring, B: FTS MATCH candidates plus the current scoring formula, and C: the same FTS scope plus BM25. Run flat-leaf retrieval only, keep questions, rewrites, Gold and Top-K fixed, and build only a private experimental index outside `corpus.sqlite`. Reject native trigram when it hard-drops two-character Chinese terms; rerun with deterministic Chinese bigram/trigram tokens and a zero-candidate full-scan fallback. Do not adopt a production index or start embeddings in this task. | `scripts/run_v4_fts_bm25_ab.py`; `tests/test_v4_fts_bm25_ab.py`; private `data/knowledge_base_v4/retrieval_indexes_experimental/fts_bm25_v2.sqlite`; `data/knowledge_base_v4/evaluation/fts_bm25_ab_results_v2.json`; `data/knowledge_base_v4/evaluation/fts_bm25_ab_report_v2.md`; T030 refresh: 23,250 rows / 100% candidate-group recall / zero fallback; corpus hash unchanged |
| T030 | KB agent | closed | Repair the isolated v4 corpus after confirming that T016 captured and then wrongly deleted the current `《中华人民共和国矿产资源法》` because its 2024 presidential-order number was absent from a departmental-document allowlist. Restore exactly this law from the user-approved official MNR full-text URL with governed metadata, source/hash preservation and article/paragraph evidence, and correct the future governance boundary so absence from the departmental allowlist cannot by itself delete a law or administrative regulation. Separately perform a metadata-first audit of the other 273 T016 deletions for current laws and administrative regulations, including `《稀土管理条例》`, but do not restore any additional document without a later user decision. Do not modify v3 or create/rebuild final chunks, FTS, embeddings, ANN or KG. | `coordination/kb_agent_t030_foundational_law_repair_prompt.md`; controlled idempotent v4 mutation script and rollback; private T030 readable/machine-readable repair report and T016 upper-law audit; Q22 evidence mapping; integrity/FK/v4 validation/full tests; `cloud_sync_required=false` |
| T031 | Main agent | done | Apply the user's final professional corrections to the 107-item QA review package and promote it to an evidence-bound expanded Gold set without overwriting the audit input. Split Q17 by common/associated mineral and Q25 by the four mining-right application types; correct Q16/Q20/R014/R056/V006/V007; withdraw R034's unsupported positive answer and retain it as an abstention Gold case. Add the beneficiation-test dual-standard and explicit-hierarchy constraint to query/answer prompts. Do not mutate the corpus or refresh retrieval experiments in this task. | `scripts/promote_v4_qa_review_to_gold.py`; private `data/knowledge_base_v4/evaluation/user_qa_curation_v2/qa_gold_111_v2.json` and `.md`; `data/knowledge_base_v4/reports/t031_gold_qa_promotion.md`; 111 Gold cases / 110 retrieval-scored / 1 abstention; v4 hash unchanged; 280/280 tests |
| T032 | Main agent | done | Prepare the accepted 111-case Gold set for controlled keyword-retrieval A/B without running retrieval. Preserve an independent original-question route plus DeepSeek natural-language and keyword rewrites; deterministically demote ungrounded model-added standards/clauses to unverified parallel searches; freeze required evidence groups, score-neutral optional evidence, branch-contamination rules, and R034 abstention behavior; group semantic siblings before deterministic development/validation/holdout splitting. Do not build production FTS, embeddings, ANN, vectors, or a knowledge graph. | `scripts/prepare_v4_gold_retrieval_experiment.py`; `data/knowledge_base_v4/evaluation/t032_retrieval_experiment_v1/`; 111/111 validated rewrites; 110 retrieval-scored / 1 abstention; splits 72/22/17; database hash unchanged; integrity/FK/v4 validation; 290/290 tests |
| T033 | Main agent | done | Isolate the contribution of DeepSeek keyword JSON against the original-question route on the expanded Gold. Compare A original question with B `lexical_query` plus `lexical_terms` using the identical flat exhaustive scorer over all 23,250 T027 leaves. Evaluate development and validation only; keep the 17-case holdout sealed and exclude R034 from positive retrieval scoring. Do not use semantic rewrites, subqueries, routing filters, parent-child expansion, FTS/BM25, fusion, embeddings, ANN, or a knowledge graph. | `scripts/run_v4_keyword_rewrite_ab.py`; `tests/test_v4_keyword_rewrite_ab.py`; private `data/knowledge_base_v4/evaluation/t033_keyword_rewrite_ab_v1/`; 93 scored cases; A/B `Unit Hit@10` 89.2%/78.5%, `MRR@20` 0.658/0.628, `Coverage@20` 89.6%/84.8%; holdout unexecuted; database hash unchanged; 300/300 tests |
| T034 | Main agent | done | Test the user's proposed two-stage keyword architecture. Reuse the validated T033 A baseline; in B, rank files from the DeepSeek keyword JSON across all 23,250 leaves, select Top-N documents, then use the original question and unchanged scorer only inside those documents. Select the smallest safe N from 5/10/20/30 using development evidence-document-group retention only, freeze it before validation, and keep holdout sealed. Do not use semantic rewrites, subqueries, routing filters, branch protection, parent-child expansion, FTS/BM25, fusion, embeddings, ANN, or a knowledge graph. | `scripts/run_v4_document_then_clause_ab.py`; `tests/test_v4_document_then_clause_ab.py`; private `data/knowledge_base_v4/evaluation/t034_document_then_clause_ab_v1/`; Top-30; development/validation candidate document-group recall 100%/100%; A/B `Unit Hit@10` 89.2%/90.3%, `MRR@20` 0.658/0.679, `Coverage@20` 89.6%/88.9%; holdout unexecuted; database hash unchanged; 312/312 tests |
| T035 | Main agent | done | Isolate FTS filtering and BM25 ordering in the first-stage Top-30 document router while keeping T034's original-question clause retrieval fixed. Compare A full-corpus current scoring, B FTS MATCH candidates plus current scoring, and C the identical FTS candidate population plus BM25. Score development and validation only, keep R034 outside positive retrieval and the 17-case holdout sealed, and do not add rewrites, subqueries, protected routing, parent-child expansion, fusion, vectors, ANN, or a knowledge graph. | `scripts/run_v4_fts_bm25_document_router_abc.py`; `tests/test_v4_fts_bm25_document_router_abc.py`; private `data/knowledge_base_v4/evaluation/t035_fts_bm25_document_router_abc_v1/`; A/B/C Top-30 document-group recall 100%/100%/99.4%; total P50 857.0/738.4/480.7 ms; B preserved `Unit Hit@10` and `Coverage@20` with a 0.005 `MRR@20` decrease, C missed QA010/Q14's required policy document; holdout unexecuted; corpus and experimental-index hashes unchanged; 324/324 tests |
| T036 | Main agent | done | Measure the isolated semantic contribution of Qwen dense retrieval against the safe T035 lexical baseline. Embed the unchanged 23,250 T027 leaves as documents and only the 93 development/validation original questions as queries with `qwen3.7-text-embedding`, 1024-dimensional dense output and one fixed general retrieval instruction. Perform exact cosine full scan only; keep R034 outside positive retrieval and the 17-case holdout unembedded and unexecuted. Do not use rewrites, keyword JSON in the vector arm, model answers, routing filters, parent-child expansion, branch protection, fusion, ANN, knowledge graph, or production indexes. | `scripts/run_v4_qwen_dense_retrieval_ab.py`; `tests/test_v4_qwen_dense_retrieval_ab.py`; private experimental cache under `data/knowledge_base_v4/retrieval_indexes_experimental/qwen37_text_embedding_1024_v1/`; private `data/knowledge_base_v4/evaluation/t036_qwen_dense_retrieval_ab_v1/`; A/B `Unit Hit@10` 90.3%/96.8%, `MRR@20` 0.674/0.777, `Coverage@20` 88.9%/95.9%, wrong-branch contamination 4.8%/1.6%, P50 738.4/203.0 ms; four visible coverage regressions retained; holdout unexecuted; corpus hash unchanged; 338/338 tests |
| T037 | Main agent | done | Preserve the 23,250 T036 document vectors as a durable hash-bound private snapshot, then compare A frozen FTS-keyword Top-50, B frozen Qwen-vector Top-50, C their deduplicated union with equal-weight reciprocal-rank fusion (`RRF k=60`), and D their intersection as a diagnostic. Reuse T036 rankings without new API calls; keep R034 outside positive retrieval and all 17 holdout cases unexecuted. Do not add anchor protection, route quotas, parent-child expansion, reranking, ANN, knowledge graph, or production activation. | `scripts/promote_v4_qwen_document_embeddings.py`; `tests/test_v4_qwen_document_embedding_promotion.py`; private durable artifact under `data/knowledge_base_v4/embedding_artifacts_private/qwen37_text_embedding_1024_t027_v1/`; `scripts/run_v4_parallel_union_rrf_abcd.py`; `tests/test_v4_parallel_union_rrf_abcd.py`; private `data/knowledge_base_v4/evaluation/t037_parallel_union_rrf_abcd_v1/`; A/B/C/D concrete-evidence hit rate at 10 (`Unit Hit@10`) 90.3%/96.8%/97.8%/95.7%, first-evidence mean reciprocal rank at 20 (`MRR@20`) 0.674/0.777/0.797/0.793, required-evidence coverage at 20 (`Coverage@20`) 88.9%/95.9%/97.4%/93.4%; union/intersection candidate evidence recall 98.7%/89.1%; holdout unexecuted; corpus hash unchanged; 354/354 tests |
| T038 | Main agent | done | Audit the two T037 Top-50 union misses before further fusion work. Preserve Gold v2 and create Gold v3 by deduplicating retrieval groups while retaining distinct answer claims, and classify QA034 B.7.4 as conditionally required only on the disputed-investigation argument path. Compare A keyword-50/vector-50 union, B keyword-50/vector-60 union, C A plus bounded visible-numbering family expansion, and D B plus the same expansion. Do not add blanket text overlap, modify retrieval/citation text or vectors, call the Embedding API, rerank final results, execute holdout, or build ANN/KG/production indexes. | `scripts/amend_v4_gold_evidence_contract_v3.py`; `tests/test_v4_gold_evidence_contract_v3.py`; private `data/knowledge_base_v4/evaluation/user_qa_curation_v3/`; `scripts/run_v4_candidate_boundary_structure_abcd.py`; `tests/test_v4_candidate_boundary_structure_abcd.py`; private `data/knowledge_base_v4/evaluation/t038_candidate_boundary_structure_abcd_v1/`; A/B/C/D unconditional candidate recall 152/153, 153/153, 153/153, 153/153; average candidates 86.0/94.7/103.0/112.5; generic structure expansion adds no recall beyond B and is not selected; holdout unexecuted; corpus/retrieval/vector hashes unchanged; 367/367 tests |
| T039 | Main agent | done | With Gold v3 and the T038 keyword-50/vector-60 union fixed, compare A plain equal-weight RRF with B bounded Top-20 admission protection for each route head. Select head depths only on 71 development cases, freeze before reporting 22 validation cases, and keep 17 holdout cases sealed. Do not change RRF scores, add anchors, parent-child expansion, model reranking, new Embedding calls, ANN, KG or production indexes. | `scripts/run_v4_route_head_admission_ab.py`; `tests/test_v4_route_head_admission_ab.py`; private `data/knowledge_base_v4/evaluation/t039_route_head_admission_ab_v1/`; selected keyword Top-1/vector Top-4; A/B `Unit Hit@10` 97.8%/97.8%, `MRR@20` 0.797/0.797, `Coverage@20` 97.5%/98.6%, complete-evidence success 93.5%/94.6%, wrong-branch contamination 10.9%/8.8%; QA084 recovered with zero coverage regressions; validation unchanged; holdout unexecuted; corpus/retrieval/vector hashes unchanged; 378/378 tests |
| T040 | Main agent | closed | Hold T039 and the T038 keyword-50/vector-60 candidate union fixed, then compare A T039 route-head-protected RRF with B two-pass score-blind `deepseek-v4-flash` evidence selection and reranking. Give B only the original question, source metadata, citation eligibility and source text in deterministic hash order; hide route identity, ranks and scores. Freeze 71 development cases before executing 22 validation cases and keep the 17-case holdout sealed. Do not use rewrites, subqueries, new Embedding calls, ANN, KG, final Chunk or production indexes. | `scripts/run_v4_model_only_reranker_ab.py`; `tests/test_v4_model_only_reranker_ab.py`; private `data/knowledge_base_v4/evaluation/t040_model_only_reranker_ab_v1/`; A/B `Unit Hit@10` 97.8%/87.1%, `MRR@20` 0.797/0.757, `Coverage@20` 98.6%/83.3%, complete-evidence success 94.6%/78.5%, wrong-branch contamination 8.8%/0%; zero coverage improvements and 18 regressions; first/final model stages retained 126/121 of 153 necessary groups; pure model reranking rejected; holdout unexecuted; corpus/retrieval/vector hashes unchanged; 390/390 tests |
| T041 | Main agent | closed | Perform a read-only evidence-fate audit over every one of the 93 development/validation cases and 153 unconditional Gold-v3 evidence groups. Trace each interchangeable evidence ID through frozen keyword Top-50, dense Top-60, the T038 union, plain RRF, T039 route-head admission and both T040 model selection stages; classify single-route suppression, multi-evidence completeness loss, same-document crowding and Top-20 cutoff effects. Do not rerun retrieval, tune parameters, call model/Embedding APIs, copy source text into the audit, execute holdout or mutate any corpus/index/vector. | `scripts/audit_v4_layerwise_evidence_fate.py`; `tests/test_v4_layerwise_evidence_fate_audit.py`; private `data/knowledge_base_v4/evaluation/t041_layerwise_evidence_fate_audit_v1/`; keyword/dense/union retained 142/148/153 groups; plain/protected RRF Top-20 retained 146/147; protected Top-10 retained 128; T040 stages retained 126/121; five incomplete cases and two late-first-evidence cases fully traced; database hash unchanged; 400/400 tests |
| T042 | Main agent | closed | Compare T039's frozen atomic Top-20 with structure-aware evidence packages that preserve every original Top-20 leaf and append only citable members of a same table, explicit numbered-clause family or bounded section. Select package types, activation threshold and family size on 71 development cases, report 22 validation cases independently, and keep 17 holdout cases sealed. Parent nodes are non-citable; do not backfill with lower-ranked candidates, rerun retrieval, call a model/Embedding API, modify corpus/vectors, or create ANN/KG/production indexes. | `scripts/run_v4_structured_evidence_bundle_ab.py`; `tests/test_v4_structured_evidence_bundle_ab.py`; private `data/knowledge_base_v4/evaluation/t042_structured_evidence_bundle_ab_v1/`; selected table＋explicit＋section / 2 Top-20 hits / max 12 leaves; group recall 147/153 to 151/153; complete cases 88/93 to 91/93; validation 26/26 groups; zero coverage regressions; 43 bundles of which 36 contained no Gold-required evidence, so production adoption rejected pending a relevance gate; holdout unexecuted; corpus/vector hashes unchanged; 413/413 tests |
| T043 | Main agent | closed | Calibrate QA026's Gold answer to the current implementing-regulation article 29 without overwriting Gold v3. Correct the accepting authority, restore the omitted review-time exclusion, preserve source-specific terminology, and bind the answer claim to the existing citable leaf. Do not change any question, evidence ID, required group, corpus, vector or historical experiment artifact. | `scripts/amend_v4_gold_answer_contract_v4.py`; `tests/test_v4_gold_answer_contract_v4.py`; private `data/knowledge_base_v4/evaluation/user_qa_curation_v4/`; only QA026 answer/claim calibrated; 111 questions and all scoring groups unchanged; Gold v3 hash-bound and preserved; corpus/retrieval/vector hashes unchanged; 420/420 tests |
| T044 | Main agent | closed | Compare T035's final-clause mechanical two/three/four-character segmentation with semantic concept segmentation while holding its FTS Top-30 documents and candidate leaves fixed. Reuse only T032's frozen DeepSeek locked/verbatim analysis; exclude synonyms, canonical substitution, Embedding, model calls, relation/authority bonuses and residual character fragments from B. Score 71 development and 22 validation cases against current Gold v4 and keep 17 holdout cases sealed. | `scripts/run_v4_semantic_concept_segmentation_ab.py`; `tests/test_v4_semantic_concept_segmentation_ab.py`; private `data/knowledge_base_v4/evaluation/t044_semantic_concept_segmentation_ab_v1/`; A/B `Unit Hit@10` 90.3%/81.7%, `MRR@20` 0.674/0.558, `Coverage@20` 89.0%/82.7%, complete-evidence success 83.9%/80.6%; pure concept replacement rejected; QA026 article 29 full rank 174/132; holdout unexecuted; corpus/retrieval/vector hashes unchanged; 431/431 tests |
| T045 | Main agent | closed | Recompare mechanical character segmentation with governed semantic concept families under the user's approved scoring rule: variants inside one concept share one max-pooled contribution, while independent concepts sum. Reuse T044 A byte-for-byte and keep the same T035 Top-30 documents, leaves, Gold v4 and frozen DeepSeek analysis. Add only a small auditable query-only registry for `勘查实施方案/勘查方案`, `采矿证/采矿许可证` and unambiguous Ⅰ-type forms; do not derive terms from Gold or evidence text, call a model/Embedding API, or execute holdout. | `schemas/v4_governed_concept_families_v1.json`; `scripts/run_v4_governed_concept_family_ab.py`; `tests/test_v4_governed_concept_family_ab.py`; private `data/knowledge_base_v4/evaluation/t045_governed_concept_family_ab_v1/`; A/B `Unit Hit@10` 90.3%/81.7%, `MRR@20` 0.674/0.553, `Coverage@20` 89.0%/82.9%, complete-evidence success 83.9%/80.6%; QA026 article 29 rank 174/13 and coverage 50%/75%; pure semantic replacement rejected, concept families retained as a future auxiliary signal; holdout unexecuted; corpus/retrieval/vector hashes unchanged; 442/442 tests |
| T046 | Main agent | closed | Compare the frozen mechanical ranking with a mechanically protected governed-concept hybrid. Preserve mechanical Top-10 and its order, then fuse the remaining full mechanical and T045 semantic rankings by weighted RRF (`k=60`). Select semantic weight only on 71 development cases under aggregate and per-case coverage, first-hit and wrong-branch guards; report the previously observed 22-case validation split without using it in the objective and keep 17 holdout cases sealed. Do not call model/Embedding APIs or mutate corpus, leaves, vectors or indexes. | `scripts/run_v4_mechanical_concept_hybrid_ab.py`; `tests/test_v4_mechanical_concept_hybrid_ab.py`; private `data/knowledge_base_v4/evaluation/t046_mechanical_concept_hybrid_ab_v1/`; selected semantic weight 1.5; A/B `Unit Hit@10` 90.3%/90.3%, `MRR@20` 0.674/0.676, `Coverage@20` 89.0%/92.8%, complete-evidence success 83.9%/89.2%, wrong-branch rows 2/1; six coverage improvements and zero regressions; QA026 50%/100%; holdout unexecuted; corpus/retrieval/vector hashes unchanged; 453/453 tests |
| T047 | Main agent | closed | Evaluate whether a question-only hypothetical answer helps after retrieval rather than biasing retrieval. Freeze each T038 keyword Top-50 plus vector Top-60 union before any model call; generate neutral evidence requirements and an explicitly unverified hypothesis without exposing candidates; compare full per-candidate evidence matrices with and without that hypothesis; use the same deterministic coverage selector and keep the 17-case holdout sealed. Do not modify retrieval, Gold, corpus, vectors or production indexes. | `scripts/run_v4_post_union_hypothesis_evidence_matrix_ab.py`; `tests/test_v4_post_union_hypothesis_evidence_matrix_ab.py`; private `data/knowledge_base_v4/evaluation/t047_post_union_hypothesis_matrix_ab_v2/`; A/B matrix recall 97.4%/97.4%, `Unit Hit@10` 100.0%/98.9%, `MRR@20` 0.856/0.863, `Coverage@20` 97.6%/95.9%, complete-evidence success 93.5%/90.3%; zero coverage gains and three regressions; hypothetical-answer enhancement rejected; holdout unexecuted; corpus/retrieval/vector hashes unchanged; 464/464 tests |
| T048 | Main agent | closed | Test whether one unified, non-exclusive evidence-role prompt can handle evidence chains, list rows, general/special rules, classifications and level definitions without relying on a unique question type. Hold T047's original question, neutral requirements and frozen keyword Top-50 plus vector Top-60 union fixed; compare A T047 neutral matrix with B the new prompt under the same selector, and report C's role-aware selector separately as an offline diagnostic. Require a candidate-verbatim role basis and stop before confirmation if the development safety gate fails. Do not use hypotheses, modify retrieval, execute holdout or mutate corpus/vectors/indexes. | `scripts/run_v4_unified_evidence_role_ab.py`; `tests/test_v4_unified_evidence_role_ab.py`; private `data/knowledge_base_v4/evaluation/t048_unified_evidence_role_ab_v2/`; development A/B/C matrix recall 96.9%/96.9%/96.9%, `Unit Hit@10` 100.0%/95.8%/94.4%, `MRR@20` 0.829/0.839/0.828, `Coverage@20` 97.4%/94.0%/91.6%, complete-evidence success 93.0%/88.7%/85.9%; B three gains/five regressions, C two gains/six regressions; prompt and role selector rejected before confirmation; confirmation/holdout unexecuted; corpus/retrieval/vector hashes unchanged; 474/474 tests |
| T049 | Main agent | closed | Evaluate a safe, non-demoting post-union supplement. Preserve T039's protected Top-20 and order byte-for-byte; use T047's neutral requirements and frozen neutral candidate matrix only to append citation-eligible candidates for requirements not recognized in the prefix. Select the cap from +2/+4/+6/+10 on 71 development cases, then report the 22 previously observed confirmation cases. Compare against an equal-size T039 continuation so context growth is not credited to the model. Do not use hypotheses, make new model/Embedding calls, execute holdout or mutate corpus/vectors/indexes. | `scripts/run_v4_protected_gap_supplement_ab.py`; `tests/test_v4_protected_gap_supplement_ab.py`; private `data/knowledge_base_v4/evaluation/t049_protected_gap_supplement_ab_v1/`; selected cap +2; overall A/B/C evidence-pool coverage 98.6%/98.8%/98.8%, complete-evidence success 94.6%/95.7%/95.7%, retained groups 147/148/148; QA026 recovered with zero regression but equally by B and C; C wrong-branch rows 5 versus B 7; non-demoting architecture accepted as safe, current neutral supplement not adopted as a new baseline; holdout unexecuted; corpus/retrieval/vector hashes unchanged; 484/484 tests |
| T050 | Main agent | closed | Test a baseline-aware direct-gap auditor under the T049 append-only safety architecture. Show the model the original question, T047 neutral requirements, protected T039 Top-20 and remaining frozen keyword-50/vector-60 candidates while hiding route, rank and score. Require an exhaustive covered/partial/missing audit and candidate-verbatim basis for at most ten direct supplements; compare +2/+4/+6/+10 against an equal-size T039 continuation on 71 development cases. Execute the 22-case confirmation split only if C strictly improves necessary-evidence recall without adding wrong application branches; keep holdout sealed. | `scripts/run_v4_direct_gap_audit_ab.py`; `tests/test_v4_direct_gap_audit_ab.py`; private `data/knowledge_base_v4/evaluation/t050_direct_gap_audit_ab_v1/`; selected diagnostic cap +4; development A/B/C retained groups 123/124/124 of 127, evidence-pool coverage 98.7%/99.0%/98.8%, complete-evidence success 94.4%/95.8%/95.8%, wrong-branch rows 5/10/5; C improves QA020 but loses B's QA034 gain and does not recover QA026; development gate failed; confirmation/holdout unexecuted; corpus/retrieval/vector hashes unchanged; 500/500 tests |
| T051 | Main agent | closed | Determine whether T050 fails because the model must copy long opaque `runit-*` identifiers. Reuse the frozen T050 development inputs and change only model-visible candidate IDs to deterministic three-letter aliases assigned over the existing randomized union order. Hide real IDs, preserve raw responses privately, map only exact aliases back to real IDs, and apply the unchanged T050 guards. Compare at inherited cap +4 and report +2/+4/+6/+10 diagnostically; do not execute confirmation or holdout. | `scripts/run_v4_short_alias_interface_ab.py`; `tests/test_v4_short_alias_interface_ab.py`; private `data/knowledge_base_v4/evaluation/t051_short_alias_interface_ab_v1/`; malformed supplement IDs 17→0 and QA026 recovered, but invalid supplements 102→103; aliases produce 78 base-as-supplement and 23 remaining-as-base-support errors; base/control/long/alias retained groups 123/124/124/124 of 127; alias loses QA020 while gaining QA026 and ties equal-size continuation; short alias accepted as interface hygiene only, not retrieval baseline; confirmation/holdout unexecuted; corpus/retrieval/vector hashes unchanged; 517/517 tests |
| T052 | Main agent | closed | Test whether separating baseline auditing and gap supplementation into two role-isolated calls removes T051's candidate-role confusion. Keep the protected Top-20 immutable; let stage one see only base candidates, derive open requirements deterministically, and let stage two see only open requirements and remaining frozen candidates. Keep all other inputs, aliases, model settings and guards fixed; compare +2/+4/+6/+10 and do not execute confirmation or holdout unless the development gate passes. | `scripts/run_v4_two_stage_gap_audit_ab.py`; `tests/test_v4_two_stage_gap_audit_ab.py`; private `data/knowledge_base_v4/evaluation/t052_two_stage_gap_audit_ab_v1/`; role/alias errors all zero and discarded supplements 103→60; QA020 recovered and QA026 preserved; cap +4 A/B/C/D retained groups 123/125/124/125 of 127 with wrong-branch rows 5/12/5/7; T052 ties equal-size continuation and fails the development gate; QA015/QA034 remain visible-candidate semantic omissions; confirmation/holdout unexecuted; corpus/retrieval/vector hashes unchanged; 533/533 tests |
| T053 | Main agent | closed | Isolate whether T047's post-union neutral-requirement expansion causes the remaining selection failures. Keep the complete T052 retrieval frontier, protected base, aliases, stage prompts and guards fixed; replace only the question-only requirement contract with explicit or indispensable core requirements, non-gap optional checks and material ambiguities. Compare against frozen T052 and an equal-size continuation at +2/+4/+6/+10; execute development only. | `scripts/run_v4_minimal_evidence_requirements_ab.py`; `tests/test_v4_minimal_evidence_requirements_ab.py`; private `data/knowledge_base_v4/evaluation/t053_minimal_evidence_requirements_ab_v1/`; required slots 462→73 and open gaps 235/47 questions→16/16; supplements 167→49, non-Gold 165→48, wrong branches 7→5; A/B/C/D retained groups 123/125/125/124 of 127; no improvement, QA020 regression, QA015/QA034 unresolved; gate failed; confirmation/holdout unexecuted; corpus/retrieval/vector hashes unchanged; 551/551 tests |
| T054 | Main agent | closed | Test whether an explicit structured application-material list can use the authoritative branch manifest to determine exactly which complete rows are absent after the T053 frozen evidence pool. Use a question-only deterministic new-establishment/extension trigger, append only missing citation-eligible attachment-4 rows already inside the frozen keyword/dense candidate union, compare against an equal-size original-order continuation, and require every non-target pool to remain identical. Do not read Gold for selection, call models/Embedding, change retrieval/ranking, or execute confirmation/holdout. | `scripts/run_v4_structured_list_branch_completion_ab.py`; `tests/test_v4_structured_list_branch_completion_ab.py`; private `data/knowledge_base_v4/evaluation/t054_structured_list_branch_completion_ab_v1/`; triggered QA019/QA020; QA019 already 14/14, QA020 9/10→10/10 through one existing-union row; A/B/C retained groups 124/124/125 of 127; C improves only QA020, other 70 questions unchanged, wrong-branch rows stay 5; development gate passed as a bounded auxiliary-overlay candidate but not activated; confirmation/holdout unexecuted; corpus/retrieval/vector hashes unchanged; 560/560 tests |
| T055 | Main agent | closed | Test whether an explicit continuously numbered sibling-clause family can repair a missing middle clause after the T054 frozen evidence pool. Require both immediate siblings already retained, the middle clause already inside the frozen keyword/dense candidate union, a shared question-target anchor across all three clauses, and for “how determined” questions a determination relation in the middle clause. Append only; compare against an equal-size original-order continuation; keep every non-trigger pool identical; do not use Gold for selection, call models/Embedding, alter retrieval/ranking or execute confirmation/holdout. | `scripts/run_v4_intervening_clause_gap_completion_ab.py`; `tests/test_v4_intervening_clause_gap_completion_ab.py`; private `data/knowledge_base_v4/evaluation/t055_intervening_clause_gap_completion_ab_v1/`; only QA015 triggers and adds GB/T 13908-2020 clause 8.7.2.2 `runit-c1b9d10cec13993f`; QA015 2/3→3/3 while equal-size B stays 2/3; A/B/C retained groups 125/125/126 of 127; C improves only QA015, other 70 questions unchanged, wrong-branch rows stay 5; QA016 rejected by the determination-relation guard; bounded development gate passed but overlay not activated; confirmation/holdout unexecuted; corpus/retrieval/vector hashes unchanged; 570/570 tests |
| T056 | Main agent | closed | Split compound QA034 into Gold-v5 flow and material subquestions, then test independent score-blind evidence selection as one append-only overlay. Freeze T055's 24-row prefix and the exact 79-candidate lexical/dense union; use the same `deepseek-v4-flash` prompt, temperature zero, short aliases and maximum 12 selections for both branches; hide Gold, route, rank and score; compare each branch with an equal-size T039 continuation. Do not change other development pools or execute confirmation/holdout. | `scripts/amend_v4_gold_qa034_split_contract_v5.py`; `tests/test_v4_gold_qa034_split_contract_v5.py`; `scripts/run_v4_parallel_question_decomposition_ab.py`; `tests/test_v4_parallel_question_decomposition_ab.py`; private `data/knowledge_base_v4/evaluation/user_qa_curation_v5/` and `t056_parallel_question_decomposition_ab_v1/`; Gold v5 112 cases/184 groups; A/B/C 129/130/132 of 136; QA034A 3/4→4/4, QA034B 3/9→5/9; four Appendix-B groups still missed and two assessment-report/Appendix-C rows added; other 70 cases unchanged; gate failed and scheme not adopted; confirmation/holdout unexecuted; corpus/retrieval/vector hashes unchanged; 586/586 tests |
| T057 | Main agent | closed | Test the approved division of responsibility: let `deepseek-v4-flash` decompose QA034 and identify the target standard, workflow/material report branch, appendix and numbered family without answering or selecting evidence IDs; freeze that route, then let deterministic code expand the explicit B.7.1—B.7.6 family from the 23,250 frozen retrieval leaves and append only missing citation-eligible members. Compare A T056 frozen pool, B equal-size original-order continuation and C routed family completion; preserve every other case and stop before confirmation/holdout or activation. | `scripts/run_v4_model_routed_structured_family_completion_ab.py`; `tests/test_v4_model_routed_structured_family_completion_ab.py`; private `data/knowledge_base_v4/evaluation/t057_model_routed_structured_family_completion_ab_v1/`; formal v2 route selected `DZ/T 0479-2024` / chapters 5–6 / informative Appendix B and excluded Appendix C; the nine-row family has five rows inside and four outside the frozen 79-candidate union; A/B/C retained groups 132/132/136 of 136; QA034B 5/9→5/9→9/9; other 71 cases unchanged; bounded mechanism gate passed, but inherited eight-leaf Appendix-C pollution blocks full-architecture adoption; confirmation/holdout unexecuted; corpus/retrieval/vector hashes unchanged; 596/596 tests |
| T058 | Main agent | closed | Rebuild every split-development evidence pool from unchanged T039 order without inheriting T053—T057 experimental additions, admit only the first 20 search- and citation-eligible leaves, and independently replay the accepted deterministic list, intervening-clause and routed-structure rules. Compare A clean rebuild/replay, B source-defined mutually exclusive branch quarantine plus original-order refill, and C frozen governed-concept normative protection. Preserve quarantined rows in audit, use Gold only after selection, and stop before confirmation/holdout or activation. | `scripts/run_v4_clean_base_deterministic_replay_ab.py`; `tests/test_v4_clean_base_deterministic_replay_ab.py`; private `data/knowledge_base_v4/evaluation/t058_clean_base_deterministic_replay_ab_v1/`; 39 cases skip 75 non-citable ranked rows; branch filter triggers only QA019/QA020/QA034A/QA034B; A/B/C retain 134/135/136 of 136 groups; source-forbidden rows 19/0/0 and application wrong branches 5/0/0; B alone recovers QA034A, C alone recovers QA026; no non-trigger change or regression; clean development gate passed but architecture not activated; confirmation/holdout unexecuted; corpus/retrieval/vector hashes unchanged; 606/606 tests |
| T059 | Main agent | closed | Apply the frozen T058 A/B/C architecture exactly once to the 22-question confirmation split without selecting or changing any parameter, trigger, route or evidence rule on confirmation. Score against unchanged Gold v5 only after pool construction, require C to retain all 26 groups with zero branch pollution and zero per-question regression, and keep the 17-question holdout sealed. | `scripts/run_v4_clean_base_deterministic_replay_confirmation.py`; `tests/test_v4_clean_base_deterministic_replay_confirmation.py`; private `data/knowledge_base_v4/evaluation/t059_clean_base_deterministic_replay_confirmation_v1/`; A/B/C all 24/26 groups, question-macro coverage 98.2%, complete-evidence success 95.5%, zero regression and zero branch pollution; no frozen overlay triggers; only QA030 misses GB/T 19492-2020 clauses 4.7/4.8, which are dense-only ranks 32/26 and protected ranks 63/52; strict confirmation gate failed, architecture not activated, holdout unexecuted; corpus/retrieval/vector hashes unchanged; 616/616 tests |
| T060 | Main agent | closed | Correct QA030's stale review metadata without changing the accepted question, reference answer, claims, citations or five required evidence groups. Preserve Gold v5 and create a new hash-bound Gold version in which QA030 explicitly relies only on GB/T 19492-2020 clauses 4.1/4.2/4.3/4.7/4.8 and excludes DZ/T 0400-2022 as an answer basis. | `scripts/amend_v4_gold_qa030_metadata_contract_v6.py`; `tests/test_v4_gold_qa030_metadata_contract_v6.py`; private `data/knowledge_base_v4/evaluation/user_qa_curation_v6/`; Gold v6 remains 112 cases/184 groups; only QA030 `review_note` changed; source v5 and corpus/retrieval/vector artifacts unchanged; `cloud_sync_required=false` |
| T061 | Main agent | closed | Test same-section relational completion for broad classification/type/composition questions after T059. Freeze T058/T059 final pools; require at least two explicit retained clauses in one classification-labelled section; append only in-union citable siblings stating classification/hierarchy or explicit source-to-target estimation relations; compare with equal-size unchanged-T039 continuation. Exclude pure work-stage questions unless they explicitly ask types/categories/system, use Gold only after selection, evaluate all 94 known cases, and keep holdout sealed. | `scripts/run_v4_classification_section_completion_ab.py`; `tests/test_v4_classification_section_completion_ab.py`; private `data/knowledge_base_v4/evaluation/t061_classification_section_completion_ab_v1/`; rejected pre-freeze broad trigger selected irrelevant QA100 clause 5.1; frozen trigger only QA030 and adds GB/T 19492-2020 clauses 4.7/4.8; A/B/C retain 160/160/162 of 162, B has no gain, C has one improved case, zero regression/wrong branch/non-required addition; targeted mechanism gate passed but no generalization or activation claim; holdout unexecuted; corpus/retrieval/vector hashes unchanged; 631/631 tests |
| T062 | Main agent | closed | Prepare a fresh classification-system boundary set for user review before any further experiment. Include eight broad questions that may justify bounded same-section completion, eight single-point classification questions that must not expand a whole section, and eight negative controls about stages, procedures, materials or criteria. Bind draft answers to current citation-eligible evidence, keep exact questions absent from Gold v6, do not run retrieval or the sealed holdout, and do not freeze the draft as Gold before user approval. | User approved the 24 questions / 78 singleton evidence groups for T063 testing; `scripts/prepare_v4_classification_boundary_review_set.py`; `tests/test_v4_classification_boundary_review_set.py`; private `data/knowledge_base_v4/evaluation/t062_classification_boundary_review_v1/`; draft JSON SHA-256 `a95e192...8d717`; corpus/retrieval/Gold hashes unchanged; `cloud_sync_required=false` |
| T063 | Main agent | closed | Freeze the user-approved T062 contract and test the unchanged T061 classification-section completion on the fresh 24-question boundary set. Compare A clean Top-20, B equal-size unchanged-ranking continuation and C unchanged T061 completion using original questions, lexical Top-50 plus dense Top-60, equal RRF and frozen route-head admission. Measure necessary evidence, false triggers, non-required additions and regression on all 94 known questions; do not tune the rule, rewrite questions or execute the sealed holdout. | `scripts/run_v4_classification_boundary_generalization_ab.py`; `tests/test_v4_classification_boundary_generalization_ab.py`; private `data/knowledge_base_v4/evaluation/t063_classification_boundary_generalization_ab_v1/`; candidate union 74/78; A/B/C 41/41/42; only CBP004 improves; zero false trigger/non-required addition/known-case regression; fresh gate failed; 7 focused and all 644 tests pass; database/retrieval/document-vector hashes unchanged; holdout unexecuted; `cloud_sync_required=false` |
| T064 | Main agent | closed | Test whether the four T063 candidate-union misses can be recovered without globally increasing Top-K. Keep the frozen original-question lexical Top-50 plus dense Top-60 union as A; make B append the same count from an extended unchanged-route RRF order; make C append only direct children of source parents named verbatim in a broad family question and routed by an explicit standard number or at least two named parents in one document. Use Gold only after pool construction, keep single-point/negative guards, and do not change final reranking or run the sealed holdout. | `scripts/run_v4_minimal_source_family_candidate_ab.py`; `tests/test_v4_minimal_source_family_candidate_ab.py`; private `data/knowledge_base_v4/evaluation/t064_minimal_source_family_candidate_ab_v1/`; A/B/C 74/74/78 of 78 candidate groups and 22/22/24 complete candidate questions; C triggers only CBP002/CBP008 and adds the exact four missing necessary clauses; B adds four non-required continuations; zero C false trigger/non-required addition; candidate-boundary gate passed; v4 validation and all 652 tests pass; final rerank/known-94/holdout unexecuted; hashes unchanged; `cloud_sync_required=false` |
| T065 | Main agent | closed | Test whether T064's complete candidate union can retain every necessary group inside a fixed 20-row final evidence pool. Keep the current T061 selection common to all arms; A fills by frozen order, B promotes the same number of ordinary candidates as C, and C reserves only direct children of named source parents or repeated paired fields before unchanged-order fill. Use no case ID, Gold, target rank or expected evidence leaf during selection. Preserve single-point/negative controls, do not run known-94 final regression or the sealed holdout, and do not activate the architecture. | `scripts/run_v4_source_structure_final_pool_ab.py`; `tests/test_v4_source_structure_final_pool_ab.py`; private `data/knowledge_base_v4/evaluation/t065_source_structure_final_pool_ab_v1/`; fixed20 A/B/C retain 42/45/78 of 78 and complete 17/17/24; C broad positives 61/61, triggers CBP002—CBP008 only, zero false trigger/non-required package/A→C regression; B improves CBP002/006/007 but regresses CBP005; fresh fixed-pool gate passed; v4 validation and all 660 tests pass; known-94/holdout unexecuted; hashes unchanged; `cloud_sync_required=false` |
| T066 | Main agent | closed | Independently replay the combined T064 candidate-completion plus T065 fixed-20 source-structure path on the frozen 94-question known set. Compare A current T061 variable pools, B fixed-20 pools reserving all accepted specialized-rule additions, and C the same fixed budget plus unchanged T064/T065. Check necessary evidence, wrong branches, non-required expansion, reservation loss and compatibility; do not run the sealed holdout or activate the architecture. | `scripts/run_v4_combined_source_structure_known94_regression.py`; `tests/test_v4_combined_source_structure_known94_regression.py`; private `data/knowledge_base_v4/evaluation/t066_combined_source_structure_known94_regression_v1/`; A/B/C retain 162/162 groups and complete 94/94; B/C exactly20 with zero wrong branch; all 12 prior reservations retained; T064/T065 zero new known-set trigger and B=C; result/report SHA-256 `82e85d3...01719` / `3202c68...b819`; 8 focused and all 668 tests pass; v4 validation ok; holdout unexecuted, architecture inactive, hashes unchanged; `cloud_sync_required=false` |
| T067 | Main agent | closed | Freeze the 17-question sealed-holdout identity, input hashes, candidate architecture, hard evidence thresholds, one-time execution rules and failure disposition without reading question text or Gold evidence rows and without running retrieval. Explicitly audit whether T066 is executable end to end on unseen questions; keep execution unauthorized until a generic runner passes known-94 and fresh-24 shadow replay and the user separately approves the one-time run. | `scripts/prepare_v4_sealed_holdout_acceptance_contract.py`; `tests/test_v4_sealed_holdout_acceptance_contract.py`; private `data/knowledge_base_v4/evaluation/t067_sealed_holdout_acceptance_contract_v1/`; contract/report SHA-256 `d40f149...c694c` / `7157786...3247`; hard gates 100% candidate evidence, 100% `Coverage@20`, 17/17 complete, zero harmful rows, exact20; runtime closure false because T066 inherits known pools and upstream has identity/cache dependencies; 8 focused and all 676 tests pass; v4 validation ok; holdout text unread, embeddings/rankings absent, execution unauthorized; superseded by accepted T068 runtime closure and user-authorized T069 one-time execution; `cloud_sync_required=false` |
| T068 | Main agent | closed | Implement and freeze one case-ID/Gold/cache-free end-to-end fixed-20 runner before any sealed execution. Starting from the original question and its query vector, recompute FTS document routing, lexical Top-50, exact dense Top-60, equal RRF/head admission and generic structural overlays; shadow-replay known94 plus fresh24, require 240/240 candidate and final evidence groups, exact20, zero wrong branch/non-required selection, and verify determinism across Python hash seeds. Keep the sealed 17 unread/unembedded/unexecuted and require another explicit user approval. | `scripts/run_v4_generic_fixed20_shadow_replay.py`; `tests/test_v4_generic_fixed20_shadow_replay.py`; private `data/knowledge_base_v4/evaluation/t068_generic_fixed20_shadow_replay_v1/`; runner/result/report SHA-256 `db1e761...0efc1` / `971dd49...39136` / `6f24b19...37ae2`; runtime amendment SHA-256 `5c53b9c...0510`; 116 query vectors reused plus 2 known QA034 split vectors, holdout vectors 0; known94 162/162, fresh24 78/78, total 240/240 candidate and final, 118/118 complete, exact20, zero wrong branch/non-required selection; hash-seed projection `76755c9...cd260` identical; 8 focused and all 684 tests pass; v4 validation ok; runtime accepted for the separately authorized T069 one-time execution but not activated in production; `cloud_sync_required=false` |
| T069 | Main agent | closed | Execute the explicitly authorized 17-question sealed holdout exactly once with the hash-pinned T068 runtime. Persist all 17 query vectors and immutable per-case rankings; require 100% of the frozen necessary evidence groups in both candidate and final fixed20 pools, 17/17 complete questions, exact20, zero wrong/ineligible/duplicate rows and a separate manual evidence audit. Report any failure without tuning, reranking or reclaiming the set as blind. | `scripts/run_v4_sealed_holdout_one_time_acceptance.py`; `tests/test_v4_sealed_holdout_one_time_acceptance.py`; private `data/knowledge_base_v4/evaluation/t069_sealed_holdout_one_time_acceptance_v1/`; preflight corrected the handoff count from 21 to the frozen actual 22 groups before any vector/ranking existed; wrapper/authorization SHA-256 `932d1b7...cf92` / `d21512b...7288`; candidate evidence 22/22, final evidence 21/22, complete questions 16/17; QA008's DZ/T 0208-2020 clause 1 is dense rank 39, absent from lexical Top-50 and falls to protected union rank 71, so it is excluded from fixed20; exact20 and zero wrong branch/ineligible/duplicate/unsupported selection otherwise; manual audit passes but automatic hard gate and final acceptance fail; result/report SHA-256 `7946fde...fa8d2` / `d84e0db...2494`; architecture remains inactive and this set is observed diagnostic-only; 6 focused and all 690 tests pass; v4 validation/integrity/FK checks pass; corpus/FTS hashes unchanged; `cloud_sync_required=false` |
| T070 | Main agent | closed | Diagnose whether controlled alias normalization alone repairs observed QA008 without changing its frozen query vector, fusion, structural rules or fixed20. Compare A original, B original plus directed `沙金→砂金`, and C original plus surface alias and `沙金→金属砂矿类`; merge variants into one lexical route by each candidate's best variant rank so aliases do not accumulate weight. Treat QA008 only as observed diagnostic and audit trigger selectivity on known94, fresh24 and the other 16 T069 cases. | `scripts/run_v4_controlled_alias_normalization_abc.py`; `tests/test_v4_controlled_alias_normalization_abc.py`; private `data/knowledge_base_v4/evaluation/t070_controlled_alias_normalization_abc_v1/`; A reproduces T069; A/B/C target lexical/dense/fused/final ranks are `未进前50/39/71/未进前20`, `未进前50/39/65/未进前20`, and `未进前50/39/65/未进前20`; C routes DZ/T 0208-2020 document first and its concept-only variant ranks clause 1 at 37, but best-rank union with the original and surface variants dilutes it to merged lexical rank 82; 134 non-target questions have zero alias trigger and delegate to frozen T068; all pools exact20 with zero wrong branch; surface and concept alias-union schemes both insufficient, no activation/generalization claim; script/result/report SHA-256 `515cfd1...c223` / `cc7acf9...de64` / `d884e64...2240`; 6 focused and all 696 tests pass; v4 validation ok; no new embedding, corpus/index/vector/cloud/deployment mutation; `cloud_sync_required=false` |
| T071 | Main agent | closed | Test high-confidence directed alias replacement without T070's variant union. Keep the frozen original question for audit and the structural selector, but give the lexical route exactly one query per arm: A original, B `沙金→砂金`, C `沙金→金属砂矿类`. Reuse the original persisted query vector and unchanged T068 fusion/fixed20. Verify exact A reproduction, target ranking, no structural harm and zero trigger across the 134 non-target known/fresh/observed questions. | `scripts/run_v4_high_confidence_alias_replacement_abc.py`; `tests/test_v4_high_confidence_alias_replacement_abc.py`; private `data/knowledge_base_v4/evaluation/t071_high_confidence_alias_replacement_abc_v1/`; A lexical/dense/fused/final `未进前50/39/71/未进前20`; B `未进前50/39/64/未进前20`; C `37/39/10/10`, recovering DZ/T 0208-2020 clause 1; C routes the target document first and all 20 final rows come from that document, so document precision improves while clause concentration remains broad; all arms exact20 with zero wrong/non-required structural selection, 134 non-target triggers zero; high-confidence standard-family replacement is sufficient for observed QA008 but not generalized or activated; script/result/report SHA-256 `d47b557...064bb` / `9ab9d32...fb315` / `4853414...dcb4a`; 6 focused and all 702 tests pass; v4 validation ok; no new embedding or corpus/index/vector/cloud/deployment mutation; `cloud_sync_required=false` |
| T072 | Main agent | closed | Prepare a user-review boundary set for the two-layer alias contract before any new retrieval experiment: globally normalize `沙金→砂金`, but allow `砂金/砂金矿→金属砂矿类` only for explicit standard-applicability intent; technical-detail questions must not expand and ambiguous questions must stop for user confirmation. Bind every scored answer to current citable evidence, avoid exact overlap with Gold v6/T063 and do not run A/B until user approval. | User approved the complete 18-question review set and authorized testing on 2026-07-29; `scripts/prepare_v4_alias_intent_boundary_review_set.py`; `tests/test_v4_alias_intent_boundary_review_set.py`; private `data/knowledge_base_v4/evaluation/t072_alias_intent_boundary_review_v1/`; 6 positive standard-applicability, 8 negative technical-detail and 4 ambiguous confirmation cases; 14 scored cases/14 singleton evidence groups, all bound to current searchable/citable DZ/T 0208-2020 rows; no exact Gold-v6/T063 question overlap; JSON/report SHA-256 `b3ce20a...32123` / `acd0e31...87f71`; transferred unchanged into T073 frozen contract; architecture remained inactive and `cloud_sync_required=false` |
| T073 | Main agent | closed | Freeze the user-approved T072 contract and measure the contribution of intent-gated standard-family mapping as one lexical-route variable. A uses only the canonical question after `沙金→砂金`; B changes only the six explicit standard-applicability lexical queries to `金属砂矿类`. Share one canonical-question vector and the unchanged T068 generic keyword Top-50, dense Top-60, equal fusion and fixed20 runtime across arms. Require eight technical-detail candidate orders/final pools to remain identical and four ambiguous questions to stop before embedding/retrieval. | `scripts/run_v4_alias_intent_gated_mapping_ab.py`; `tests/test_v4_alias_intent_gated_mapping_ab.py`; private `data/knowledge_base_v4/evaluation/t073_alias_intent_gated_mapping_ab_v1/`; 14 new canonical-question `qwen3.7-text-embedding` vectors persisted privately, 4 ambiguous vectors zero, existing 23,250 document vectors reused; candidate evidence A/B both 14/14; final evidence A 10/14→B 14/14, driven by positive cases 2/6→6/6 while technical controls stay 8/8 with byte-identical candidate order/final pools; improved AIP002—AIP005, zero positive regression; 4/4 ambiguous stop; all 28 final pools exact20, unique, searchable/citable, zero wrong branch/non-required structural selection; boundary and utility gates pass; frozen/result/report SHA-256 `0c22460...4dcb4` / `1e9dd40...6db25` / `d4be658...9d267`; 21 T072/T073 focused and all 723 tests pass; v4 validation/integrity ok at 156 documents and 20,670 units; corpus/FTS/document vectors unchanged; architecture and production rule remain inactive pending user decision, `cloud_sync_required=false` |
| T074 | Main agent | closed | Activate the T073-approved two-layer sand-gold routing rule only in the local generic query stack. Keep original/canonical/lexical/semantic questions and mapping audit fields separate; prohibit hard document scope; stop ambiguous questions before every retrieval route; preserve historical `QueryPlan` positional compatibility. Reuse T073's 14 saved query vectors and replay the frozen boundary contract through the actual production route, requiring exact T073-B candidate/final reproduction and no corpus/index/vector mutation. | `src/mining_qa/governed_query_routing.py`; updated `query_understanding.py`, `question_resolution.py`, `query_analysis.py`, `knowledge_store.py` and exported schema; `scripts/run_v4_governed_routing_activation_replay.py`; `tests/test_governed_query_routing.py`; `tests/test_v4_governed_routing_activation_replay.py`; private `data/knowledge_base_v4/evaluation/t074_governed_routing_activation_replay_v1/`; candidate/final evidence 14/14, complete 14/14, six positive maps, eight technical controls unchanged, four ambiguous stops, zero hard scope; all 14 candidate orders/final pools reproduce frozen T073 B; 14 vectors reused/new Embedding calls 0; v4 validation/integrity ok at 156 documents and 20,670 units; corpus/FTS/retrieval-unit/document-vector/query-vector hashes unchanged; 99 focused and all 734 tests pass; script/result/report SHA-256 `9bf9f14...024b5` / `e95e97f...6bc91` / `6aa1927...1f697`; no KG/cloud/deployment/service mutation; `cloud_sync_required=false` |
| T075 | Main agent | review_needed | Prepare a user-review contract for a small governed relation layer before any graph implementation. Define a bounded relation vocabulary, bind every proposed edge to current citation-safe evidence, include relationship-heavy positives, direct-retrieval negative controls and an ambiguity-first stop case, and freeze a future A/B in which the T074 route is unchanged and relation candidates are additive only. Do not create graph tables, indexes, embeddings, rankings or retrieval results. | `scripts/prepare_v4_governed_relation_layer_review.py`; `tests/test_v4_governed_relation_layer_review.py`; private `data/knowledge_base_v4/evaluation/t075_governed_relation_layer_review_v1/`; 24 questions (16 development/8 confirmation), 18 positive/5 no-expansion/1 stop, 62 proposed relations and 94 necessary evidence groups; every proposed edge is evidence-bound and all evidence is current/searchable/citable; four v4 graph tables confirmed absent; 8 focused tests and v4 validation pass at 156 documents/20,670 units; script/test/JSON/report SHA-256 `75bd0ae...94b1` / `8999ed1...f431` / `cc749ed...ad88` / `e5e7c47...90a7`; graph build/A-B/new Embedding all unauthorized; `knowledge_graph_created=false`; `cloud_sync_required=false`; pending user review |
| T076 | Main agent | closed | Build a local-only v4 production adapter without replacing the online system. Preserve the existing `/knowledge/*` response contract; isolate v4 runtime configuration from v3; freeze the T068 lexical/dense/equal-RRF/structural fixed20 chain and T074 governed sand-gold routes; copy the accepted FTS into a private runtime directory; pin corpus, retrieval-unit, FTS, vector, row-map and code hashes; retain v3 as the default and rollback. Require frozen shadow replay, API compatibility, ambiguity-before-Embedding stops and keyword fallback on vector failure. Do not create ANN/KG, connect to the GeoWiki server, deploy or restart services. | `src/mining_qa/v4_retrieval_store.py`; runtime switch in `src/mining_qa/knowledge_service.py`; `scripts/promote_v4_local_production_runtime.py`; `scripts/run_v4_local_production_shadow.py`; private `data/knowledge_base_v4/runtime_private/hybrid_fixed20_v1/` and `data/knowledge_base_v4/evaluation/t076_local_production_shadow_v1/`; manifest/FTS SHA-256 `c66eb52...e92f9` / `4aa39d2...f6c2f`; corpus/retrieval-unit/vector hashes remain `9f23c72...1d97` / `6d96def...99e72` / `8a5d0b3...644f`; 132/132 scored final pools reproduce their frozen order, T068 118/118, T074 14/14, ambiguity stops 4/4 with zero live Embedding during replay; real Qwen smoke test routes `沙金应该使用哪个标准？` to DZ/T 0208-2020 clause 1; 17 focused tests, v4/promotion/shadow validation and all 759 tests pass; v3 remains default, local v4 shadow ready, cloud/deployment/restart unauthorized, no ANN/KG; `cloud_sync_required=false` |
| T077 | Main agent | closed | Execute the first isolated local end-to-end shadow through separately started v4 Knowledge API and QA API processes. Freeze the already-observed 22-case validation split, four reviewed ambiguity-first sand-gold cases and one synthetic nonexistent-standard guard; call the configured DeepSeek understanding/planning/evidence/answer chain and Qwen query Embedding; map final API sources back to exact retrieval leaves and score Gold evidence plus key facts without grading writing style. Isolate app DB, traces, usage and gap logs; stop both services after the run. Do not reuse the former sealed holdout as blind, connect to GeoWiki cloud, deploy, restart online services or create ANN/KG. | `scripts/run_v4_local_e2e_shadow.py`; `tests/test_v4_local_e2e_shadow.py`; private `data/knowledge_base_v4/evaluation/t077_local_e2e_shadow_v1/`; deterministic v1.1 rescore changes only four equivalent expressions and leaves model outputs unchanged; strict pass 14/22, exact necessary evidence 16/22, key facts 16/22, answered 17/22; ambiguity stop 3/4; missing-authority guard 0/1; completed-request average/P95 12,428/20,112 ms; P0 nonexistent-standard substitution and two unhandled KB timeouts; P1 domain/evidence-sufficiency/source-authority findings; online replacement not recommended; script/test/result/report SHA-256 `9d91843...dc03a` / `e0a21d0...3c91` / `9815c15...c427c` / `9099556...ae326`; 23 T076/T077 focused tests and all 765 tests pass; local processes stopped, v3 default, no cloud/ANN/KG; `cloud_sync_required=false` |
| T078 | Main agent | closed | Repair T077's nonexistent-authority P0 without changing the frozen retrieval baseline. Classify explicit standard mentions as strict authority, status lookup, comparison/relation or incidental mention; for strict authority, validate the catalog and governance state before planning, inject the accepted standard as an existing hard filter, discard sources outside the anchor and require the requested clause or an explicit descendant clause before answering. Missing, repealed, unavailable or unverified authority must stop without silently substituting another standard. | `src/mining_qa/authority_anchor.py`; guarded integration in `src/mining_qa/agent.py`; `tests/test_authority_anchor_guard.py`; v4 standard-scope regressions in `tests/test_v4_local_production_runtime.py`; `data/knowledge_base_v4/evaluation/t078_authority_anchor_guard_v1/authority_anchor_guard_report_v1.md`; MISS001 changes from `answered` with 3 unrelated standards to `insufficient_evidence` with zero sources/body-searches/retrieval rounds and 10.247 ms in-process request time; status/comparison/incidental boundaries preserved; 773/773 tests pass; corpus hash unchanged at `9f23c72...1d97`; v3 default, no cloud/ANN/KG/index/vector mutation; `cloud_sync_required=false` |
| T079 | Main agent | closed | Repair T077's KB-timeout P0 without changing the frozen v1 retrieval baseline or retrieval assets. Separate the QA-to-KB timeout from model timeouts; bound v4 query Embedding to a short independent budget; coalesce identical concurrent Embedding and effective searches; fall back to the unchanged keyword route when Embedding fails; use any successful supplementary KB route when the primary fails; and return a traced `insufficient_evidence` response with no fabricated answer or false KB-gap task when every KB route fails. | `src/mining_qa/v4_retrieval_store_v2.py`; timeout settings in `src/mining_qa/config.py` and `knowledge_client.py`; safe Agent handling in `src/mining_qa/agent.py`; v2 selection in `src/mining_qa/knowledge_service.py`; `tests/test_v4_timeout_resilience.py`; private `data/knowledge_base_v4/runtime_private/hybrid_fixed20_v2/runtime_manifest.json`; v1 remains independently valid; forced Embedding failure retrieves QA051/QA064 Gold groups by keyword at ranks 2/1; three concurrent QA064 searches call Qwen once and complete in 1,482 ms; local E2E QA051/QA064 return HTTP 200 `answered` in 10,146/12,052 ms with KB time 1,325/1,624 ms and expected facts; 778/778 tests pass; corpus SHA-256 unchanged at `9f23c72...1d97`, integrity `ok`, foreign-key violations 0; adapter/test/manifest SHA-256 `c52399c...27e6b` / `07be12b...8e90e` / `330f3b6...137f`; v3 default, no cloud/ANN/KG/index/vector mutation; `cloud_sync_required=false` |
| T080 | Main agent | closed | Repair and independently revalidate all T077 P1 findings without changing retrieval assets. Admit geological sample preparation while retaining explicit food/medicine/metallography exclusions; route canonical `砂金` ambiguity to confirmation before retrieval; remove invented evidence requirements for QA011 and the basic-analysis-item misclassification for QA042; prefer the general normative clause for QA069; and preserve QA030's broad classification identity through model paraphrasing without changing lexical or dense query text. | `src/mining_qa/domain_lexicon.json`; `query_understanding.py`; `retrieval_planner.py`; `agent.py`; independent `structural_query` transport in `knowledge_client.py` and v4 adapters; private `data/knowledge_base_v4/runtime_private/hybrid_fixed20_v4/`; `data/knowledge_base_v4/reports/t080_t077_p1_remediation.md`; six-case local E2E: QA061/QA011/QA042/QA030/QA069 `answered`, AIA002 `clarification_required` with zero sources; QA030 returns exactly GB/T 19492-2020 clauses 4.1/4.2/4.3/4.7/4.8; QA069 returns exactly GB/T 13908-2020 clause 8.7.2.3; all 792 tests pass; corpus hash unchanged `9f23c72...1d97`, integrity `ok`, foreign-key violations 0; v4 manifest SHA-256 `4c080c2...a015e`; v3 default, no cloud/ANN/KG/index/vector rebuild; `cloud_sync_required=false` |
| T081 | Main agent | closed | Resolve T077's remaining QA096 P2 by distinguishing a correct answer from an over-narrow Gold source preference. Preserve Gold v6; verify all proposed alternatives against the frozen current retrieval leaves; keep the question, reference answer and group count unchanged; replace the sole coarse GB/T leaf requirement with precise current clause 8.4 alternatives for petroleum/natural gas, offshore petroleum/natural gas, coalbed methane and shale gas; and explicitly record that the GB/T definition is clause 5.2 although its current merged 5.1-5.4 leaf is labelled 5.1. Do not mutate corpus/retrieval/index/vector assets. | `scripts/amend_v4_gold_qa096_equivalent_evidence_contract_v7.py`; `tests/test_v4_gold_qa096_equivalent_evidence_contract_v7.py`; private `data/knowledge_base_v4/evaluation/user_qa_curation_v7/`; Gold remains 112 cases/184 unconditional groups; only QA096 citations/evidence claims/alternative IDs/review note change; JSON/report SHA-256 `84d2e95...88aa5` / `62c0520...9001`; six focused tests pass; v6 and all retrieval assets preserved; `cloud_sync_required=false` |
| T082 | Main agent | closed | Re-run the complete accepted final-API behavior contract after T081 on Gold v7 and `v4-hybrid-fixed20-p1fix-v4`. Reuse the already-observed T077 validation scope rather than claiming a blind holdout: 22 validation questions, four ambiguity-first questions and one nonexistent-authority guard. Start isolated local Knowledge/QA processes, call configured DeepSeek and Qwen query Embedding, score final sources and key facts, stop all processes, and require every gate to pass. | `scripts/run_v4_local_e2e_acceptance_v2.py`; `tests/test_v4_local_e2e_acceptance_v2.py`; private `data/knowledge_base_v4/evaluation/t082_local_e2e_acceptance_v1/`; 22/22 answered, exact evidence 22/22, key facts 22/22, full pass 22/22; ambiguity stop 4/4; missing-authority guard 1/1; average/P95 10,892/17,766 ms; QA096 passes in 8,236 ms with three precise current clause 8.4 sources; result/report SHA-256 `ba5f6bc...ce1ec` / `84b1e94...ce2d`; all 803 tests pass; corpus hash unchanged `9f23c72...1d97`, integrity `ok`, foreign-key violations 0; v3 default, cloud/deployment/restart unauthorized, no ANN/KG/index/vector rebuild; `cloud_sync_required=false` |
| T083 | Main agent | closed | Replace the online v3 knowledge and retrieval runtime with accepted v4 while preserving registration, login, invitation, account, quota, frontend and public API architecture. Establish a verified rollback point; preserve the live application DB; transfer only explicit private v4 runtime assets; remotely verify hashes and preload the store before switching; keep the KB private behind Nginx; and remove every active v3 runtime dependency, including candidate staging. | `scripts/deploy_v4_cloud.sh`; `src/mining_qa/v4_candidate_store.py`; original-route protection in `src/mining_qa/knowledge_client.py` and `api.py`; `tests/test_v4_candidate_store.py`; private `data/knowledge_base_v4/reports/t083_v4_cloud_cutover.md`; rollback `20260730T080653Z`; active corpus/manifest SHA-256 `9f23c72...1d97` / `4c080c2...a015e`; health reports v4, 156 documents, 23,250 leaves/vectors, query Embedding ready, ANN 0 and KG 0; 7 users/6 invitations/27 sessions preserved; public KB route 404; QA030 five-clause result stable 5/5 repeated runs, ambiguity and missing-authority guards pass; active process has no v3 DB file descriptor; all 807 tests pass; `cloud_sync_required=false` |
| T085 | Main agent | closed | Govern DeepSeek Token use and cache-prefix stability without sacrificing final evidence. Add a bounded privacy-safe stage ledger; move invariant output Schema ahead of dynamic content; compare the accepted two-call path with unified question-understanding/retrieval-planning; and fall back to the dedicated Planner when deterministic completeness checks reject an incomplete unified plan. | `src/mining_qa/llm_observability.py`; `src/mining_qa/prompt_layout.py`; guarded unified planning in `question_resolution.py` / `retrieval_planner.py`; `scripts/run_v4_prompt_schema_layout_ab.py`; private T085 A/B artifacts; `data/knowledge_base_v4/reports/t085_token_cache_governance.md`; final B 22/22 evidence/facts/full pass, Planner calls 22→7, total model calls -20.5%, total Tokens -9.4%, average/P95 latency -12.5%/-10.8%; no corpus/index/vector/cloud mutation; `cloud_sync_required=false` |
| T086 | Main agent | closed | Deploy the accepted T085 release and current-state documentation without changing the v4 knowledge assets or the existing account/application architecture. Synchronize only Git-tracked production files, pin the accepted T085 switches, retain privacy-bounded observation, preserve a clean rollback point, and independently verify the live runtime, private route boundary and representative answers before GitHub publication. | deployed code commit `7ff4b60`; rollback `20260803T004333Z`; `UNIFIED_QUERY_PLANNING_ENABLED=true`; `PROMPT_LAYOUT_VARIANT=schema_prefix`; stage ledger disabled; active `v4-hybrid-fixed20-p1fix-v4`, 156 documents and 23,250 leaves/vectors; ANN 0 / KG 0; 7 users retained; public `/knowledge/*` remains 404; gold-engineering-distance smoke `answered` in about 3.66 s and multi-evidence sample-quality smoke `answered` with four sources in about 13.16 s; 855/855 tests pass; sampled shadow records contain no question, answer, citation or evidence body; `cloud_sync_required=false` |

## T018 Execution Contract

### Dependency and source

- Start only after the T016 cleanup and T017 service-guide ingestion/index rebuild are complete.
- Parent document: `policy-d4869b5b5bf8804f`, `自然资规〔2023〕4号《自然资源部关于进一步完善矿产资源勘查开采登记管理的通知》`.
- Authoritative local attachment: `data/knowledge_base/raw/mnr_policy/attachments/policy-d4869b5b5bf8804f/采矿权申请资料清单及要求.doc.doc`.
- Official attachment URL: `https://f.mnr.gov.cn/202305/P020230512660474974800.doc`.
- Parent official URL: `https://f.mnr.gov.cn/202305/t20230512_2786192.html`.

### Parsing and schema requirements

- Parse the legacy Word attachment with a structured document/table parser. Do not infer missing rows from model knowledge.
- Preserve the four application types declared by the parent policy: new establishment, extension, change, and cancellation.
- Create a stable attachment document or governed child artifact linked to the parent with a relation such as `ATTACHMENT_OF` / `IMPLEMENTS_MATERIAL_LIST_FOR`.
- Store each application type as a separately retrievable section. Preserve table headers, material names, required form/copy rules, conditions, notes, and any conditional branches as structured table data.
- For the extension section, create concise row-level or logically grouped evidence chunks so `采矿证延续需要提交什么材料？` can return the complete applicable list without mixing new-establishment, change, or cancellation materials.
- Keep `visibility='internal'`, preserve the official attachment URL and parent URL, and expose only short directly relevant evidence quotes through `/knowledge/search`.
- Link matching T017 service guides to this attachment as supporting sources without duplicating contradictory material rows. If a guide and attachment differ by jurisdiction or date, preserve both scope/date fields and report the difference for review.

### Retrieval and acceptance checks

- `采矿证延续需要提交什么材料？` must rank the attachment's extension section and/or the matching official service guide above `自然资规〔2023〕6号` and unrelated old replies.
- `采矿证办理应该依据哪个文件？` must rank parent `自然资规〔2023〕4号` and may cite attachment 4/service-guide details as supporting evidence.
- Assert that new-establishment, extension, change, and cancellation rows remain isolated by application type.
- Returned sources must contain clickable official URLs and identify whether the evidence comes from the parent policy, attachment 4, or a service guide.
- Rebuild FTS, local vectors, configured dense embeddings, and KG relations only after inserting the attachment evidence. Run `PRAGMA integrity_check`, KB regression, and the two questions above through `/api/ask`.
- Do not deploy the refreshed private DB directly. Report `cloud_sync_required=true` for main-agent review.

### Required completion report

- Append a dated T018 entry to `coordination/kb_agent_status.md` with parser used, section/table/row counts, attachment-parent/service-guide relations, index deltas, integrity result, regression results, and `cloud_sync_required=true`.
- Record any ambiguous merged cells, unreadable fields, or conflicts in `coordination/kb_agent_questions.md`; otherwise state `Blocked: None`.

## T016 Execution Contract

### Authoritative input

- Workbook: `/home/nalanmading/下载/0. 继续有效文件.xls`.
- Sheet: `Sheet1`.
- Header row: row 3; relevant columns are `文件名称` and `文号`.
- Verified workbook shape at task issue time: 222 non-empty data rows, all 222 rows have a document number, document-number years range from 1986 through 2024.
- Use the isolated `.xls` interpreter `/home/nalanmading/.venvs/codex/bin/python` with `xlrd`; do not install legacy Excel packages into the system Python or project venv.

### Cleanup scope

- Only classify documents previously crawled from the web for the MNR policy/law corpus, normally identified by `source_type='official_fulltext'` together with the MNR policy source trace/platform/category.
- Apply deletion only to documents with a confirmed publication date earlier than `2026-01-01`.
- Preserve every document published on or after `2026-01-01`; this workbook rule does not decide their validity.
- Do not touch governed technical standards, OCR compilation standards, supplemental standards, user uploads, candidates, or unrelated local documents.
- If `documents.standard_no` is empty, first recover the official `文号` from bibliographic/source metadata or the crawled source. Completion requires resolving the publication date and document number for every in-scope record; do not silently classify unknown metadata as deleted or retained.

### Matching rule

- Build an allowlist from workbook `文号`, normalizing Unicode width, whitespace, bracket variants, and dash variants before exact comparison.
- A pre-2026 crawled document is retained only when its normalized official document number exactly matches an allowlist number.
- Use title comparison only as a diagnostic cross-check. Do not use fuzzy title similarity to override a document-number mismatch.
- Record duplicate document numbers, one-to-many matches, title conflicts, and metadata repairs in the report.

### Required deletion

- Produce a dry-run metadata-only manifest before mutation, including document ID, title, document number, publication date, source URL, deletion reason, and counts of dependent records. Do not copy deleted full text into the manifest.
- Create one private timestamped SQLite rollback backup outside the serving path before the destructive transaction and report its path.
- Remove excluded records from the active `documents`, `chunks`, `chunks_fts`, `chunk_vectors`, `chunk_embeddings`, and all KG relations/entities derived only from deleted documents or chunks.
- Remove or update associated active raw HTML, downloaded attachments, processed artifacts, and MNR manifests when they are exclusively owned by deleted documents. Do not remove a file still referenced by a retained document.
- Update document/chunk/table counts and any registries so deleted records cannot appear in `/knowledge/search`, `/knowledge/standards`, direct chunk lookup, vector search, graph retrieval, or future rebuilds.
- Modify the MNR ingest/governance flow so a later crawl cannot re-ingest an excluded pre-2026 document. The same workbook-derived allowlist and cutoff rule must be applied before insertion.

### Acceptance checks

- Run `PRAGMA integrity_check` and report `ok`.
- Assert that every remaining in-scope pre-2026 crawled document has a normalized document number present in the 222-row allowlist.
- Assert zero remaining chunks, FTS rows, local vectors, dense embeddings, or KG evidence references for deleted document/chunk IDs.
- Assert 2026-and-later crawled documents are unchanged by this rule.
- Keep and regression-test `自然资规〔2023〕6号`, which is present in the workbook; the policy-authority question must continue to cite its tenth section correctly.
- Run the existing KB/API regression suites and add a cleanup-specific repeatable validation command or test.
- Do not deploy the refreshed DB to the cloud directly. Mark `cloud_sync_required=true` in the completion report so the PRD/main agent can review and synchronize it.

### Required completion report

Append a dated T016 completion entry to `coordination/kb_agent_status.md` containing:

- Workbook row count and normalized allowlist count.
- In-scope crawled-document count before cleanup.
- Retained, deleted, metadata-repaired, ambiguous, and 2026+ untouched counts.
- Deleted document IDs/titles/document numbers in a metadata-only manifest path.
- Before/after counts for documents, chunks, FTS, local vectors, dense embeddings, KG entities, and KG relations.
- Raw/attachment/artifact files deleted or retained because of shared references.
- Rollback backup path, integrity-check result, regression commands/results, and `cloud_sync_required=true`.
- Any blocker or unresolved ambiguity in `coordination/kb_agent_questions.md`; otherwise explicitly state `Blocked: None`.

## T017 Execution Contract

### Authoritative input

- Source directory: `/home/nalanmading/下载/2. geowiki`.
- Verified source shape at task issue time: 41 Markdown files totaling about 488 KiB.
- Ingest exactly 40 numbered guide files, `01_*.md` through `40_*.md`.
- Treat `_INDEX.md` as the corpus manifest/catalog only; do not expose it as a substantive answer source or duplicate its full item list into ordinary evidence results.
- `_INDEX.md` declares 40 items, 920 standard sections, 46 attachment links, 40 flowchart links, and 40 application-material tables.

### Relationship to T016

- T016 and T017 may be analyzed and scripted in parallel, but SQLite mutations and index rebuilds must be serialized.
- Execute the T016 cleanup transaction first, then ingest T017, then perform one final coherent rebuild/cleanup of FTS, local vectors, dense embeddings, and KG.
- T017 guides are explicitly outside the T016 deletion scope even though their URL dates are before 2026. They are a separate `www.mnr.gov.cn/bsznxxk/fwzn` service-guide corpus, not the MNR normative policy/law corpus governed by the valid-document workbook.

### Document mapping

- Create one stable document per numbered Markdown file, using `source_url` or `source_page_id` as the idempotent identity key. Re-running the ingest must update/replace the same document instead of creating duplicates.
- Use a distinct document type such as `service_guide` / `administrative_service_guide`, `source_type='official_fulltext'`, `text_access='html_text'`, `visibility='internal'`, and a source platform identifying the official Natural Resources Ministry service-guide site.
- Preserve YAML metadata including title, publisher, category, source URL, catalog URL, online-service URL, source page ID, retrieval date, tags, content hash, section/attachment/link counts, and quality/correction metadata.
- `url_date` is derived from the URL and is not an asserted publication date. Store it in bibliographic/source metadata and do not write it into `publish_date` unless an official page explicitly confirms the publication date.
- Keep all 40 guides distinct. Do not merge similarly named older/newer registration and application guides solely because their topics overlap.

### Chunking and structured evidence

- Preserve each official guide heading as a stable section path. Expected guide structure is 23 official sections per guide, including scope, approval basis, accepting/deciding authority, application conditions, application materials, process, time limit, result, consultation, office information, and flowchart.
- Create section-level evidence chunks with concise quotes and no page number. Empty official sections marked `原网页未提供内容` may be retained as metadata but should not rank as answer evidence.
- Parse every GFM application-material table into a structured table chunk with headers, rows, notes, and the parent section path. Do not flatten table columns into ambiguous prose.
- Preserve official attachment and flowchart URLs as link metadata. Do not download or OCR linked attachments as part of T017 unless the user separately assigns that work.
- Preserve transparent link-correction records from the Markdown source and do not silently rewrite additional legal/source links.

### Retrieval and indexing

- Insert all answerable chunks into FTS and update local hash vectors, configured dense embeddings, and KG only after T016 cleanup and T017 ingestion are complete.
- Add useful service-guide entities/relations where evidence supports them, such as `Guide -> APPLIES_TO -> Matter`, `Guide -> ACCEPTED_BY -> Organization`, `Guide -> DECIDED_BY -> Organization`, `Guide -> REQUIRES_MATERIAL -> Material`, and `Guide -> HAS_TIME_LIMIT -> Duration`.
- Ensure guide questions prefer `service_guide` evidence over unrelated standards or policy documents while still allowing cited policy/standard links to remain supporting references.
- Every returned guide source must include its clickable official `source_url` and the correct guide title.

### Acceptance checks

- Assert exactly 40 service-guide documents are active and there are no duplicate `source_url` or `source_page_id` identities.
- Reconcile expected source totals: 920 official sections, 40 application-material tables, 46 attachment links, and 40 flowchart links; explain any parser count difference.
- Add regression questions covering at least:
  - `自然资源部探矿权首次登记需要哪些材料？`
  - `采矿许可变更开采方式怎么办理？`
  - `矿产资源储量评审备案需要提交什么材料？`
  - `矿产资源开采方案的办结时限是多久？`
- Regression answers must cite the matching guide, return only directly relevant sections/table rows, and include the official source URL.
- Run `PRAGMA integrity_check`, existing KB/API regression suites, and T016 cleanup validation after the final index rebuild.
- Do not deploy the refreshed DB to the cloud directly. Include the combined post-T016/T017 DB state and `cloud_sync_required=true` in the completion report.

### Required completion report

Append a dated T017 entry to `coordination/kb_agent_status.md` containing:

- Source file count, accepted/rejected file count, and any metadata/parser repairs.
- Documents, section chunks, table chunks, FTS rows, local vectors, dense embeddings, KG entities, and KG relations added.
- Reconciled section/table/attachment/flowchart totals and duplicate checks.
- Manifest and ingest-script paths, integrity result, regression commands/results, and `cloud_sync_required=true`.
- Confirmation that T017 documents were excluded from the T016 allowlist cleanup scope.
- Any blocker in `coordination/kb_agent_questions.md`; otherwise explicitly state `Blocked: None`.

## Status Values

- `pending`
- `in_progress`
- `blocked`
- `review_needed`
- `done`

## T023 Execution Contract

### Scope and non-mutation rule

- Scope is every `data/knowledge_base_v4/` document whose governed source trace identifies a compilation PDF, including all current, repealed, and not-yet-answerable technical standards. Documents from official HTML, standalone files, service guides, attachments, and supplemental materials are outside this task.
- This is an audit only. Do not update `documents`, `pages`, units, raw/clean text, metadata, hashes, source JSON, or any active serving corpus. Do not create FTS, vector, embedding, ANN, or KG artifacts.
- The authoritative comparison source is the original compilation PDF recorded by each document's `source_trace`, not title-only inference, model memory, or a neighboring document's existing split.

### Required boundary audit

- For every in-scope document, inspect the included first and last global source pages and the immediately preceding/following global PDF pages when available. Use original-page rendering plus OCR/layout evidence where text alone is insufficient.
- Verify that the start boundary contains the document's own cover, standard number/title, or an explicitly documented legitimate first-content page. Identify a missing cover/title page, a preceding-document tail, a catalogue/table-of-contents page, a category divider, a blank page, or a duplicate page as a start-boundary anomaly.
- Verify that the end boundary contains the document's own final substantive content, appendices, references, or a documented legitimate terminal page. Identify a following-standard cover/content page, a category divider, a blank page, a catalogue page, or a duplicate page as an end-boundary anomaly.
- Detect both leakage and omission: a clean last page alone is insufficient when an own cover or a substantive first/last page may have been omitted.
- Reconcile all proposed ranges at compilation-PDF level. No proposed standard ranges may overlap. Any gap must be explicitly classified as a category divider, deliberate blank, table of contents, unassigned material, or a source/split ambiguity requiring review.

### Required audit artifact

- Produce a private machine-readable manifest and a readable report. Include one row for every in-scope document, not only anomalies.
- Each row must contain: document ID, standard number/title, source PDF, current global start/end, proposed global start/end, first/last included-page classification, adjacent-page classification, result (`pass`, `boundary_anomaly`, or `review_needed`), anomaly type, evidence page references, and a recommended non-mutating disposition.
- Use explicit anomaly values such as `missing_own_cover`, `leading_previous_document`, `leading_category_divider`, `leading_blank_page`, `trailing_next_document`, `trailing_category_divider`, `trailing_blank_page`, `duplicate_page`, and `source_ambiguity`. Do not collapse distinct causes into a generic empty-page label.
- Cross-reference already confirmed decisions for `DZ/T 0208-2020`, `DZ/T 0331-2020`, and `DZ/T 0347-2020`; do not silently implement them in this audit.

### Acceptance and completion report

- Report in-scope document count, pass/anomaly/review-needed counts, anomaly counts by type, and coverage of every compilation-derived document.
- Confirm every anomaly has source-page evidence and every `review_needed` case has a precise question in `coordination/kb_agent_questions.md`.
- Validate the untouched v4 SQLite corpus with `PRAGMA integrity_check` and report `ok`; before/after corpus and retrieval-artifact counts must be unchanged.
- Append a dated T023 completion entry to `coordination/kb_agent_status.md`, name the report/manifest paths, report the coverage and integrity result, state `cloud_sync_required=false`, and explicitly state that T022 remains the only task permitted to apply approved remediation.

## T024 Execution Contract

### Authoritative source and preservation

- Official full-text URL: `http://www.nrsis.org.cn/mnr_kfs/file/read/cfcc17356c3b3055ff9382f4b86f58a1`.
- Download the returned file into a private, non-serving v4 source-evidence directory such as `data/knowledge_base_v4/source_evidence/t024/`. Do not commit the PDF, full extracted text, Markdown, JSON, images, or comparison artifacts to GitHub.
- Before use, verify that the response is an openable PDF and that its visible content identifies `DZ/T 0479-2024《压覆矿产资源调查评估规范》` with its published code/date. Record source URL, retrieval timestamp, HTTP/content metadata when available, file size, SHA-256, and page count. If any identity check fails, stop and report a blocker rather than accepting the file by URL alone.
- Preserve the downloaded PDF byte-for-byte. The Markdown and parsed text are derivative artifacts, never replacements for the raw official PDF.

### Official parsing and Markdown artifact

- Parse the official PDF page by page. Prefer its text layer only when it faithfully represents visible content; use the isolated PaddleOCR environment for pages without a usable text layer or with a documented low-confidence/structural extraction failure.
- Create a private governed Markdown artifact and companion JSON/page manifest. The Markdown must retain stable heading/section structure and explicit source-page anchors. Preserve raw page text separately from any clean/normalized text and record hashes for both.
- Do not fill a missing official-page passage from the old draft, model knowledge, or a website transcription. Any unreadable official passage must be reported as unreadable, with page evidence.
- Classify the formal PDF's pages 2 and 6 from their actual rendered appearance. If they are truly blank, retain their original-page records as `intentional_blank_page`, generate no answer evidence from them, and do not treat them as OCR failures.

### Formal-versus-draft comparison and draft deprecation

- Compare the published PDF with the existing `压覆矿产资源调查评估规范-报批稿.pdf` using title, clause, appendix, reference, and printed-page anchors. Do not assume identical physical page numbers.
- Produce a private page/section alignment manifest with: official page(s), draft page(s), section path, comparison result (`same_content`, `format_only`, `published_text_changed`, `published_content_added`, `draft_only_content`, `unreadable`, or `unaligned`), concise difference description, and evidence references. Explicitly identify every normative clause or appendix that differs.
- Create a deprecation record for the draft with role `historical_draft_comparison`, `answer_eligible=false`, and reason `superseded_by_published_DZ/T_0479-2024`. Retain its source file/hash and comparison relationship; do not delete it or merge its text into the formal artifact.
- Do not mutate the v4 document/page/unit records in T024. T022 must apply the approved switch: formal published text becomes the candidate answer source and the draft remains excluded from positive retrieval.

### Acceptance and completion report

- Required outputs: official PDF source record, official Markdown/JSON page artifact, raw/clean page hashes, rendering/OCR quality report, page/section comparison manifest, and draft-deprecation record under private v4 paths.
- Report formal PDF page count, parsed/blank/unreadable page counts, Markdown heading/section count, alignment counts by result, all substantive differences, source hashes, and any unresolved question in `coordination/kb_agent_questions.md`.
- Confirm no FTS, vector, embedding, ANN, KG, cloud deployment, or active v4-corpus mutation occurred. Run `PRAGMA integrity_check` and report unchanged before/after v4 corpus/retrieval-artifact counts with `cloud_sync_required=false`.
- Append a dated T024 completion entry to `coordination/kb_agent_status.md`. T024 completion is evidence preparation only; activation remains blocked on T022 and user approval.

## T025 Execution Contract

### Scope

- Verify these P0-affected sources: `DZ/T 0208-2020`, `DZ/T 0331-2020`, `DZ/T 0347-2020`, `DZ/T 0479-2024`, `GB 50830-2013`, `GB/T 12719-2021`, `自然资规〔2023〕4号附件4《采矿权申请资料清单及要求》`, and `《矿产资源储量技术标准解读300问》`.
- Use authoritative platform priority: MNR official pages/files for policy and attachment 4; NRSIS for natural-resources industry standards; the national standard platform or other demonstrably official full-text source for GB/GB/T standards. For the interpretive book, verify edition/ISBN/source identity and retain its non-normative role; do not pretend it has a standard-platform full text.
- A title, status, release date, or search-result page verifies metadata only. It does not verify a clause, table, appendix, or page body.

### Official content verification

- Record every attempted official URL, retrieval time, response identity, SHA-256 when a file is obtained, and the resulting access category: `official_body_verified`, `official_metadata_only`, `official_body_unavailable`, or `content_conflict`.
- Where official full text is accessible, compare the local governed content with it at the P0-relevant pages/sections and the document's first/last substantive pages. Verify standard number/title, release/implementation date, the exact relevant content, and page/section correspondence. Do not infer unobserved text from model knowledge.
- Where an official platform exposes only metadata, record that limitation precisely. Do not call local text officially body-verified merely because its title/date matches the metadata.
- For `DZ/T 0479-2024`, use the official PDF acquired by T024 as the body-verification baseline. For attachment 4, use the official MNR DOC URL and retain the existing 21-row/80-cell independent benchmark as part of the ledger.
- Record any textual discrepancy, missing local page, extra local page, edition/version conflict, inaccessible source, or uncertain page alignment as a new review item in `coordination/kb_agent_questions.md` rather than changing text silently.

### Gating and completion

- Do not change v4 document/page/unit records, answerability, indexes, or cloud state in this task. T022 is the only mutation task.
- A normative source with `official_metadata_only`, `official_body_unavailable`, or `content_conflict` must be listed as ineligible for positive-answer activation pending a user-approved source decision. Do not promote it by title similarity or prior answer quality.
- Produce a readable report and machine-readable ledger with one source row per in-scope document, verification category, scope checked, official/local source references and hashes, comparison result, unresolved risks, and recommended T022 disposition.
- Run `PRAGMA integrity_check` and report unchanged before/after v4 corpus/retrieval-artifact counts. Append a dated T025 completion entry to `coordination/kb_agent_status.md`, set `cloud_sync_required=false`, and explicitly state whether T022 is ready or which gates remain.

## T026 Execution Contract

### Baseline and scope

- Operate only on the isolated post-T022 `data/knowledge_base_v4/` corpus. Do not change the v3 production database, serving API or cloud deployment.
- Verify the input database hash is `956766f34728bfe5b3e1da8056d342225b6317206072db99223a9c3a2272ae33` before the first mutation. If it differs, stop and reconcile the delta rather than forcing the script to run.
- Recompute scope from the current database. Expected active P1 scope is 142 low-confidence OCR findings on current documents, two active oversized units, and one archived oversized finding. Four low-confidence pages belonging to repealed `DZ/T 0325-2018` remain deferred P2 work.
- After finding-level governance, apply a document-level quality gate to all 70 current technical documents still marked `status_verified_pending_quality_review`; absence of a low-confidence finding alone is not sufficient for promotion.

### Semantic OCR governance

- Treat OCR confidence as a risk signal, not a correctness verdict. Review visible text, neighboring context, clause structure, tables and domain semantics before deciding whether any text is wrong.
- Semantic reasoning may classify a likely error, but it may not supply normative wording. Any correction must be copied from the visible original page, a verified official full text or a controlled OCR/layout result checked against the page image.
- Prefer local comparison of only the affected page, adjacent page or matching official clause when uncertainty remains. Do not rerun or replace an entire document solely to improve a confidence score.
- Classify every finding explicitly as accepted accurate, confirmed OCR error, resolved by local source comparison, requiring user decision, or deferred repealed-source P2.
- For unresolved cases, write a precise question in `coordination/kb_agent_questions.md` with page/clause identity, page evidence, existing text, candidate text and the exact disputed character, number, unit or structure. Continue unrelated work while waiting.
- Preserve source files, original text, clean text, old/new hashes, page/unit mappings and decision evidence. Never silently overwrite source or governed text.

### Structural and document governance

- Split the two active oversized `《矿产资源储量技术标准解读300问》` units only at explicit question numbers, headings, clauses, tables or source-page boundaries. Preserve wording and order exactly. Close the already archived unit's finding as superseded without recreating it.
- Keep the 300-question publication explicitly interpretive; it cannot override a current normative source.
- Promote a pending technical document to `approved_for_service` and `can_answer=1` only when identity/status, document boundary, readable body, page/unit mapping and every active P0/P1 finding pass. Otherwise retain it as unavailable and record a concrete hold reason.
- Do not create retrieval chunks, FTS, vectors, embeddings, ANN or KG in this task.

### Mutation and acceptance

- Before mutation, create a byte-identical rollback database and a dry-run decision manifest. Apply accepted updates in one transaction with assertion-driven rollback. The mutation script must be idempotent.
- Use `/home/nalanmading/.venvs/codex/bin/python` for PaddleOCR, PP-StructureV3 and table recognition, with CPU by default. Do not install OCR dependencies into the project or system Python.
- Completion requires `PRAGMA integrity_check=ok`, no foreign-key violations, exact reconciliation of every active-scope finding, unchanged text for accepted-accurate findings, evidence and hashes for each correction/split, and zero current positive-answer documents with unresolved P0/P1 findings.
- Report promoted and held document counts, all hold reasons, before/after database hashes and counts, rollback path, and repeat-run `already_applied` behavior.
- Run v4 `--validate-only` and the complete unit-test suite. Keep all content artifacts private, set `cloud_sync_required=false`, and do not mark T026 done while an unreported blocker remains.

## T026 Closure Record

- Closed at: 2026-07-25 (Asia/Shanghai).
- Disposition: independently accepted by the main agent after checking the report, database state and constraints, evidence/manifests, mutation idempotency, v4 validation, and the complete 207-test suite.
- Accepted database: `data/knowledge_base_v4/db/corpus.sqlite`, SHA-256 `a0c8e10d75b047d2476c2047881f1fad903f1dd6c8bbdc6308fc761b3ecd1610`.
- Accepted corpus counts: 155 documents, 3,644 pages, and 20,580 content units. `PRAGMA integrity_check=ok`; foreign-key violations: 0.
- Deferred work: four low-confidence pages belonging to repealed `DZ/T 0325-2018` remain P2 and do not block T026 closure.
- Retrieval gate: closure does not authorize a final chunk, FTS, embedding, ANN, knowledge-graph, cloud-sync, deployment, or production-service mutation.

## T030 Execution Contract

### Confirmed defect and authority

- The current `《中华人民共和国矿产资源法》` official full-text source is `https://f.mnr.gov.cn/202411/t20241109_2873922.html`, supplied and approved by the user.
- The original MNR crawl did not miss this page. T016 later deleted document `policy-679076ae4a72c1fa` because `第三十六号主席令`, published/formed on `2024-11-08`, was absent from the 222-row pre-2026 allowlist.
- The departmental `继续有效文件` workbook is not a complete negative-validity list for laws or administrative regulations. Absence from that workbook must not, by itself, delete or prohibit a law or administrative regulation.
- T030 is authorized to restore exactly the current `《中华人民共和国矿产资源法》` to isolated v4. All other T016-deleted candidates are audit-only until the user records a later disposition.

### Baseline and isolation

- Work only in `/home/nalanmading/My-project/my-1st-agent` and mutate only `data/knowledge_base_v4/db/corpus.sqlite` plus private v4 evidence/report artifacts and the governance code/tests required by this repair.
- Before mutation, require SHA-256 `a0c8e10d75b047d2476c2047881f1fad903f1dd6c8bbdc6308fc761b3ecd1610`, 155 documents, 3,644 page records and 20,580 content units. If any value differs, stop before mutation and report the observed delta.
- Preserve the accepted T026 database as a byte-identical private rollback copy. Produce a dry-run manifest before the transaction and record before/after database hashes and counts.
- Do not modify `data/knowledge_base/` or any v3/production database, do not connect to the cloud server, and keep `cloud_sync_required=false`.

### Official-source acquisition and governed restoration

- Fetch the supplied official MNR page and verify visible identity before use: title, `第三十六号主席令`, the `2024-11-08` revision/adoption statement, current status and the `2025-07-01` effective date. Preserve raw HTML byte-for-byte with retrieval timestamp, response metadata when available, file size and SHA-256.
- Do not restore the old one-chunk legacy representation from the rollback database as authoritative content. Parse the newly acquired official body in its original order into auditable chapter, article and paragraph evidence. Preserve exact wording and punctuation; do not summarize, modernize or complete text from model memory.
- Use one stable v4 document identity for the law and record `document_type=law`, current effective/review/answerability state, official URL/platform, bibliographic metadata and source-artifact mapping. Detect and reject duplicate title/source/identity insertion.
- HTML has no physical page numbers. Do not fabricate page citations. If v4 schema-compatible source records are created in `pages`, keep `page_no=NULL`, use the official URL/source anchor, and explicitly document that they are non-physical source records.
- Verify all articles are present and ordered without gaps or duplicates. Give special acceptance evidence for article 70 paragraph 2, which governs intentional submission of a false mineral-resource-reserve report. Provide the exact new v4 `unit_id` or evidence-unit group for later Q22 Gold activation, but do not independently rewrite unrelated Gold questions.

### Governance-rule correction and audit-only scope

- Correct the future allowlist gate so it is authority/document-type aware. Laws and administrative regulations must be governed from competent official sources and explicit status/repeal evidence; their absence from a departmental normative-document workbook is not negative validity evidence.
- Preserve the T016 workbook rule only for the document class it actually governs. Add tests demonstrating that a pre-2026 departmental document outside the applicable allowlist is still rejected while a verified current law or administrative regulation is not rejected solely for workbook absence.
- Read the T016 deletion manifest/dry-run evidence and produce a metadata-first audit of all other deleted candidates that may be laws or administrative regulations. At minimum include `《稀土管理条例》`. For each candidate record title, document number, authority level, official URL, T016 deletion reason, observed official status/evidence, relevance and recommended disposition.
- Do not restore, alter answerability, or ingest full text for any audit candidate other than `《中华人民共和国矿产资源法》`. Put uncertain authority/status questions in `coordination/kb_agent_questions.md`.

### Mutation, validation and handoff

- Implement an assertion-driven, transactional and idempotent repair. A repeat invocation must return `already_applied` and leave the repaired database hash unchanged.
- Completion requires `PRAGMA integrity_check=ok`, zero foreign-key violations, exact document/page/unit count reconciliation, complete source and hash mappings, no duplicate law record, and verified article 70 paragraph 2 evidence.
- Run `PYTHONPATH=src .venv/bin/python scripts/build_kb_rebuild_v4.py --validate-only` and the complete unit-test suite. Add focused tests for official-body structure, Q22 evidence availability, allowlist authority boundaries, rollback safety and idempotency.
- Do not create or rebuild final RAG chunks, production/experimental FTS, embeddings, ANN, vectors or knowledge graph. Do not refresh T027-T029 retrieval experiments in T030; report that those frozen benchmarks predate the corpus repair and require a separately approved rerun later.
- Write readable and machine-readable T030 reports under `data/knowledge_base_v4/reports/`, append an evidence-based completion entry to `coordination/kb_agent_status.md`, and move T030 to `review_needed` rather than `done`. The main agent must independently accept the report, database mutation, source evidence, governance tests and Q22 mapping before closure.

## T030 Closure Record

- Closed at: 2026-07-26 (Asia/Shanghai).
- Disposition: independently accepted by the main agent after reviewing the official-source artifacts, parser and mutation code, database/report reconciliation, rollback and idempotency evidence, allowlist boundary tests, the 24-record upper-law audit, Q22 mapping, v4 validation, and the complete 268-test suite.
- Accepted database: `data/knowledge_base_v4/db/corpus.sqlite`, SHA-256 `9f23c72ddc9406f3f526a0635c0b45321cd0d6d4f9ea36168d6ce38e4fcd1d97`.
- Accepted corpus counts: 156 documents, 3,645 page records, and 20,670 content units. `PRAGMA integrity_check=ok`; foreign-key violations: 0.
- Mutation isolation: comparison against the byte-identical T026 rollback database found no removed or changed pre-existing row in any v4 table; only the authorized law, its 90 units, one non-physical source record, version/artifact mappings, measurements, and T030 build record were added. The v3 hash remains `dfdc3479a2369e0c356791054c9486354b90a9226a5b1dfa04f2466b0da453a0`.
- Q22 evidence mapping is accepted: `unit-0a2e82a83cfc9249`, `第七十条`第二款. This closure does not itself edit or rerun the Gold evaluation set.
- Audit dispositions: accept the two superseded-law and six expressly repealed-regulation statuses; keep all eight deleted and audit-only. Preserve the MNR classification of `《煤炭生产许可证管理办法实施细则》` with `classification_conflict` until competent-authority evidence supports reclassification. No other audited candidate, including `《稀土管理条例》`, is authorized for restoration by this closure.
- Retrieval gate: at T030 closure the T027-T029 artifacts still predated the repaired corpus. The user subsequently and separately authorized Q22 Gold activation and a baseline-only refresh; current T027-T029 reports are pinned to the accepted T030 hash. No final chunk, production FTS, embedding, ANN, vector, knowledge graph, cloud sync, deployment, or service mutation was authorized or performed.

## Notes

- MVP knowledge base uses SQLite + FTS5 first; Elasticsearch/OpenSearch is a later scale upgrade path.
- MVP vector retrieval uses local deterministic hashed character n-gram vectors in SQLite; ChromaDB/FAISS is a later scale/quality upgrade path.
- MVP graph retrieval uses lightweight SQLite KG tables; Neo4j is a later graph upgrade path.
- Embedding provider must be configurable and must not use `deepseek-v4-flash`.
