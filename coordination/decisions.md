# Decisions

Confirmed cross-agent decisions should be recorded here.

## Confirmed

### 2026-07-08

- Product display name is `geowiki`; the Chinese subtitle is `一款专注地质领域的百科全搜`.
- MVP knowledge base prioritizes schema, standard catalog query, and Elasticsearch full-text search.
- ChromaDB vector search and Neo4j graph search are later enhancements.
- `deepseek-v4-flash` is used for generation, not embedding.
- Embedding provider must be configurable.
- OCR candidate tool is PaddleOCR / PP-StructureV3 / TableRecognitionPipelineV2.
- Uploaded third-party documents are private by default and may enter the controlled service-visible knowledge scope only after admin approval; the knowledge base itself is never public.
- Standard status priority: national standards prefer national standard official platforms; natural-resource industry standards prefer `nrsis.org.cn`; conflicts must be preserved and surfaced.
- Community edition uses AGPL-3.0.
- Enterprise edition uses a commercial license and may live in a private repository or outside GitHub.
- Public repositories must not include real standard PDFs, OCR full text, prebuilt standard knowledge bases, or prebuilt standard vector indexes.

### 2026-07-09

- MVP knowledge service may use SQLite + FTS5 first, as long as `/knowledge/search` and `/knowledge/standards` keep the agreed response contract; Elasticsearch/OpenSearch remains a scale/production upgrade path.
- Knowledge API should be a standalone FastAPI service under `/knowledge/*`; QA backend calls it through `KNOWLEDGE_BASE_URL` and must not read KB files directly.
- MVP knowledge service is trusted local/internal. External API auth, rate limiting, and usage logging are owned by the QA backend. KB schema should still store `visibility`, `owner_user_id`, `organization_id`, and `review_status`.
- Search and chunk APIs return capped evidence quotes by default. Full chunk text is only for trusted internal/local calls when explicitly requested.
- Upload/candidate/review workflows are schema/stub level for MVP; governed standards search and catalog come first.
- First governed-standard ingestion should include all governed outputs, but mark them `visibility=internal` by default, not externally public cloud content.
- Knowledge base assets are a protected moat: raw files, OCR text, chunk text, indexes, DB files, catalog internals, and KB APIs must not be publicly exposed. Commercialization exposes only the controlled QA API output.

### 2026-07-11

- `/home/nalanmading/下载/0. 继续有效文件.xls` is the authoritative validity allowlist for previously web-crawled MNR files published before 2026. Pre-2026 crawled files absent from the workbook must be removed from the active knowledge base and all derived retrieval artifacts, and the ingest flow must prevent them from being reintroduced. Files published in 2026 or later are outside this cleanup rule.
- The 40 numbered Markdown files under `/home/nalanmading/下载/2. geowiki` form a separate official MNR mineral-service-guide corpus and must be ingested as structured, internally visible service-guide documents. `_INDEX.md` is a corpus manifest only. These guides are outside the pre-2026 normative-document allowlist cleanup rule.

### 2026-07-24

- `《矿产资源储量技术标准解读300问》` may be cited as an interpretive reference. Every answer and source display that uses it must explicitly identify the source title as `《矿产资源储量技术标准解读300问》`; it is not a normative standard and must not override, replace, or establish a conclusion contrary to an applicable current law, regulation, standard, or official service guide.
- P0 source-quality findings will be reviewed and decided one by one before any corpus mutation. After every P0 finding has a recorded disposition, the KB agent must perform one unified, controlled v4 governance/remediation run rather than applying piecemeal changes. The run must preserve raw sources and mappings, record before/after hashes and decisions, validate integrity, and must not build FTS, vectors, embeddings, ANN, or KG artifacts until separately approved.
- For `DZ/T 0208-2020《矿产地质勘查规范 金属砂矿类》`, the governed source boundary is compilation PDF global pages 355-387. Current split pages 34 (global page 388, category divider) and 35 (global page 389, blank) are out of scope and must be removed in the later unified remediation. This is a source-boundary defect, not an OCR repair.
- For `DZ/T 0331-2020《地热资源评价方法及估算规程》`, the governed source boundary is compilation PDF global pages 1151-1198. Current split page 49 (global page 1199) is a verified completely blank trailing page; page 48 ends the standard with its complete reference list and the source compilation PDF itself ends at page 1199. Remove only current split page 49 in the later unified remediation. This is a trailing-page boundary defect, not an OCR repair.
- For `DZ/T 0347-2020《矿山闭坑地质报告编写规范》`, the governed source boundary is compilation PDF global pages 575-583. Current split page 10 (global page 584, `二、能源矿产类` category divider) and page 11 (global page 585, completely blank) are out of scope; global page 586 starts `DZ/T 0215-2020《矿产地质勘查规范 煤》`. Remove current split pages 10-11 in the later unified remediation. This is a source-boundary defect, not an OCR repair.
- For `DZ/T 0479-2024《压覆矿产资源调查评估规范》`, the supplied official full-text source is `http://www.nrsis.org.cn/mnr_kfs/file/read/cfcc17356c3b3055ff9382f4b86f58a1`. The existing `压覆矿产资源调查评估规范-报批稿.pdf` is deprecated as a historical comparison source: it must never provide positive-answer evidence or be merged into published-standard text. The formal PDF must be downloaded, hash-preserved, parsed into a governed Markdown artifact, and compared page-by-page against the draft before the formal text can replace the draft in the unified T022 remediation run.
- For `GB 50830-2013《冶金矿山采矿设计规范》`, source PDF page 111 is a verified non-content transition page between the explanatory preface and explanatory table of contents. It contains only scan/binding artifacts, not recoverable standard content. Retain the raw page and page mapping, classify it as `intentional_blank_page_with_scan_artifacts`, generate no retrievable unit/evidence, and reclassify the empty-page finding as accepted rather than requiring OCR or deletion.
- For the same `GB 50830-2013` source, retain all 176 local page records and their source mappings for audit, but exclude local pages 1-4 and 109-176 from positive retrieval. Local pages 5-108 map one-to-one to the 104-page MOHURD official standard body and are the only page-derived normative answer source. Manually curated table units may remain answer-eligible only when their captions/context map to that normative body. This conservative, reversible disposition resolves `T025-Q001`; the user's explicit 2026-07-25 instruction to execute T022 is the activation approval for this disposition and the unified mutation run.
- For `GB/T 12719-2021《矿区水文地质工程地质勘查规范》`, source PDF page 2 is a verified completely blank page between the cover and table of contents. Retain the raw page and page mapping, classify it as `intentional_blank_page`, generate no retrievable unit/evidence, and reclassify the empty-page finding as accepted rather than requiring OCR or deletion.
- For `《矿产资源储量技术标准解读300问》`, source PDF pages 4, 12, 134, and 136 are verified intentional blank divider pages: respectively between the editorial list and preface, the solid-mineral divider and content, the end of solid-mineral content and the oil/gas divider, and the oil/gas divider and subsequent content. Retain their raw page mappings, classify all four as `intentional_blank_page`, generate no retrievable unit/evidence, and reclassify the empty-page findings as accepted rather than requiring OCR or deletion. The document's separate interpretive-only citation rule remains in force.
- For `自然资规〔2023〕4号附件4《采矿权申请资料清单及要求》`, the official raw DOC is available locally and its controlled DOCX conversion matches the existing derivative text exactly. An independent OOXML-table benchmark verified all 21 logical material rows, all 80 required (`▲`) application-material cells, all material names, and all `要求` texts against the 80 v4 application-material units. Keep the existing content units and source artifacts. The current v4 attachment page model is invalid: its 93 `pages` records have no physical page numbers and merely duplicate 80 row units, 4 application summaries, 8 table summaries, and 1 overview. During T022, remove these pseudo-page records, retain the 93 content units with official-DOC/table-row source references, and set page count to unknown/not-applicable rather than 93. Do not fabricate physical page citations.
- T026 is independently accepted and formally closed. The accepted v4 database SHA-256 is `a0c8e10d75b047d2476c2047881f1fad903f1dd6c8bbdc6308fc761b3ecd1610`, with 155 documents, 3,644 pages, and 20,580 content units; integrity, foreign-key, idempotency, v4 validation, and the complete 207-test suite passed. The four deferred low-confidence pages from repealed `DZ/T 0325-2018` remain P2. Retrieval artifacts and cloud activation require a separate decision.
- v4 retrieval development will proceed as controlled incremental A/B experiments on the frozen T026 corpus. Start with the simplest exhaustive lexical search across all retrieval-eligible v4 evidence, then change one variable at a time and measure its incremental value before introducing dense vectors, ANN, or a knowledge graph. Model-generated pre-retrieval answers may be tested only as unverified search hypotheses: the original-question retrieval route remains independent, supporting and contradictory/exception evidence must both be searched, and no hypothesis may enter the final answer without direct governed evidence. A fixed evidence-level evaluation set and unchanged corpus snapshot are required for every comparison.
- T027 adopts a reproducible retrieval-only derived layer while keeping `content_units` and `corpus.sqlite` immutable. All 3,944 short source units receive document/standard/section and available neighbour context; all 156 long source units have an explicit split/retain/reconstruct decision; all 1,224 cross-page units retain page-record and hash evidence. The 151 derived leaves still requiring human review remain searchable as risk signals but are all prohibited from direct citation; 38 of those leaves remain at least 1,200 characters because no safer verified boundary is available. Final chunks, FTS, embeddings, ANN, knowledge graph, v3 mutation, cloud sync, and deployment remain unstarted.
- The controlled T027 pre/post A/B fixes the 21 questions, existing DeepSeek rewrites, scorer, and database hash. After the user-approved Q23A Gold correction, on the 20 scored questions semantic rewrite retrieval improved `Unit Hit@10` from 45% to 55%, `Doc Hit@5` from 65% to 70%, `MRR@20` from 0.236 to 0.259, and `Coverage@20` from 45.7% to 53.9%. Three-route fusion improved `Coverage@20` from 45.7% to 61.9% but reduced `Doc Hit@5` from 75% to 70% and `MRR@20` from 0.177 to 0.162. These regressions must be addressed through later scoring, parent-child expansion, and retrieval design rather than changing the Gold answers.
- T028 records a benchmark result, not production adoption. Conservative parent-child expansion leaves ordinary single-point rankings unchanged, uses parent nodes only for navigation/ranking, expands attachment 4 only within the explicit application branch, and permits bounded parent expansion only for condition-bundle questions. Across all four query arms, the two attachment-list questions reached 100% `Complete List Recall@20` with 0% `Wrong-branch Contamination@20`; semantic-rewrite `Unit Hit@10` rose from 55% to 60%, `MRR@20` from 0.259 to 0.338, and `Coverage@20` from 53.9% to 60.0%; three-route-fusion `Coverage@20` rose from 61.9% to 70.0% without reducing `Unit Hit@10`. Q16 fusion evidence coverage rose from 50% to 100%. On 2026-07-25 the user approved expanding Q23A from 13 to all 14 new-establishment evidence rows; `runit-451b99d810dd9865`, item 5 `矿山地质环境保护与土地复垦方案公告结果`, is now included, while its extension-specific requirements-column wording must remain visible in any answer. Q23A Gold and the official branch now reconcile at 14/14 with no missing or out-of-branch IDs. Verification passed all 230 tests, read-only SQLite integrity and foreign-key checks, and the v4 database hash remained `a0c8e10d75b047d2476c2047881f1fad903f1dd6c8bbdc6308fc761b3ecd1610`. Final chunks, FTS, embeddings, ANN, knowledge graph, v3 mutation, cloud sync, and deployment remain unstarted.
- T029 isolates FTS candidate filtering from BM25 ranking and does not adopt a production index. Native SQLite trigram was rejected after its semantic-rewrite FTS candidate stage recalled only 80.5% of Gold evidence groups, principally because two-character Chinese terms such as `详查`, `延续`, and `办理` were not indexed. The accepted experimental comparator uses deterministic Chinese bigram/trigram tokens in a private 23,160-row FTS5 file; it reached 100% candidate-group recall with no fallback, built in 6.48 seconds, and occupied 82.64 MiB. FTS filtering followed by the current scorer retained semantic-rewrite `Unit Hit@10` at 55% and raised `Coverage@20` from 53.9% to 59.3%, but reduced P50 latency only from 476.3 ms to 440.3 ms because the mean candidate set remained 15,340 leaves. Direct BM25 reduced semantic-query P50/P95 to 31.4/47.2 ms and slightly raised `MRR@20` from 0.259 to 0.268, but reduced `Unit Hit@10` from 55% to 50%; on three-route fusion it raised `MRR@20` from 0.162 to 0.270 but reduced `Coverage@20` from 61.9% to 60.0%. BM25 also worsened individual cases including Q01, Q09, Q14, and Q23B, and flat application-material results still mixed branches. Therefore BM25 is retained only as a promising fast lexical route to be considered with protected exact-term rules and the already validated parent-child expansion; it is not a standalone replacement. Verification passed all 235 tests, both corpus and experimental-index integrity checks, zero corpus foreign-key violations, and an unchanged corpus hash `a0c8e10d75b047d2476c2047881f1fad903f1dd6c8bbdc6308fc761b3ecd1610`. No embedding, ANN, knowledge graph, cloud sync, deployment, v3 mutation, or final production FTS was created.
- T030 corrects a confirmed T016 governance-boundary defect: the original MNR crawl captured the current `《中华人民共和国矿产资源法》`, but T016 deleted it because the pre-2026 `第三十六号主席令` was absent from a 222-row departmental-document allowlist. The user approved restoring exactly this law to isolated v4 from `https://f.mnr.gov.cn/202411/t20241109_2873922.html`. Laws and administrative regulations must be governed by competent official sources and explicit status/repeal evidence; absence from the departmental `继续有效文件` workbook is not negative validity evidence. Other T016-deleted laws/regulations, including `《稀土管理条例》`, are audit-only and require a later user decision before restoration. T030 must not modify v3 or build/rebuild FTS, embeddings, ANN, vectors or knowledge graph, and must keep `cloud_sync_required=false`.

### 2026-07-26

- T030 is independently accepted and formally closed. The accepted v4 database SHA-256 is `9f23c72ddc9406f3f526a0635c0b45321cd0d6d4f9ea36168d6ce38e4fcd1d97`, with 156 documents, 3,645 page records, and 20,670 content units. The restored current `《中华人民共和国矿产资源法》` is an answerable law with 8 chapters, 80 complete ordered unique articles, and 90 structural units derived from the hash-preserved official MNR full-text HTML. Integrity, foreign keys, rollback, idempotency, v4 validation, artifact hashes, row-level mutation isolation, 12 focused tests, and the complete 268-test suite passed; v3 remained unchanged and no retrieval index or cloud action occurred.
- Q22 evidence mapping is accepted at `unit-0a2e82a83cfc9249`, `第七十条`第二款. The paragraph must be cited with its complete consequences and serious-circumstance qualifier; accepting the mapping does not itself alter or rerun the Gold evaluation set.
- Accept the T030 upper-law audit dispositions for the two superseded law pages and six expressly repealed administrative-regulation pages; keep them deleted and audit-only. For `《煤炭生产许可证管理办法实施细则》`, preserve the official MNR `行政法规` classification with `classification_conflict` until competent-authority evidence justifies reclassification. No other audited candidate, including `《稀土管理条例》`, is restored without a separate user decision.
- The user subsequently authorized Q22 Gold activation and refresh of the affected T027-T029 baselines. Q22 is now the 21st scored case, requires `unit-0a2e82a83cfc9249`, and preserves the complete article 70 paragraph 2 consequences: confiscation of illegal gains, a RMB 200,000-1,000,000 fine, and recovery of the mining right by the original grantor in serious circumstances. There are no diagnostic-only cases in the frozen Gold set.
- The T027 refresh is pinned to v4 SHA-256 `9f23c72ddc9406f3f526a0635c0b45321cd0d6d4f9ea36168d6ce38e4fcd1d97`: 19,356 eligible source units became 23,250 derived leaves and 4,445 parents; all 48 Gold evidence groups resolve. Semantic-rewrite preprocessing raised `Unit Hit@10` from 47.6% to 57.1%, `MRR@20` from 0.213 to 0.262, and `Coverage@20` from 48.3% to 56.1%. Three-route-fusion `Coverage@20` rose from 48.3% to 63.7%, while its `MRR@20` slightly fell from 0.173 to 0.169.
- The T028 refresh retains the prior design decision: semantic-rewrite parent-child retrieval raised `Unit Hit@10` from 57.1% to 61.9%, `MRR@20` from 0.262 to 0.337, and `Coverage@20` from 56.1% to 61.9%; three-route-fusion `Coverage@20` rose from 63.7% to 71.4%. Both application-list questions reached 100% complete-list recall with zero wrong-branch contamination. Q22 stayed at rank 3 with 100% `Coverage@20`, confirming that conservative parent expansion did not disturb this single-clause legal-responsibility case.
- The T029 refresh rebuilt only the isolated private experimental FTS5 comparator: 23,250 rows, 83.85 MiB, SHA-256 `4aa39d2121d5168690e42dbed099e33c5dd888207777b28a3b9b8e4cbeaf6c2f`, 100% candidate-group recall, and zero fallback. For semantic rewrites, FTS filtering plus current scoring kept `Unit Hit@10` at 57.1%, raised `Coverage@20` from 56.1% to 61.2%, and reduced P50 from 488.8 ms to 449.4 ms; direct BM25 reduced P50 to 31.5 ms but lowered `Unit Hit@10` to 52.4%. Q22 ranked 3rd under full scan and 4th under both FTS reranking and BM25 because near-match provisions about report authenticity, inspection fraud, and other fraud contexts outranked the exact penalty. BM25 therefore remains experimental and is not a standalone replacement.
- Refresh verification passed all 271 tests, v4 `--validate-only`, corpus and experimental-index integrity checks, zero corpus foreign-key violations, compilation, and diff checks. The v4 and v3 hashes remained `9f23c72...1d97` and `dfdc3479...53a0`. No production FTS, final chunk, embedding, vector, ANN, knowledge graph, cloud sync, deployment, or service mutation occurred; `cloud_sync_required=false`.
- During user review of the 107-question package, citation display is tightened: a document title and clause/table locator alone are insufficient. Every already-located answer must also expose the concrete citation-safe clause, subclause, or table-row content. For the 21 existing Gold cases, these visible texts come from the T027 derived leaves while source `unit_id` locations remain visible for provenance; unsafe source-unit text must not be expanded directly.
- For Q14 and Q15, the required answer and score use only the direct `自然资规〔2023〕4号` rule that a conversion application relies on an assessed/filed reserve report and that exploration degree varies by mine scale/type. `DZ/T 0430-2023` note 2 to clause 3.1 is retained only as score-neutral optional evidence: retrieval neither adds nor loses credit, and the statement is removed from the canonical answer. This clarifies answer/citation presentation without changing required Gold groups, so T027-T029 baselines do not require another rerun. The updated review-package checks and the complete 273-test suite passed.
- The user accepted the 107-item QA review source after the identified professional corrections. Q17 is split into two distinct Gold questions for common and associated minerals, and Q25 into four questions for mining-right new establishment, extension, change, and cancellation; the accepted expanded set therefore contains 111 cases rather than mechanically preserving 107. Of these, 110 have directly scored T027 retrieval-leaf evidence. R034 is retained as one explicit abstention Gold case because no current v4 provision determines whether exploration-degree resource-ratio evaluation must use cumulative discovered or retained resources; the prior positive answer and its mismatched scale-classification citations are withdrawn. R056 now requires both the applicable mineral-specific stage clause and `DZ/T 0340-2020` clause 5.2.2 hierarchy evidence. Query and answer prompts require both standard roles, allow a higher test to cover a lower test only when the supplied normative text establishes the relationship, and preserve conflicts. The v4 corpus remains SHA-256 `9f23c72ddc9406f3f526a0635c0b45321cd0d6d4f9ea36168d6ce38e4fcd1d97`; validation, integrity/FK checks, and all 280 tests passed. No production FTS, embedding, ANN, knowledge graph, cloud sync, deployment, v3 mutation, or retrieval-baseline refresh occurred.
- T032 freezes the 111-case input contract for the next controlled keyword-retrieval experiments without running retrieval. Every case retains the original question independently alongside a DeepSeek `deepseek-v4-flash` natural-language rewrite, keyword query/array, anchors, and parallel subqueries. All 111 rewrites pass deterministic validation; 14 model/user-confirmation flags remain visible but are non-blocking for this user-preauthorized benchmark, while a later interactive system will require confirmation. Any model-added standard, document, clause, or conclusion not grounded in the original question is removed from the primary semantic rewrite and may survive only as an explicitly unverified parallel-search hypothesis; it cannot become a hard filter or answer evidence.
- T032 also freezes evidence and data-partition boundaries. The 110 retrieval-scored cases retain their required evidence groups and clause/table/document provenance; Q14/Q15 keep `DZ/T 0430-2023` clause 3.1 note 2 only as score-neutral optional evidence, and pre-existing optional Q18 appendix-table evidence is preserved rather than discarded. Q23/Q25 application types carry explicit sibling-branch contamination rules. R034 remains an abstention case and forbids the unsupported positive claim that cumulative discovered resources must be used. Semantic siblings and condition variants are grouped before the deterministic split, yielding development 72, validation 22, and holdout 17 with no family crossing sets and both technical-standard and legal/policy/service coverage in each set. Verification passed all 290 tests, v4 `--validate-only`, database integrity and zero foreign-key violations; corpus SHA-256 remained `9f23c72ddc9406f3f526a0635c0b45321cd0d6d4f9ea36168d6ce38e4fcd1d97`. No retrieval A/B, production FTS, embedding, vector, ANN, knowledge graph, cloud sync, deployment, or service mutation occurred; `cloud_sync_required=false`.
- T033 changes only the keyword query representation. On the same 23,250 flat T027 leaves and the same exhaustive lexical scorer, A uses the original question while B uses the DeepSeek `lexical_query` plus weighted `lexical_terms`. Development and validation contributed 93 scored cases; R034 remained outside positive retrieval scoring and the 17-case holdout was not executed. Overall A/B results were: clause-level `Unit Hit@10` 89.2%/78.5%, `Doc Hit@5` 89.2%/90.3%, `MRR@20` 0.658/0.628, evidence-group `Coverage@20` 89.6%/84.8%, and complete-evidence success 84.9%/81.7%. Validation independently showed `Unit Hit@10` 90.9%/77.3% and `Coverage@20` 90.9%/81.8%. Thus B slightly improved finding the correct document but materially worsened locating the correct clause, which is the primary GeoWiki objective.
- The T033 subgroup result is retained as a design signal rather than averaged away: on nine multi-evidence cases, B raised `Unit Hit@10` from 66.7% to 88.9% and `Coverage@20` from 57.4% to 79.6%; on 76 single-evidence cases it reduced those metrics from 90.8% to 77.6% and from 93.4% to 85.5%. Application-branch contamination also rose from 3.0% to 6.8%. Therefore keyword JSON is rejected as a standalone replacement for the original-question route, but may later be tested as a bounded auxiliary route for multi-evidence questions. This decision does not authorize fusion, protected routing, FTS/BM25, parent-child expansion, vectors, embeddings, ANN, knowledge graph, holdout execution, cloud sync, deployment, or service mutation. Verification passed all 300 tests, v4 `--validate-only`, database integrity and zero foreign-key violations. The v4 database remained SHA-256 `9f23c72ddc9406f3f526a0635c0b45321cd0d6d4f9ea36168d6ce38e4fcd1d97`; `cloud_sync_required=false`.
- T034 tests keyword JSON only as a document-candidate stage, followed by the unchanged original question for clause retrieval. Candidate Top-N was selected on 71 development cases from the fixed grid 5/10/20/30 solely by required evidence-document-group retention, without consulting validation clause metrics. Top-5, Top-10, Top-20, and Top-30 retained 81.5%, 88.5%, 99.2%, and 100% of development evidence document groups respectively; Top-30 was therefore frozen. It also achieved 100% candidate document-group recall and a 100% candidate-complete rate on all 22 validation cases. The selected scope averaged 9,545 leaves overall, 41.1% of the 23,250-leaf corpus.
- The T034 clause result is modest and mixed. Overall A fullscan versus B document-then-clause produced `Unit Hit@10` 89.2%/90.3%, `MRR@20` 0.658/0.679, `Coverage@20` 89.6%/88.9%, complete-evidence success 84.9%/83.9%, and wrong-branch contamination 3.0%/4.8%. On validation, B preserved identical `Unit Hit@10` 90.9% and `Coverage@20` 90.9% while raising `MRR@20` from 0.601 to 0.631. Q23A/Q23B coverage improved, but file selection cannot distinguish new-establishment/extension/change/cancellation sections inside the same attachment; explicit branch protection remains necessary. One development case, QA032/R043, moved from rank 19 to 35 and lost `Coverage@20`, despite its evidence document being retained, because re-scoring inside the subset changed relative clause ranking. The two-stage structure is therefore retained as an experimental candidate, not adopted as a final strategy. Current full-scan stage 1 plus stage 2 is slower (P50 857 ms versus 785 ms); only a later controlled FTS/BM25 candidate-stage experiment can establish an efficiency benefit. This decision does not authorize holdout execution, FTS/BM25, branch protection, fusion, natural-language rewrite use, vectors, embeddings, ANN, knowledge graph, cloud sync, deployment, or service mutation. Verification passed all 312 tests, v4 `--validate-only`, database integrity and zero foreign-key violations. The v4 database remained SHA-256 `9f23c72ddc9406f3f526a0635c0b45321cd0d6d4f9ea36168d6ce38e4fcd1d97`; `cloud_sync_required=false`.
- T035 compares only the Top-30 document-routing implementation under the frozen T034 architecture. A scores all 23,250 leaves with the current lexical formula; B uses FTS MATCH to reduce the mean first-stage population to 13,699 leaves and applies the same formula; C uses BM25 over exactly B's FTS population. All three restore the original question for the unchanged second-stage clause search. Across 93 development/validation cases, A/B/C achieved required-document-group recall of 100%/100%/99.4%, concrete-evidence hit rate at 10 (`Unit Hit@10`) of 90.3%/90.3%/90.3%, first-evidence mean reciprocal rank at 20 (`MRR@20`) of 0.679/0.674/0.671, and required-evidence coverage at 20 (`Coverage@20`) of 88.9%/88.9%/88.6%.
- T035 therefore retains FTS candidate filtering plus the current scorer as the safer experimental first-stage option: versus A it preserved `Unit Hit@10`, `Coverage@20` and validation's 100% required-document-group recall, with a 0.005 `MRR@20` decrease, while reducing total P50 latency from 857.0 to 738.4 ms, a reduction of 118.6 ms or 13.8%. BM25 reduced P50 further to 480.7 ms, but validation Top-30 completeness fell to 21/22 because QA010/Q14 lost required `自然资规〔2023〕4号`; that document was present in the shared FTS pool, proving a BM25 ordering loss rather than an FTS candidate miss. BM25 also reduced QA020/Q23B `Coverage@20` from 80% to 50%. Strict Top-30 BM25 routing is therefore not adopted. No compensation experiment, larger Top-N, branch protection, fusion, semantic rewrite, vector, ANN, knowledge graph, production FTS, holdout execution, cloud action, deployment, or service mutation is authorized by this result. Verification passed the 12 focused tests and all 324 tests, v4 `--validate-only`, corpus/index integrity and zero foreign-key violations. Corpus SHA-256 remained `9f23c72ddc9406f3f526a0635c0b45321cd0d6d4f9ea36168d6ce38e4fcd1d97`; experimental FTS SHA-256 remained `4aa39d2121d5168690e42dbed099e33c5dd888207777b28a3b9b8e4cbeaf6c2f`; `cloud_sync_required=false`.
- T036 uses the user-approved Alibaba Cloud API with `qwen3.7-text-embedding`, 1024-dimensional dense output, asymmetric `document`/`query` roles and one fixed general retrieval instruction. The unchanged 23,250 T027 leaves were embedded as private experimental documents; only the 93 development/validation original questions were embedded as queries. The vector arm searches every leaf by exact cosine similarity and uses no rewrite, keyword JSON, model-generated answer hypothesis, routing filter, parent-child expansion, branch protection, fusion, ANN or knowledge graph. R034 remains outside positive retrieval, and all 17 sealed holdout questions remain unembedded and unexecuted.
- T036 establishes strong independent semantic value. Against the safe T035 FTS-plus-current-scoring baseline, exact dense retrieval raised concrete-evidence hit rate at 10 (`Unit Hit@10`) from 90.3% to 96.8%, correct-document hit rate at 5 (`Doc Hit@5`) from 88.2% to 94.6%, first-evidence mean reciprocal rank at 20 (`MRR@20`) from 0.674 to 0.777, required-evidence coverage at 20 (`Coverage@20`) from 88.9% to 95.9%, and complete-evidence success from 83.9% to 90.3%; wrong-branch contamination fell from 4.8% to 1.6%. Validation independently reached 100% `Unit Hit@10` and 98.2% `Coverage@20`. Measured online P50 was 203.0 ms, comprising 195.0 ms query Embedding and 7.1 ms exact local scan, versus 738.4 ms for the current Python lexical implementation; the latency comparison is implementation-specific and is not evidence that every dense deployment will be faster than an optimized lexical engine.
- Dense retrieval is not adopted as a standalone replacement. Coverage improved on 13 cases but regressed on four: QA022 missed one of 11 industrial-indicator evidence groups within Top-20, QA028 pushed a cross-document supporting clause to rank 565, QA031 retained only half of its eight multi-standard evidence groups, and exact numeric-threshold QA075 placed its required clause at rank 256 and scored zero `Coverage@20`. These failures demonstrate complementary lexical strengths and justify a later separately approved parallel-fusion experiment, not deletion of the protected lexical route or immediate production activation. The private document/query caches contain 23,250/93 normalized vectors, consumed 4,724,676 input Tokens in 1,256 calls, and cost an estimated RMB 2.3623 before free quota. Document/query vector SHA-256 values are `8a5d0b30858e6e0d586712492089884bbba1cc06bfdc755ea9ed62c84771644f` and `da4371299bb870781a943a4ec721f0fa5d9359a4e91cd5c42f63c981ea3c78d0`. Verification passed 14 focused tests and all 338 tests, v4 `--validate-only`, corpus integrity and zero foreign-key violations. Corpus SHA-256 remained `9f23c72ddc9406f3f526a0635c0b45321cd0d6d4f9ea36168d6ce38e4fcd1d97`; no ANN, production vector index, production FTS, knowledge graph, cloud deployment, service mutation or v3 mutation occurred; `cloud_sync_required=false`.
- T036's 23,250 document vectors are promoted from a disposable experiment cache into the durable private snapshot `qwen37_text_embedding_1024_t027_v1`. The snapshot is bound to the v4 corpus, T027 leaves, exact Embedding input, `qwen3.7-text-embedding`, 1024 dimensions, and vector/mapping hashes; its mapping contains IDs and hashes but no source text. The snapshot itself is immutable by hash. If the active corpus later evolves, rows whose text hash, model, dimension and text contract remain unchanged may be reused; only new, changed, removed/superseded or contract-incompatible leaves require reconciliation or re-embedding. This is storage governance, not production activation, ANN creation or an assertion that source knowledge can never change.
- T037 establishes the candidate-set architecture: keyword and vector retrieval should execute in parallel, then contribute a deduplicated union. Across 93 development/validation cases, A keyword, B vector, C union with equal-weight reciprocal-rank fusion, and D intersection achieved concrete-evidence hit rate at 10 (`Unit Hit@10`) of 90.3%/96.8%/97.8%/95.7%, first-evidence mean reciprocal rank at 20 (`MRR@20`) of 0.674/0.777/0.797/0.793, and required-evidence coverage at 20 (`Coverage@20`) of 88.9%/95.9%/97.4%/93.4%. Of 156 required evidence groups, 139 appeared in both Top-50 routes, 10 only in vector, 5 only in keyword and 2 in neither; union candidate recall was 98.7%, versus 89.1% for intersection. Intersection never improved coverage over the union and regressed six cases, so intersection is rejected as the retrieval candidate set.
- Equal-weight reciprocal-rank fusion is retained only as a diagnostic baseline, not the final ranking rule. It rescued QA075's exact numeric evidence from vector failure at final rank 9, but cross-route agreement pushed QA084's vector-only required evidence from vector rank 4 to fused rank 26; QA020 also lost coverage, and QA026/QA030 had complete union candidates that were not fully preserved in the final Top-20. A later separately approved experiment should evaluate route-level preservation or protected evidence before any production decision. T037 reused frozen T036 Top-50 rankings, made no new Embedding API calls, and did not use anchor protection, route quotas, parent-child expansion, reranking, ANN, knowledge graph, production indexes, holdout execution, cloud sync, deployment, service mutation or v3 mutation. Verification passed 16 focused tests and all 354 tests, both T037 and durable-vector `--validate-only`, v4 `--validate-only`, database integrity and zero foreign-key violations. Corpus/vector/mapping/manifest/T037-result SHA-256 values are `9f23c72...1d97`, `8a5d0b3...644f`, `862ccce...074c`, `736b6aa...c911`, and `3927e98...0c5`; all private data artifacts remain Git-ignored and `cloud_sync_required=false`.
- Gold v3 is an amendment to, not an overwrite of, the accepted v2 audit artifact. Retrieval evidence groups are identities of retrievable evidence, so multiple answer claims supported by one leaf must not count that leaf more than once in retrieval coverage. QA029 and QA034 each had one duplicate group removed while their two distinct answer claims remain separately auditable. QA034's `DZ/T 0479-2024` B.7.4 is no longer unconditionally required: it activates only when the user reports an objection to the investigation result and enters the joint-selection/argument path. The general answer must explicitly distinguish query/investigation, no-objection handling, and the objection/argument branch.
- Current T027 leaves intentionally do not use blanket 5-10-character overlap. They are cut at visible semantic boundaries rather than fixed lengths; cross-page continuous units are already stitched, and short units receive non-citable neighbouring search context. Citation text must remain atomic. Blanket overlap would duplicate normative wording across evidence IDs, distort BM25/lexical term frequency, create citation deduplication ambiguity, and change all document Embedding inputs. If a future source requires fixed-length fallback splitting, overlap may be tested only in its search representation while preserving citation provenance; it is not applied to the present 23,250 leaves.
- T038 resolves the two apparent T037 candidate misses. QA015's `GB/T 13908-2020` clause 8.7.2.2 was present at dense rank 60 and lexical stage-two rank 264; QA034's conditional B.7.4 was at dense rank 53 and lexical stage-two rank 63. On Gold v3's 153 unconditional development/validation evidence groups, keyword-50/vector-50 retained 152, while keyword-50/vector-60 retained all 153 and the one conditional group; average union size rose only from 86.0 to 94.7. Therefore keyword Top-50 plus vector Top-60 is selected as the next ranking experiment's candidate boundary.
- Generic visible-numbering family expansion is not selected as a default route. It also retained 153/153, but activated on 38 cases, inserted 1,586 extra candidates and increased the average pool to 103.0; combining it with vector Top-60 increased the average to 112.5 without additional recall. The mechanism remains useful only after a document or clause family is independently confirmed, where it can expand `8.7.2.*` or `B.7.*` without opening unrelated families. T038 did not rerank final Top-20, so T037's RRF ordering problem remains the next separate question.
- T038 verification passed 13 focused tests and all 367 tests, Gold v3 and T038 `--validate-only`, v4 `--validate-only`, database integrity, zero foreign-key violations, compilation and diff checks. Corpus/T027/Gold-v3/T038-result hashes are `9f23c72...1d97`, `6d96def...e72`, `a45a308...6ca`, and `1f916d2...42c`. Private outputs remain Git-ignored; no corpus, retrieval text, vector, v3 production KB, cloud, service, ANN, KG or production-index mutation occurred; `cloud_sync_required=false`.
- T039 defines route-head protection as Top-20 admission, not score boosting: protected candidates may displace the lowest unprotected RRF members, but every RRF score and the relative RRF order remain unchanged. Both routes must have a nonzero head. On the 71-case development grid from 1 through 10, selection first maximized complete evidence success and `Coverage@20`, applied no-regression guards for `Unit Hit@10`, `MRR@20` and wrong-branch row count, and then chose the smallest actual intervention. This froze keyword Top-1 and vector Top-4 before validation was examined.
- On Gold v3 with the T038 keyword-50/vector-60 candidate boundary, plain RRF versus protected admission achieved concrete-evidence hit rate at 10 (`Unit Hit@10`) of 97.8%/97.8%, first-evidence mean reciprocal rank at 20 (`MRR@20`) of 0.797/0.797, necessary-evidence coverage at 20 (`Coverage@20`) of 97.5%/98.6%, and complete-evidence success of 93.5%/94.6%. QA084's vector-rank-4 evidence moved from fused rank 27 to rank 20; QA075's keyword-rank-1 numeric evidence remained at rank 10. Coverage improved on one development case and regressed on none. The 22-case validation metrics were exactly unchanged, so this is accepted only as a bounded known-failure repair, not a demonstrated generalized gain.
- T039 does not solve multi-evidence ordering. QA015, QA020, QA026, QA030 and QA034 remain incomplete within Top-20 even though T038 proved all 153 unconditional groups are present in the candidate union; QA067 and QA084 still miss `Unit Hit@10`. Wrong application-branch rows decreased from six to five, but nonzero contamination means a later anchor/branch experiment remains necessary. Any next experiment must separately isolate multi-evidence coverage or anchor protection and must not treat route admission as a model reranker.
- T039 verification passed 11 focused tests and all 378 tests, T039 and T038 `--validate-only`, v4 `--validate-only`, database integrity, zero foreign-key violations, compilation and diff checks. Corpus/T027/vector/T039-result hashes are `9f23c72...1d97`, `6d96def...e72`, `8a5d0b3...644f`, and `ba6024f...f858`. Private outputs remain Git-ignored; no corpus, retrieval/citation text, vector, v3 production KB, cloud, service, final Chunk, ANN, KG or production-index mutation occurred; `cloud_sync_required=false`.
- T040 isolates the user's proposed model-controlled reranking from retrieval scoring. A strictly reuses T039's keyword Top-1/vector Top-4 protected RRF Top-20. B receives the exact same T038 keyword Top-50 plus vector Top-60 union, but `deepseek-v4-flash` sees only the original question, candidate ID, citable source metadata, citation eligibility and verbatim source text in deterministic hash order. Route identity, route rank, lexical/vector/RRF scores, question rewrites and parallel subqueries are absent. A first pass screens at most 40 candidates and a second pass selects and orders at most 20; the model may return only existing candidate IDs, and its reasons never become evidence. The 71 development caches and prompt/input contract were frozen before the 22 validation cases were executed; the 17-case holdout remains sealed.
- T040 rejects pure score-blind model reranking as the current final ranking method. Across 93 cases, T039 versus the model arm achieved concrete-evidence hit rate at 10 (`Unit Hit@10`) of 97.8%/87.1%, first-evidence mean reciprocal rank at 20 (`MRR@20`) of 0.797/0.757, necessary-evidence coverage at 20 (`Coverage@20`) of 98.6%/83.3%, and complete-evidence success of 94.6%/78.5%. Validation independently produced `Unit Hit@10` of 100.0%/81.8% and `Coverage@20` of 98.2%/78.2%. The model improved Coverage on zero cases and regressed on 18. It selected only 4.4 candidates on average, showing systematic over-compression of list, classification, hierarchy and complementary evidence.
- The loss is attributable to model selection, not upstream recall. The fixed T038 candidate union contains all 153 necessary evidence groups; the first model screening pass retained 126 (82.4%) and the final pass retained 121 (79.1%). The first pass lost 27 groups across 17 cases, and the second pass discarded five more groups across QA020, QA028, QA034 and QA084. In 16 cases the final Gold evidence was incomplete while the model still returned `evidence_sufficient=true`, so model self-declared sufficiency is not a reliable answer gate. QA075 remains a real positive signal: the model raised exact numeric evidence from rank 10 to rank 1. It also eliminated measured wrong application-branch pollution. These bounded strengths may later justify an auxiliary semantic judge, but only with deterministic evidence-retention safeguards and a separately approved experiment; they do not justify replacing T039.
- T040 verification passed 12 focused tests and all 390 tests, T040/T039/T038/Gold-v3/v4 `--validate-only`, database integrity, zero foreign-key violations, compilation and diff checks. The two-pass run made 186 cached model calls using 2,158,222 Tokens. Corpus/frozen-contract/T040-result/T040-report SHA-256 values are `9f23c72...1d97`, `18a6019...acb`, `82d6759...0ca`, and `7f9fe9b...1bb`. Private source text, model caches and results remain Git-ignored. No corpus, retrieval/citation text, document/query vector, v3 production KB, cloud, service, final Chunk, Embedding, ANN, KG or production-index mutation occurred; `cloud_sync_required=false`. T040 is closed and no next experiment is authorized by this decision.
- T041 establishes a layer-by-layer evidence-fate ledger before any further reranking design. It traces all 153 unconditional Gold-v3 evidence groups across the same 93 development/validation cases without rerunning retrieval. Keyword Top-50 retained 142 groups and dense Top-60 retained 148; 137 groups appeared in both routes, five only in keyword and 11 only in dense. The T038 union retained all 153 groups and all 93 cases were evidence-complete at the candidate boundary. This confirms that the current primary defect is final evidence preservation and ordering, not upstream union recall.
- Equal-weight RRF materially suppresses route-exclusive evidence. Plain RRF Top-10/Top-20 retained 128/146 groups; T039's keyword Top-1/vector Top-4 admission left Top-10 unchanged at 128 and raised Top-20 only to 147. The remaining six missing groups occur in QA015, QA020, QA026, QA030 and QA034. QA015's 8.7.2.2 is dense rank 60 but RRF rank 98; QA020's tenth material row is dense rank 19 but RRF rank 31; QA026's implementing-regulation evidence is dense rank 11 but RRF rank 21; QA030's clauses 4.7/4.8 are dense ranks 32/26 but RRF ranks 63/52; QA034's clause 6.1 is lexical/dense rank 33/23, RRF rank 21 and protected rank 22. QA067's specialized-standard evidence is dense rank 1 but RRF rank 14, QA075's exact numeric evidence is lexical rank 1 but RRF rank 10, and QA084's exact clause is dense rank 4, RRF rank 27 and protected rank 20. These cases distinguish route-consensus bias, multi-evidence/list or family incompleteness, same-document crowding and the hard Top-20 cutoff.
- T041 therefore requires future ranking work to treat evidence preservation as a hard experimental contract. A model or scoring rule may not silently delete a protected single-route head or a confirmed complementary/family member; every stage must report evidence-group recall, complete-case rate, first-evidence rank and per-case regressions. The audit itself selects no new mechanism and authorizes no new A/B or holdout execution. Verification passed 10 focused tests and all 400 tests, T041/T040/T039/T038/Gold-v3/v4 validation, database integrity, zero foreign-key violations, compilation and diff checks. Corpus/T041-result/T041-report hashes are `9f23c72...1d97`, `6655ad6...202`, and `bee7e7b...ff6`; private outputs remain Git-ignored, `cloud_sync_required=false`, and no retrieval, model, Embedding, corpus, vector, index, cloud or service mutation occurred. T041 is closed.
- T042 treats a larger context as a retrieval container, not as a larger citation chunk. A strictly reuses T039's atomic Top-20. B preserves those same 20 leaves in the same order, collapses eligible same-table, explicit-numbered-clause-family or bounded-section members into navigation containers, and appends only the container's original citable leaves. Parent text is never answer evidence, and collapsing a container cannot admit lower-ranked unrelated candidates. A 42-policy grid selected table＋explicit＋section, at least two Top-20 members and at most 12 children using only 71 development cases; 22 validation cases were reported only after selection, and 17 holdout cases remain sealed.
- T042 confirms that structure-aware evidence packages can repair semantic-boundary truncation. Across 93 cases, required-group retention rose from 147/153 (96.1%) to 151/153 (98.7%), complete cases rose from 88/93 to 91/93, and no case regressed. QA015, QA020 and validation case QA030 became complete; QA084's first correct evidence container improved from rank 20 to rank 3. Validation rose from 24/26 to 26/26 retained groups and from 21/22 to 22/22 complete cases. The frozen atomic Top-20 metrics remained unchanged at `Unit Hit@10` 97.8%, `MRR@20` 0.797 and `Coverage@20` 98.6%; wrong application-branch rows remained five. QA026 and QA034 remain incomplete, and QA067/QA075 are not helped by structural packaging.
- T042 does not authorize production activation. B activated 43 packages on 21 questions; only seven contained Gold-required evidence, only three packages rescued the four missing groups, and 36 contained no Gold-required evidence. Mean expanded leaves rose from 20.0 to 20.7, P95/max were 25.4/28, and mean characters rose from 3,018 to 3,138. The bounded cost is acceptable for an experiment, but the low selection precision would inject avoidable semantic noise. Atomic citation units must remain atomic; a later separately approved experiment may test an evidence-package relevance gate, but must not silently delete the frozen frontier or execute holdout cases.
- T042 verification passed 13 focused tests and all 413 tests, plus T042/T041/T040/T039/T038/Gold-v3/v4 `--validate-only`, database integrity and zero foreign-key violations. Corpus/T042-result/T042-report SHA-256 values are `9f23c72...1d97`, `23a3f32...123`, and `d213d1d...277`. No corpus, retrieval/citation text, document/query vector, production FTS, final Chunk, model/Embedding call, ANN, KG, v3 production KB, cloud, deployment or service mutation occurred; private outputs remain Git-ignored and `cloud_sync_required=false`. T042 is closed, and no next experiment is authorized by this decision.
- Gold v4 is a user-approved answer calibration layered on immutable Gold v3. QA026's synthesized reference answer incorrectly named `自然资源部` even though its own accepted evidence claim and the current `《中华人民共和国矿产资源法实施条例》` article 29 correctly name `原矿业权出让部门`. Gold v4 corrects that authority, explicitly retains `勘查方案、开采方案`, restores `评审所需时间不计算在许可审查期限内`, and records that the policy, regulation and service guide use different source-specific terms. Only QA026's reference answer, corresponding claim and review note change; all 111 questions, citations, evidence IDs, required groups, counts and Gold v3/T038-T042 historical artifacts remain unchanged.
- Gold v4 verification passed 7 focused tests and all 420 tests, with source Gold v3, T027 leaves and v4 corpus hash-bound. Corpus/T027/Gold-v3 hashes remain `9f23c72...1d97`, `6d96def...e72`, and `a45a308...6ca`. No retrieval result, model/Embedding call, vector, corpus, production index, cloud or service mutation occurred; private Gold v4 outputs remain Git-ignored and `cloud_sync_required=false`. T043 is closed.
- T044 distinguishes auditable query segmentation from semantic similarity. DeepSeek is used to produce explicit concept objects such as subject, action, object, condition and document anchor; Embedding is not treated as a splitter because a vector cannot itself expose stable, reviewable concept boundaries. T044 reused the already frozen T032 `deepseek-v4-flash` output and accepted only locked anchors and lexical terms that can be copied verbatim from the original question. Candidate anchors, one-character anchors, non-verbatim model additions, canonical substitutions and synonyms were excluded. Duplicate anchors with different roles were merged, and multiple variants within one concept were aggregated by their maximum score so one user concept could not be counted repeatedly.
- T044 holds retrieval scope fixed: both arms use exactly T035's FTS Top-30 documents and the same candidate leaves. A reuses T035's mechanical two/three/four-character ranking byte-for-byte; both arms are scored against current Gold v4. The older frozen manifest differs only where later Gold evidence governance removed duplicate QA029/QA034 requirements, and that drift is explicitly recorded rather than allowed to control current scoring. Across 71 development and 22 validation cases, A/B achieved `Unit Hit@10` 90.3%/81.7%, `MRR@20` 0.674/0.558, `Coverage@20` 89.0%/82.7%, and complete-evidence success 83.9%/80.6%. First-evidence rank improved on 20 cases and regressed on 29; coverage improved on six and regressed on 12. Validation independently fell from 90.9% to 77.3% for both `Unit Hit@10` and `Coverage@20`, so literal semantic concepts cannot replace the mechanical lexical safety route.
- T044 also records why concept scoring remains useful as an auxiliary feature. It repaired otherwise noisy cases such as QA030 and QA067, and QA026's implementing-regulation article 29 moved from mechanical full rank 174 to literal-concept rank 132. It still failed Top-20 because the question says `勘查实施方案` while the regulation says `勘查方案`, and T044 deliberately prohibited synonym expansion. A future separately approved experiment should therefore preserve a mechanical lexical route and test governed source-aware concept families. Alternative expressions of one concept should receive one pooled or capped contribution rather than an unbounded sum; independent concepts such as `勘查方案` and `评审` may contribute separately. Embedding may propose or retrieve semantic variants, but it may not silently declare legally distinct terms equivalent.
- T044 verification passed 11 focused tests and all 431 tests, plus T044, T035, Gold-v4 and v4 validation. Corpus integrity is `ok`, foreign-key violations are zero, and the corpus hash remains `9f23c72...1d97`. T044 result/report hashes are `ed8d052...0ead` and `42ee163...724`. No new model/Embedding API call, corpus or retrieval-text mutation, vector change, production FTS/index, final Chunk, ANN, KG, holdout execution, cloud sync, deployment or service mutation occurred; private outputs remain Git-ignored and `cloud_sync_required=false`. T044 is closed, and no synonym-family experiment is authorized by this decision.
- T045 implements the user-approved concept-family contract without changing the candidate frontier or tuning against Gold. A reuses T044's mechanical ranking byte-for-byte. B reuses the same T035 FTS Top-30 documents, identical leaves, frozen T032 DeepSeek concepts and T044 scorer, and adds only the predeclared query-only families `勘查实施方案/勘查方案`, `采矿证/采矿许可证`, and unambiguous Ⅰ-type written forms. Within one concept, matching variants are alternatives and contribute the maximum score once; different concepts such as `勘查方案` and `评审` contribute separately and are summed. The registry cannot rewrite citation text, and no term was extracted from Gold answers or evidence bodies.
- The concept-family mechanism is accepted as a locally useful auxiliary feature, not as a replacement retrieval route. Across 71 development and 22 validation cases, mechanical/governed-semantic `Unit Hit@10` was 90.3%/81.7%, `MRR@20` 0.674/0.553, `Coverage@20` 89.0%/82.9%, and complete-evidence success 83.9%/80.6%. Relative to T044's literal semantic arm, governed families held `Unit Hit@10` at 81.7%, raised `Coverage@20` only from 82.7% to 82.9%, and reduced `MRR@20` from 0.558 to 0.553. Validation remained at 77.3% for both `Unit Hit@10` and `Coverage@20`, well below the mechanical 90.9%.
- QA026 supplies the positive mechanism evidence: grouping `勘查实施方案` with source term `勘查方案` moved implementing-regulation article 29 from literal-concept full rank 132 to rank 13, compared with mechanical rank 174. The service-guide evidence also entered Top-20, raising required-group `Coverage@20` from 50% to 75%, although the coal/coalbed-methane evidence moved to rank 24 and kept the four-group question incomplete. The family therefore repairs terminology mismatch but does not itself preserve every complementary source.
- T045 also records two safety failures. In QA020, adding `采矿许可证` improved first-evidence rank 9 to 6 but crowded ten complementary material rows and reduced `Coverage@20` from 20% to 10%. In QA005-QA007, the Ⅰ-type family did not recover the correct table leaf and slightly worsened its already-late rank, demonstrating that synonym grouping cannot replace table/field structure or relation coverage. Future work should retain mechanical lexical retrieval as a safety route and test concept families only as an auxiliary score or protected admission signal with explicit multi-evidence/list completeness checks.
- T045 verification passed 11 focused tests and all 442 tests, plus T045, T044 and v4 validation. Corpus integrity is `ok`, foreign-key violations are zero, and the corpus hash remains `9f23c72...1d97`. Registry/result/report hashes are `f6c4f67...1d23`, `63991e4...db48`, and `faf8d3e...5c92`. No model/Embedding API call, corpus/retrieval/citation mutation, vector change, production FTS/index, final Chunk, ANN, KG, holdout execution, cloud sync, deployment or service mutation occurred; private outputs remain Git-ignored and `cloud_sync_required=false`. T045 is closed, and no hybrid experiment is authorized by this decision.
- T046 adopts mechanical retrieval as an explicit safety route rather than asking semantic concepts to replace it. A reuses T044/T045's mechanical ranking byte-for-byte. B fixes the mechanical Top-10 and its exact order, then fuses only the remaining full mechanical and T045 governed-concept rankings with weighted reciprocal-rank fusion (`k=60`). Twelve semantic weights from 0.05 through 5.0 were evaluated on 71 development cases. Eligibility required no per-case `Coverage@20` regression, aggregate `Unit Hit@10`, `MRR@20`, `Coverage@20` and complete-evidence nonregression, and no increase in wrong application-branch rows. The frozen selection priority chose weight 1.5 by complete-evidence success, coverage, MRR, branch rows and then smaller weight.
- The T046 hybrid is accepted as the next research baseline. Across 93 development/validation cases, mechanical/hybrid `Unit Hit@10` remained 90.3%/90.3%, `MRR@20` rose 0.674/0.676, `Coverage@20` rose 89.0%/92.8%, complete-evidence success rose 83.9%/89.2%, `Unit Hit@20` rose 91.4%/94.6%, and wrong application-branch rows fell 2/1. Coverage improved on QA010, QA019, QA025, QA026, QA030 and QA067 and regressed on no case. Development coverage rose 88.4% to 91.4% with zero per-case regression; the previously observed validation split rose 90.9% to 97.3% and complete cases rose 20/22 to 21/22.
- QA026 demonstrates the intended combined behavior. The mechanical safety head preserves the existing policy and coal/coalbed-methane evidence at ranks 1 and 4, while the governed `勘查实施方案/勘查方案` route brings the service guide to rank 17 and the implementing-regulation article 29 to rank 20. All four required groups therefore enter Top-20 and coverage rises from 50% to 100%. QA020 retains its mechanical 80% list coverage rather than suffering T045's pure-semantic crowding. QA030 improves from 0% to 40%, and QA067 from 0% to 100%.
- T046 does not solve every ordering class. QA015 remains at 0% because two required analysis-method clauses remain far below the frontier; QA034 remains at 75% with one workflow clause at rank 24; QA084 remains at 0% with its required standard-basis clause at rank 57; QA030 still has only two of five groups in Top-20. These are bounded structure, multi-evidence and document-internal ordering failures and should be discussed separately rather than addressed by increasing semantic weight indiscriminately.
- The 22-case validation split has been observed throughout earlier experiments and during exploratory T046 probing, so it is a confirmation split, not a new blind test. It was not part of the formal selection objective. All 17 holdout cases remain sealed; a future holdout run requires explicit user approval after the architecture and remaining failure handling are frozen.
- T046 verification passed 11 focused tests and all 453 tests, plus T046, T045 and v4 validation. Corpus integrity is `ok`, foreign-key violations are zero, and the corpus hash remains `9f23c72...1d97`. T046 result/report hashes are `bd19aa4...5cd7` and `4baef42...94a9`. No model/Embedding API call, corpus/retrieval/citation mutation, vector change, production FTS/index, final Chunk, ANN, KG, holdout execution, cloud sync, deployment or service mutation occurred; private outputs remain Git-ignored and `cloud_sync_required=false`. T046 is closed; the hybrid is a research baseline, not production activation.
- T047 evaluates the user's proposed “freeze retrieval first, then generate a hypothetical answer and look for support” architecture without allowing the hypothesis to bias recall. For every one of the 93 development/confirmation questions, the keyword Top-50 plus vector Top-60 union is frozen first and contains all 153 unconditional Gold-v4 evidence groups. A question-only framework call sees no candidate, document, text, route, rank or score and produces the same neutral evidence requirements plus an explicitly unverified hypothetical answer. A then judges every frozen candidate against the original question and neutral requirements; B judges the identical candidates with the hypothesis and atomic claims added. Every real candidate must be returned exactly once, and deterministic code—not the model—performs requirement coverage and Top-20 selection. The 71 development caches and all input/prompt hashes were frozen before the 22 confirmation cases; all 17 holdout cases remain sealed.
- T047 rejects hypothetical answers as a default evidence-matching or ranking signal. A/B full-matrix required-group recall is tied at 149/153 (97.4%), but the hypothesis reduces `Unit Hit@10` from 100.0% to 98.9%, `Coverage@20` from 97.6% to 95.9%, and complete-evidence success from 93.5% to 90.3%; `MRR@20` rises only from 0.856 to 0.863. Coverage improves on zero questions and regresses on QA022, QA034 and QA058. Confirmation coverage is unchanged at 98.2%, with only QA007's first evidence moving earlier. The hypothesis raises QA020's matrix recognition from 7/10 to 10/10 material groups but does not improve its Top-20 coverage, while QA034 matrix coverage falls 100% to 50% and QA058 falls 100% to zero.
- Hypothesis self-correction is not a reliable safety gate. The framework generates 730 unverified atomic claims; 435 receive some support tag, 295 receive neither support nor contradiction, and not one claim is marked contradicted. QA022's proposed numeric ranges differ from the visible Gold-bound table values but are still tagged as supported. Conversely, QA058's hypothetical answer is directionally consistent with the correct general-ore requirement, yet adding it causes the same correct candidate to change from `direct_support` to `irrelevant`. QA034 similarly demotes two Gold supporting-context groups. These failures show both anchoring and attention drift: a wrong hypothesis may be accepted, and a right hypothesis may still degrade candidate reading.
- The neutral full-candidate evidence matrix remains a promising auxiliary signal but is not adopted as a replacement baseline. Relative to T046's mechanically protected concept hybrid, neutral matrix Top-20 improves seven questions (QA014, QA015, QA030, QA032, QA034, QA052 and QA084) but regresses QA020 and QA026. Any later experiment must preserve the T046 safety frontier and allow a neutral judge to add or re-rank only under explicit non-demotion and multi-evidence completeness guards. It must not restore hypothetical answers by default or execute the sealed holdout without separate approval.
- T047 verification passed 11 focused tests and all 464 tests, plus T047 and v4 `--validate-only`, database integrity and zero foreign-key violations. Corpus/T027/frozen-contract/result/report SHA-256 values are `9f23c72...1d97`, `6d96def...9e72`, `bc73763...01ab`, `bfaae1b...1c35`, and `778dd24...9539`. The final valid caches record 320 successful model calls including 41 format retries and 6,369,996 Tokens; rejected all-or-nothing attempts were deliberately not cached, so this is a lower cost bound. No corpus, Gold, retrieval/citation text, vector, Embedding, production FTS/index, final Chunk, ANN, KG, holdout, v3 KB, cloud, deployment or service mutation occurred; private candidates, source text, caches and results remain Git-ignored and `cloud_sync_required=false`. T047 is closed, and no next experiment is authorized by this decision.

### 2026-07-27

- T048 tests a unified alternative to exclusive question-type classification. The keyword Top-50 plus vector Top-60 union is frozen before the model call; A reuses T047's neutral full-candidate matrix, while B changes only the candidate-judging prompt and schema so one candidate may carry several evidence roles. Roles cover direct answers, evidence-chain positions, procedure steps, common/conditional/agency-obtained list rows, general/special rules, classification overview/member, thresholds, level definitions, exceptions and contradictions. The model sees neither route identity, retrieval rank nor score, and no hypothetical answer is generated. Every candidate must be returned exactly once. A reported role is usable only when deterministic code finds its supporting phrase in that candidate's original text.
- T048 is rejected at the development gate and the 22-case confirmation split is deliberately not executed. Across 71 development questions and 127 necessary evidence groups, A/B/C matrix required-group recall is tied at 123/127 (96.9%), but A/B/C `Unit Hit@10` is 100.0%/95.8%/94.4%, `MRR@20` is 0.829/0.839/0.828, `Coverage@20` is 97.4%/94.0%/91.6%, and complete-evidence success is 93.0%/88.7%/85.9%. B's small MRR rise does not compensate for evidence loss. B improves coverage on QA015, QA020 and QA031 but regresses QA014, QA022, QA034, QA068 and QA084. C, the deterministic role-protection diagnostic, improves only QA020 and QA031 and regresses six questions, so it is also rejected.
- The mechanism is mixed rather than uniformly ineffective. B changes QA015 from one of three to all three required evidence groups in Top-20, QA020 from seven to nine of ten, and QA031 from seven to all eight. It does not recover QA026's coal/coalbed-methane special rule or QA033's cross-standard level definition. It also relabels the correct QA014 table and QA068 extrapolation rule as unrelated, drops one QA034 workflow group, lets two of eleven QA022 industrial-indicator groups fall outside Top-20, and recognizes but fails to retain QA084's standard-basis group. This shows that a unified prompt avoids a hard type gate but still creates attention and prioritization interference; a role label must not become a standalone filter or demotion authority.
- T048's development caches were frozen before scoring. The 71 cases required 73 successful calls including two format retries and recorded 2,058,115 Tokens. The model supplied roles with an invalid local verbatim basis on 154 candidate rows; normalization removed every such role before the C diagnostic. The development frozen-contract/result/report SHA-256 values are `36910e6...0a71d`, `710718e...3480`, and `e98f2d9...40750`. Ten focused T048 tests and all 474 tests passed with `PYTHONPATH=src`; T048, T047 and v4 validation passed, database integrity is `ok`, foreign-key violations are zero, and the corpus hash remains `9f23c72...1d97`. Confirmation and all 17 holdout questions remain unexecuted. No corpus, Gold, candidate, retrieval/citation text, vector, Embedding, production FTS/index, final Chunk, ANN, KG, v3 KB, cloud, deployment or service mutation occurred; private artifacts remain Git-ignored and `cloud_sync_required=false`. T048 is closed, and no next experiment is authorized by this decision.
- T049 tests the user's approved “safe base plus gap supplement” architecture without giving the auxiliary signal deletion or reranking authority. A is T039's protected Top-20 byte-for-byte. C deterministically compares T047's question-only neutral requirements with the frozen neutral labels on those 20 candidates and greedily appends citation-eligible candidates that cover still-unrecognized requirement IDs. B appends the same number of citation-eligible candidates from the unchanged T039 continuation for each question. This equal-size control is mandatory: a gain caused only by increasing the context window is not model-specific value. Hypothetical answers, T048 roles, new model calls and new Embedding calls are excluded.
- Development selection compared per-question caps +2, +4, +6 and +10 using only 71 development questions. Every cap recovered the same one additional required group and reached 99.0% development evidence-pool coverage and 95.8% complete-evidence success. Cap +2 used 29 supplements; +4 used 33 without another Gold-bound gain, while +6 and +10 selected no candidate beyond +4. The frozen priority therefore selected +2 before reporting the previously observed 22-case confirmation split.
- T049 proves the non-demotion property but does not prove an independent model recall gain. Across 93 questions, A retains 147/153 required groups with 98.6% macro coverage and 94.6% complete-evidence success. Both equal-size B and neutral C append 38 candidates, retain 148/153 groups, reach 98.8% macro coverage and 95.7% complete-evidence success, and improve only QA026. The recovered `runit-687e3eeeac717f05` is the implementing-regulation article already at T039 rank 21, so both B and C include it. C's other QA026 supplement is T039 rank 58 and is not a current Gold member. The confirmation split receives nine B/C supplements, none Gold-bound, and has no coverage or completeness change.
- C has a limited selection-purity advantage over B. The equal-size T039 continuation adds one prohibited sibling-branch application row on QA019 and one on QA020, increasing aggregate wrong application-branch rows from the protected base's 5 to 7. C does not add either row and remains at 5. Only 2 of the 38 B/C supplement IDs overlap, so C is genuinely choosing a different set, but 37 of its 38 supplements are not bound to a required group in the current Gold. These may include useful unscored context and must not automatically be called false positives, yet they provide no measured recall benefit. The architecture is retained as a safe design pattern; this neutral supplement selector is not adopted as a new research baseline. A later experiment, if separately approved, may test a baseline-aware gap-checking prompt, but must keep the immutable prefix and equal-size continuation control.
- T049 verification passed ten focused tests and all 484 tests, plus T049, T047 and v4 validation. Database integrity is `ok`, foreign-key violations are zero, and the corpus hash remains `9f23c72...1d97`. Selection/result/report SHA-256 values are `1f98c0c...1e0fc`, `1ea6ba5...1e52e4`, and `86354dc...690d6`. The 17 holdout cases remain sealed. No corpus, Gold, candidate, retrieval/citation text, vector, Embedding, production FTS/index, final Chunk, ANN, KG, v3 KB, cloud, deployment or service mutation occurred; private outputs remain Git-ignored and `cloud_sync_required=false`. T049 is closed, and no next experiment is authorized by this decision.
- T050 tests the separately approved baseline-aware direct-gap model without allowing it to modify the safe base. A is T039's protected Top-20 byte-for-byte. C receives the original question, T047's question-only neutral requirements, the explicitly marked base candidates and the rest of the already frozen keyword Top-50 plus vector Top-60 union. It receives no route identity, retrieval rank or score and cannot answer, delete, demote, reorder or rewrite. For every required neutral requirement it must audit the base as `covered`, `partial` or `missing`; a supplement is usable only if it is citation-eligible, outside the base, tied to an open requirement, marked `direct`, and carries a continuous phrase found deterministically in that candidate's own text. B appends the same number of candidates from the unchanged T039 continuation.
- Development selection compares per-question caps +2, +4, +6 and +10 on the 71 development cases only. The gate requires C to retain strictly more necessary evidence groups than equal-size B and to add no wrong application branch relative to A. No cap passes. The best diagnostic cap is +4: A/B/C retain 123/124/124 of 127 necessary groups, have question-macro evidence-pool coverage 98.7%/99.0%/98.8%, and complete-evidence success 94.4%/95.8%/95.8%. B and C each append 142 units and each adds one Gold-bound unit. C therefore has zero necessary-group gain over B and is slightly worse on question-macro coverage.
- The tied aggregate counts arise from different questions rather than equivalent selection. C recovers QA020's `runit-4ea43977830e4071` and raises that ten-group material-list case from 90% to 100%, while B does not. B instead recovers QA034's `runit-59bbfe4ac540c39d` and raises that four-group workflow case from 75% to 100%, while C selects four non-Gold-bound material rows and remains at 75%. C preserves the base's five wrong application-branch rows whereas equal-size B reaches ten; this is another bounded selectivity signal, not a recall gain.
- QA026 remains unresolved by the direct auditor. Its correct implementing-regulation unit remains available in the frozen union, but C returns no valid supplement: all five proposed supplement IDs are the invalid truncated value `runit-29` and deterministic validation drops them. Across 71 calls, validation drops 102 invalid supplements: 69 are outside the remaining-candidate set (52 are actually protected-base IDs and 17 are invented or truncated), 12 lack a candidate-verbatim basis, 10 target already-covered requirements, 10 duplicate an ID and one is citation-ineligible. It also drops 19 out-of-base supporting IDs and converts any resulting unsupported `covered`/`partial` audit to `missing`. These controls prevent malformed model output from becoming evidence but also show that the current prompt/model contract is not reliable enough to upgrade.
- T050 stops at the failed development gate. The 22-case previously observed confirmation split is not executed and all 17 holdout cases remain sealed. The immutable-prefix append-only architecture remains a valid safety pattern, but this direct-gap auditor is rejected as a research baseline. Further prompt variants should not be launched merely to continue tuning; a new experiment requires a distinct hypothesis and user approval.
- T050 verification passed 16 focused tests and all 500 tests, plus T050, T049, T047 and v4 validation. Database integrity is `ok`, foreign-key violations are zero, and the corpus hash remains `9f23c72...1d97`. The valid development caches record 73 successful calls including two format retries and 1,491,082 Tokens. Frozen-contract/selection/result/report SHA-256 values are `a3bb871...f9ca`, `27c8074...8bf`, `dac4a1c...17f2`, and `e7ab611...05e`. No corpus, Gold, candidate, retrieval/citation text, vector, Embedding, production FTS/index, final Chunk, ANN, KG, v3 KB, cloud, deployment or service mutation occurred; private outputs remain Git-ignored and `cloud_sync_required=false`. T050 is closed, and no next experiment is authorized by this decision.
- T051 isolates T050's long candidate identifier as a single interface variable. It reuses the same 71 development questions, T047 neutral requirements, protected Top-20, remaining frozen keyword-50/vector-60 candidates, candidate text and metadata, deterministic randomized order, `deepseek-v4-flash`, temperature zero, evidence rules and caps. The only model-visible change is replacing every real `runit-*` value with a unique three-letter alias such as `U-ABE`, assigned over the existing random union order. The same alias format is used in both candidate arrays, so it conveys no base/remaining role, clause, page, route, rank or score. Real IDs are hidden and restored only through exact deterministic mapping; unknown aliases are rejected without fuzzy resolution. Private raw responses are retained with hashes for this diagnostic.
- The long-ID copy failure is confirmed rather than inferred. T050 has 17 invented, truncated or clause-number-shaped supplement IDs and five malformed base-support IDs. T051 reduces both classes to zero. For QA026, the correct implementing-regulation article 29 is `U-ABE`; the model returns `U-ABE` four times with verbatim bases for review, fee and time aspects. Deterministic deduplication retains the real `runit-687e3eeeac717f05` once and raises QA026 from three of four to all four necessary groups. This proves that T050's repeated `runit-29` output was an identifier-representation failure after the model had recognized the correct article.
- Fixing the identifier does not fix the audit architecture. Total discarded supplement rows rise slightly from 102 to 103. With aliases, all 78 `outside_remaining_candidates` supplements are exact aliases of protected-base candidates, and 23 exact remaining-candidate aliases are incorrectly placed in `supporting_base_ids`. The model therefore copies short aliases correctly but does not reliably preserve which input array and output role each candidate belongs to. At the inherited +4 cap, the short-alias arm appends 122 valid units versus T050's 142 and keeps wrong application-branch rows at five, but still retains only 124 of 127 necessary groups.
- The remaining misses divide into role and semantic failures. In QA020, the correct extension-material row `runit-4ea43977830e4071` (`U-ABM`) appears explicitly in the model's base-support audit even though it is a remaining candidate; because it is never emitted as a supplement, QA020 falls from T050's 100% back to 90%. In QA034, the missing workflow clause `runit-59bbfe4ac540c39d` (`U-AAX`) is absent from the entire raw response, so this is an evidence-chain/semantic omission rather than an ID or role error. QA015's missing chemical-full-analysis clause `runit-c1b9d10cec13993f` (`U-ABU`) is likewise never mentioned. T047's question-only requirements also over-expand several questions into unasked time, fee, exception, legal-consequence or item-specific checks, increasing attention load without representing current Gold needs.
- At cap +2/+4/+6/+10, the short-alias arm always retains 124 groups; at the inherited +4 cap, protected base / equal-size continuation / long-ID / short-alias retain 123/124/124/124 of 127. Short aliases improve QA026 and regress QA020 relative to long IDs, while matching equal-size continuation on every per-case necessary-evidence coverage change. They therefore have no net retrieval recall contribution. Short letter aliases are accepted as future model-I/O hygiene because they eliminate fabricated IDs and improve auditability, but T051 is not adopted as a retrieval baseline.
- The next distinct architectural hypothesis, if approved, is to remove candidate-role memory from the model: a base-audit call sees only the protected 20 and returns missing requirement IDs; a separate supplement call sees only those program-approved gaps and remaining candidates. This should be discussed together with a narrower, question-faithful requirement contract before any execution. Merely strengthening the same one-call prompt or adding role-coded aliases is not authorized by T051.
- T051 verification passed 17 focused tests and all 517 tests, plus T051, T050 and v4 validation. Database integrity is `ok`, foreign-key violations are zero, and the corpus hash remains `9f23c72...1d97`. The 71 successful calls required no format retry and recorded 1,391,727 Tokens. Frozen-contract/result/report SHA-256 values are `fd1be49...c1ce`, `7cffb33...4eb1`, and `8fe97e0...05d3`. The 22-case confirmation split and 17-case holdout remain unexecuted. No corpus, Gold, candidate, retrieval/citation text, vector, Embedding, production FTS/index, final Chunk, ANN, KG, v3 KB, cloud, deployment or service mutation occurred; private responses and outputs remain Git-ignored and `cloud_sync_required=false`. T051 is closed, and no next experiment is authorized by this decision.
- T052 tests the approved role-isolation hypothesis while preserving every other T051 input. Stage one receives the original question, unchanged T047 neutral requirements and only the protected Top-20; it returns a covered/partial/missing audit. Deterministic code opens the partial or missing requirement IDs. Stage two receives only those open requirements and the remaining frozen keyword-50/vector-60 union candidates; it cannot see the base. Both stages use the same non-semantic three-letter aliases, `deepseek-v4-flash`, temperature zero and unchanged evidence guards. Only 71 development questions are executed.
- The interface hypothesis succeeds. Base-stage use of remaining candidates, supplement-stage use of base candidates, unknown support aliases and unknown supplement aliases are all zero. Discarded supplement rows fall from T051's 103 to 60: 28 duplicates, 20 non-verbatim relation bases, 10 candidates that do not cover an open requirement and 2 citation-ineligible rows. QA020's correct extension-material row `runit-4ea43977830e4071` (`U-ABM`) now appears in `supplements` rather than the wrong base-support field, raising the case from 9/10 to 10/10 groups. QA026 continues to return implementing-regulation article 29 `runit-687e3eeeac717f05` (`U-ABE`) and remains 4/4.
- The retrieval hypothesis nevertheless fails its development gate. At inherited cap +4, protected base / equal-size continuation / T051 single-stage / T052 two-stage retain 123/125/124/125 of 127 necessary groups, with question-macro evidence-pool coverage 98.7%/99.4%/99.0%/99.2% and complete-evidence success 94.4%/97.2%/95.8%/97.2%. T052 adds one group over T051 but only ties equal-size continuation. Its wrong application-branch rows rise from the protected base's five to seven, so it does not satisfy either strict superiority or no-new-branch criteria and is not adopted as a research baseline.
- The remaining misses are directly localized. QA015's GB/T 13908-2020 clause 8.7.2.2, `runit-c1b9d10cec13993f` (`U-ABU`), is visible to stage two and states that chemical full analysis provides a basis for determining basic-analysis and composite-analysis items; the model instead spends selections on mineral-specific item lists and leaves the case at 2/3. QA034's DZ/T 0479-2024 clause 6.1, `runit-59bbfe4ac540c39d` (`U-AAX`), is also visible but omitted while the model chooses agreements, approval documents, institutions and attachments, leaving the case at 3/4. T047's neutral framework expanded these questions into unasked lists, exceptions, time limits, agencies, consequences and forms. The next distinct hypothesis, if separately approved, is therefore to generate a question-faithful minimum evidence-requirement contract before repeating any selector experiment; no such experiment is authorized here.
- T052 verification passed 16 focused tests and all 533 tests, plus T052, T051 and v4 `--validate-only`. Database integrity is `ok`, foreign-key violations are zero, and the corpus hash remains `9f23c72...1d97`. The 142 successful stage calls required no format retry and recorded 1,414,700 Tokens. Frozen-contract/result/report SHA-256 values are `3548b8c...2b78`, `5f6cb9b...68b4`, and `29681dc...a4b7`. The 22-case confirmation split and 17-case holdout remain unexecuted. No corpus, Gold, candidate, retrieval/citation text, vector, Embedding, production FTS/index, final Chunk, ANN, KG, v3 KB, cloud, deployment or service mutation occurred; private responses and outputs remain Git-ignored and `cloud_sync_required=false`. T052 is closed, and no next experiment is authorized by this decision.
- T053 isolates T047's question-to-requirement framework from keyword recall, dense recall and ranking. It reuses the exact T052 keyword Top-50, vector Top-60, frozen union, protected Top-20, candidate text/metadata/random order, three-letter aliases, base-audit and supplement prompts, `deepseek-v4-flash`, temperature zero, cap grid and deterministic evidence guards. The new question-only framework cannot see candidates, files, evidence, routes, ranks, scores or Gold. Only explicit question-grounded or logically indispensable core requirements may open a gap; optional checks are excluded from both T052 stages. This is the sole experimental variable.
- The experiment confirms that T047's expansion is generated in the post-union requirement layer and creates substantial downstream noise. On 71 development questions, old required slots contract from 462 to 73, mean 6.51 to 1.03 and median 6 to 1; 69 questions have one core and two have two. Only 2 optional checks are emitted. Base-audit open requirements collapse from 235 across 47 questions to 16 across 16. At cap +4, supplements fall from 167 to 49, non-Gold-bound supplements from 165 to 48, and wrong application-branch rows from seven to the protected base's five. The minimum contract therefore has a real purity and branch-safety effect.
- The same contraction is too coarse to improve evidence recall. Protected base / equal-size continuation / T052 expanded requirements / T053 minimum requirements retain 123/125/125/124 of 127 necessary groups, with question-macro evidence-pool coverage 98.7%/99.4%/99.2%/99.0% and complete-evidence success 94.4%/97.2%/97.2%/95.8%. T053 improves no case over T052 and regresses QA020; compared with equal-size continuation it additionally misses QA034. QA026's implementing-regulation article 29 remains correctly selected. Because T053 neither beats T052 nor the equal-size control and has a per-case regression, the development gate fails and the minimum contract is not adopted as a research baseline.
- The residual mechanisms are now separated. QA015's single core correctly asks how basic-analysis items are determined, but the base auditor combines several general and mineral-specific candidates and incorrectly marks it covered, so GB/T 13908-2020 clause 8.7.2.2 is never sought. QA020's complete-list requirement is correctly marked partial, but the selector fills four slots with other application rows and misses the Gold-bound extension item 19 full requirement row; a question-level list label cannot compute which structured branch row is absent. QA034's material requirement opens but still crowds out DZ/T 0479-2024 clause 6.1, while a separate workflow core is rejected because the model supplies the non-contiguous phrase `需要什么流程` rather than the original `需要什么材料和流程`. Future discussion must therefore distinguish base-coverage facets, deterministic structured-list set difference and syntax-aware parallel-subquestion decomposition; another global “more/fewer requirements” prompt is not authorized by this result.
- The T053 valid frozen caches record 213 successful stage calls, no normalization retry and 1,418,653 Tokens. One supplement alias is invalid (`U-C.2`) and deterministic validation rejects it; total discarded supplements are 46. Two aborted pre-freeze attempts caused by interface-contract violations are not present in the valid caches, so the recorded usage is a lower bound rather than total API billing. T053 verification passed 18 focused tests and all 551 tests, plus T053, T052 and v4 `--validate-only`. Database integrity is `ok`, foreign-key violations are zero, and the corpus hash remains `9f23c72...1d97`. Frozen-contract/result/report SHA-256 values are `7585e6b...24ab`, `ede6c9f...9e17`, and `8390600...e4bb4`.
- The 22-case confirmation split and 17-case holdout remain unexecuted. No corpus, Gold, candidate, retrieval/citation text, lexical/dense ranking, vector, Embedding, production FTS/index, final Chunk, ANN, KG, v3 KB, cloud, deployment or service mutation occurred; private model responses and results remain Git-ignored and `cloud_sync_required=false`. T053 is closed, and no next experiment is authorized by this decision.
- T054 tests the separately approved structured-list branch-set-difference hypothesis without changing the T053 frozen pool. A reuses T053's development evidence pools byte-for-byte. C activates only when the original question explicitly asks which materials must be submitted for a mining-right new establishment or extension, resolves the corresponding `自然资规〔2023〕4号附件4` manifest of complete citation-eligible `application_material_row` leaves, and appends the source-ordered set difference. Every appended row must already exist in the frozen keyword Top-50 plus vector Top-60 candidate union. B appends the same number of candidates from the unchanged T039 order. Gold is used only after selection for scoring.
- The deterministic structure repair is exact on the two triggered development cases. QA019's new-establishment branch already has all 14 authoritative rows and receives nothing. QA020's extension branch has nine of ten rows; its only absent row is `runit-4ea43977830e4071`, the complete item-19 requirement for proof of mining-right grant-proceeds (price) payment or compensated disposition. That row is already inside the frozen candidate union. C appends exactly it and raises QA020 from 9/10 to 10/10 necessary groups; equal-size B appends `runit-48ad26c27d07acfe` instead and remains at 9/10.
- Across 71 development questions, A/B/C retain 124/124/125 of 127 necessary groups, with question-macro evidence-pool coverage 99.0%/99.0%/99.2% and complete-evidence success 95.8%/95.8%/97.2%. C improves only QA020, has zero regression, leaves the other 70 pools unchanged and keeps aggregate wrong application-branch rows at five. Both triggered lists become complete, so the bounded development gate passes. The result supports structured branch completion as an auxiliary overlay candidate; it does not authorize activation, confirmation/holdout execution or extension to ambiguous application types. QA015 and QA034 remain separate unresolved mechanisms.
- T054 verification passed nine focused tests and all 560 tests, plus T054, T053, T052 and v4 `--validate-only`. Database integrity is `ok`, foreign-key violations are zero, and corpus SHA-256 remains `9f23c72...1d97`. Result/report SHA-256 values are `73e54de...e65a` and `6fc0853...e50`. The 22-case confirmation split and 17-case holdout remain unexecuted. No corpus, Gold, candidate union, retrieval/citation text, lexical/dense ranking, vector, model/Embedding call, production FTS/index, final Chunk, ANN, KG, v3 KB, cloud, deployment or service mutation occurred; private outputs remain Git-ignored and `cloud_sync_required=false`. T054 is closed, and no next experiment is authorized by this decision.
- T055 tests the separately approved continuous-subclause “sandwich gap” hypothesis while preserving T054's development evidence pools as immutable prefixes. C may append a candidate only when the preceding and following immediate siblings in one explicit numbered-clause family are already retained, the missing middle sibling is already present in the frozen keyword Top-50 plus vector Top-60 union, and all three siblings share a question-derived target anchor. Questions asking how something is determined additionally require the middle sibling itself to contain a determination relation. B appends the same number of candidates from the unchanged T039 continuation. Gold is used only after selection for scoring.
- Five development questions are deterministically recognizable as “how determined” questions: QA015, QA016, QA036, QA046 and QA083. Only QA015 satisfies all guards. Its frozen pool already contains GB/T 13908-2020 clauses 8.7.2.1 and 8.7.2.3; C appends the intervening clause 8.7.2.2, `runit-c1b9d10cec13993f`, which states the relation between chemical full analysis and determination of basic/composite analysis items. QA015 therefore rises from two of three to all three required evidence groups. Equal-size B appends `runit-5ac8bb81449740b2` and remains at two of three. QA016 has an apparent numbered gap but is rejected because the middle clause does not contain the required determination relation.
- Across 71 development questions, A/B/C retain 125/125/126 of 127 necessary groups. Evidence-pool coverage rises from 99.2% to 99.6%, and complete-evidence success rises from 97.2% to 98.6%. C improves only QA015, regresses no case, leaves the other 70 evidence pools byte-for-byte unchanged and keeps wrong application-branch rows at five. The bounded development gate passes, but the overlay is not activated and no confirmation/holdout evaluation is authorized. QA034 is the sole remaining necessary-evidence gap and remains a separate evidence-chain problem.
- T055 verification passed ten focused tests and all 570 tests, plus T055, T054, T053 and v4 `--validate-only`. Database integrity is `ok`, foreign-key violations are zero, and corpus SHA-256 remains `9f23c72...1d97`. Result/report SHA-256 values are `ca7b73c...a295` and `7ee8898...3746`. The 22-case confirmation split and 17-case holdout remain unexecuted. No corpus, Gold, candidate union, retrieval/citation text, lexical/dense ranking, vector, model/Embedding call, production FTS/index, final Chunk, ANN, KG, v3 KB, cloud, deployment or service mutation occurred; private outputs remain Git-ignored and `cloud_sync_required=false`. T055 is closed, and no next experiment is authorized by this decision.
- The user-approved Gold v5 amendment preserves Gold v4 byte-for-byte and replaces only compound QA034 with QA034A and QA034B. QA034A independently scores four workflow groups: the 2026 policy query/applicability clause and DZ/T 0479-2024 clauses 6.1, 6.4 and 6.5. QA034B independently scores nine attachment-contract groups: clause 8.1, the informative Appendix-B title, B.7.1 through B.7.6 and clause 6.5 as the condition source for B.7.4/B.7.6. The contract explicitly excludes treating Appendix B as a nationally uniform mandatory administrative application list and excludes the Appendix-C assessment-report branch. Gold v5 contains 112 cases and 184 unconditional groups; no other case, corpus row, retrieval leaf, vector or historical baseline changed.
- T056 tests only the approved parallel-subquestion selection overlay. Both QA034A and QA034B inherit the same exact 79-candidate keyword-50/vector-60 union and the same 24-row T055 evidence prefix. Two independent `deepseek-v4-flash` temperature-zero calls receive the same universal prompt, original candidate text/metadata/relative order and non-semantic short aliases, but no Gold, route identity, rank, score, hypothetical answer or protected-pool text. The model may append at most 12 remaining citable candidates per branch, with a deterministic candidate-verbatim basis guard. B appends the same number of candidates per branch from the unchanged T039 order. Other 70 development cases are identity arms.
- Parallel decomposition fixes the flow/material competition but does not make model selection complete. QA034A's base lacks only clause 6.1; C selects it and rises from 3/4 to 4/4, but also selects 11 non-Gold candidates and reaches its 12-row cap. The equal-size continuation also happens to include clause 6.1 and reaches 4/4. QA034B starts at 3/9; C selects clause 8.1 and B.7.4 but misses the informative Appendix-B title, B.7.3, B.7.5 and B.7.6, ending at 5/9. T057's later exact membership audit corrected the original attribution: all four missing leaves exist in the 23,250 frozen retrieval leaves but are outside T056's 79-candidate lexical/dense union. T056 therefore demonstrates an incomplete result and wrong-branch selection, but by itself cannot attribute these four misses solely to model reading rather than the candidate boundary.
- T056 also exposes branch selectivity failure. The flow call selects clause 8.2, which is for a project that does press identified resources and points to the Appendix-C assessment report; the material call selects C.9.7 from that assessment-report appendix. Thus two wrong assessment-report/Appendix-C candidates enter C. Across 72 split-development cases and 136 necessary groups, A/B/C retain 129/130/132, equal to 94.9%/95.6%/97.1% micro evidence-group recall. Question-macro evidence-pool coverage is 98.7%/99.1%/99.4%, and complete-evidence success is 97.2%/98.6%/98.6%. C improves QA034A and QA034B over A with no append-only regression, but QA034B remains incomplete and the wrong branch is nonzero, so the development gate fails and the scheme is not adopted.
- T056's final valid caches record two successful calls, 19,356 Tokens and no normalization retry. A prior pre-freeze attempt was rejected because a relation basis omitted a source layout newline; at least three calls from that aborted attempt are not cached, so the recorded use is a lower bound. Deterministic normalization now permits omission of source whitespace only while recovering and storing the exact source span; it does not permit punctuation or wording changes. T056 verification passed nine focused tests and all 586 tests, plus T056, T055, Gold v5 and v4 `--validate-only`. Database integrity is `ok`, foreign-key violations are zero, and corpus SHA-256 remains `9f23c72...1d97`. T056 result/report SHA-256 values are `59fa3cc...d1788` and `b4d6c11...b0ea0`.
- The 22-case confirmation split and 17-case holdout remain unexecuted. No corpus, candidate union, retrieval/citation text, keyword/dense/RRF ranking, document/query vector, Embedding, production FTS/index, final Chunk, ANN, KG, v3 KB, cloud, deployment or service mutation occurred; private Gold, candidates, model responses and experiment results remain Git-ignored and `cloud_sync_required=false`. T056 is closed after a failed development gate. No follow-on prompt change, deterministic Appendix-B family completion, activation or confirmation/holdout run is authorized until the user reviews this result.

### 2026-07-28

- T057 tests the user-approved separation between semantic routing and deterministic structure completion. A single formal `deepseek-v4-flash` temperature-zero routing call sees the original compound QA034 plus structure metadata derived from its frozen candidate documents, but no Gold, route rank, retrieval score, evidence ID or expected answer. It may only decompose the question and identify a standard, report branch, appendix and numbered family. The frozen v2 route correctly selects `DZ/T 0479-2024《压覆矿产资源调查评估规范》`, maps workflow to chapters 5 and 6, maps materials to `未压覆矿产资源调查报告／资料性附录B／B.7`, requires the complete B.7 family and explicitly excludes the assessment-report/Appendix-C sibling.
- After route freeze, deterministic code scans the unchanged 23,250 retrieval leaves and resolves the source-defined package `8.1 → 资料性附录B → B.7.1—B.7.6`, with clause 6.5 retained as the conditional-material context for B.7.4/B.7.6. This is a bounded candidate-boundary expansion, not reranking of the 79-candidate union. Exact membership shows that five of the nine package leaves were inside T056's 79 candidates and four were outside: `runit-b69afbf9d54690fc` (informative Appendix-B title), `runit-b4a926084ce34b12` (B.7.3), `runit-c4084d01577a5c7a` (B.7.5) and `runit-2aa22b8bf5e3b8a2` (B.7.6). This corrects the earlier T056 conclusion that all four were already available to the selector.
- A reuses T056's frozen evidence pools, B appends the same number of candidates from the unchanged original T039 continuation, and C appends only the routed structural set difference. Across 72 development cases and 136 necessary evidence groups, A/B/C retain 132/132/136, with micro evidence-group recall 97.1%/97.1%/100.0%, question-macro coverage 99.4%/99.4%/100.0% and complete-evidence success 98.6%/98.6%/100.0%. QA034B rises from 5/9 in A and B to 9/9 in C; all other 71 cases are identical. The bounded mechanism gate therefore passes, and equal-size context growth does not explain the gain.
- The complete T056-plus-T057 architecture is not adopted. Before T057 additions, QA034A and QA034B contain 14 assessment-report/Appendix-C rows when counted per branch, representing eight unique leaves. Two were added by T056's model; the rest were inherited from the earlier frozen prefix. T057 adds zero Appendix-C row, so its structural completion is selective, but append-only safety also prevents it from removing inherited pollution. The mechanism is retained as a proven bounded candidate-generation primitive; activation requires a separately designed branch-purification or clean-base experiment and fresh user approval. Confirmation and holdout are not executed.
- T057 validation, T056 validation, Gold v5 validation, v4 database validation and all 596 tests pass. Database integrity remains `ok`, foreign-key violations remain zero, corpus counts remain 156 documents and 20,670 content units, and corpus SHA-256 remains `9f23c72...1d97`. T057 result/report/formal-v2-route-cache SHA-256 values are `863e454...e3d3e`, `a320b75d...196c0` and `cc54998d...64864`. The formal scored cache records one call and 6,097 Tokens; at least eight pre-freeze calls were excluded, so recorded API usage is a lower bound. No retrieval rerun, keyword/dense/RRF rewrite, corpus/Gold/vector/Embedding/index/final-Chunk/ANN/KG/v3/cloud/deployment/service mutation occurred; private artifacts remain Git-ignored and `cloud_sync_required=false`. T057 is closed, with no next experiment authorized by this decision.
- T058 rebuilds every one of the 72 split-development cases independently from unchanged T039 order and never inherits T053—T057 evidence pools. The clean base is the first 20 leaves that are both searchable and citation-eligible, not the first 20 raw search results: 39 cases skip 75 ranked rows such as non-citable chapter headings before reaching 20. This eligibility rule is common to A/B/C and therefore is not an experimental variable. Gold is used only after pool construction for scoring.
- A independently replays the frozen T054 new-establishment/extension list completion, T055 intervening numbered-clause completion and T057 model-routed B.7 deterministic family expansion. B first quarantines only source-defined mutually exclusive branches—sibling application types for QA019/QA020 and the chapter-7, clause-8.2 and Appendix-C assessment branch within DZ/T 0479-2024 for QA034A/QA034B—then refills the 20-leaf base in unchanged T039 order and replays the same rules. Quarantined rows remain in the audit and are not deleted. C alone protects normative leaves from the frozen T046 governed-concept route for non-list “规定” questions; this adds only `runit-687e3eeeac717f05`, `《中华人民共和国矿产资源法实施条例》`第二十九条, to QA026 and excludes the two service-guide candidates.
- Across 136 necessary development evidence groups, A/B/C retain 134/135/136, with micro recall 98.5%/99.3%/100.0%, question-macro evidence-pool coverage 99.3%/99.7%/100.0% and complete-evidence success 97.2%/98.6%/100.0%. A→B improves only QA034A by allowing clause 6.1 to refill the clean base; B→C improves only QA026. Neither comparison regresses a case. Application wrong-branch rows are 5/0/0, and the broader source-forbidden branch audit is 19/0/0. No non-trigger question changes. The clean development gate passes, demonstrating that all known development evidence gaps can be recovered without preserving the rejected experimental pool.
- T058 validation, T057 validation, T056 validation, Gold v5 validation, v4 database validation, ten focused tests and all 606 tests pass. Database integrity remains `ok`, foreign-key violations remain zero, corpus counts remain 156 documents and 20,670 content units, and corpus SHA-256 remains `9f23c72...1d97`. T058 result/report SHA-256 values are `7f2f3b0...61579` and `a31fb14...4baa`. No new model or Embedding call, retrieval rerun, keyword/dense/RRF rewrite, corpus/Gold/vector/index/final-Chunk/ANN/KG/v3/cloud/deployment/service mutation occurred; private results remain Git-ignored and `cloud_sync_required=false`. T058 is closed after passing its development gate, but no architecture is activated and the 22-case confirmation split and 17-case holdout remain unexecuted. A confirmation run or any next experiment requires a separate user decision.
- T059 executes the user-approved 22-question confirmation run once with the T058 implementation, development result and all upstream inputs hash-frozen before scoring. The confirmation split contains 26 necessary evidence groups and had been observed by earlier experiments, so it is a confirmation set rather than a fresh blind test. It does not select parameters or alter triggers, and Gold v5 is used only after evidence-pool construction. The 17-question holdout remains sealed.
- All three frozen arms retain 24/26 groups: micro evidence-group recall is 92.3%, question-macro evidence-pool coverage is 98.2%, complete-evidence success is 95.5%, Unit Hit@10 is 100% and MRR@20 is 0.850. A→B, B→C and A→C each have zero improvement and zero regression. No branch/list/intervening-clause/B.7/concept overlay triggers; all pools remain 20 leaves. Application wrong-branch and broader source-forbidden counts are both zero in every arm.
- The two missing groups are both in QA030, `油气的资源储量是怎么划分的？`. The clean Top-20 retains GB/T 19492-2020 clauses 4.1, 4.2 and 4.3 but not clause 4.7 (`runit-07073b1ecad0f6b8`, controlled/proved technical recoverable reserves) or clause 4.8 (`runit-117b5dcb7486b6ad`, controlled/proved economic recoverable reserves). Both leaves are present in the frozen candidate union, but only on the dense route at ranks 32 and 26; because the lexical route does not retrieve them, equal-weight RRF places them at protected ranks 63 and 52. This is not corpus or candidate recall loss and not branch pollution. It exposes a broad five-clause evidence-chain completion/fusion-truncation gap not covered by the development-specific deterministic rules.
- T059's strict confirmation gate therefore fails and the T058 architecture is not activated. T059 validation, T058 validation, v4 database validation, ten focused tests and all 616 tests pass. Database integrity remains `ok`, foreign-key violations remain zero and corpus SHA-256 remains `9f23c72...1d97`. T059 result/report SHA-256 values are `813a752...4e31` and `53b4dcc...bdcb`. No model or Embedding call, retrieval rerun, lexical/dense/RRF rewrite, corpus/Gold/vector/index/final-Chunk/ANN/KG/v3/cloud/deployment/service mutation occurred; private outputs remain Git-ignored and `cloud_sync_required=false`. T059 is closed after the failed confirmation gate. Any targeted QA030 experiment or holdout execution requires a separate user decision.
- T060 applies the user's QA030 correction as a new Gold v6 artifact and preserves Gold v5. The stale v5 `review_note` claimed that the answer was based on both GB/T 19492-2020 and DZ/T 0400-2022, although every answer claim, required group and citation is from GB/T 19492-2020. Gold v6 changes only this note to state that the five groups rely on clauses 4.1, 4.2, 4.3, 4.7 and 4.8 and that DZ/T 0400-2022 is not an answer basis. QA030's question, reference answer, evidence claims, citations and required groups remain identical; the other 111 cases and all counts remain identical. Gold v6 JSON/report SHA-256 values are `39f4ffa...c2d87` and `9690d7f...60f62`.
- T061 is a post-confirmation targeted diagnostic, not a fresh confirmation or blind test. It freezes all T058/T059 final pools. A is the frozen pool, B appends the same count from unchanged citation-eligible T039 continuation, and C appends only candidates already in the frozen lexical/dense union. C activates when the question asks a broad classification/type/composition system, at least two explicit retained clauses come from one classification-labelled section, and a missing same-section sibling states either a classification/hierarchy relation or an explicit `在…中根据…估算…` source-to-target relation. Selection reads the question, frozen pool, source structure and candidate text only; Gold v6 is used after selection for scoring.
- A preliminary overbroad `分为哪` trigger also activated QA100, `地下水资源勘查按水文地质工作精度分为哪几个阶段？`, and appended GB/T 15218-2021 clause 5.1 about storage/recharge/exploitable-quantity categories. That row was same-section but not direct evidence for the requested work stages, so the pre-freeze trigger was rejected. The frozen rule excludes pure stage questions unless they explicitly ask for types, categories or a system. This reduces final activation to QA030 alone and prevents the extra QA100 context row.
- On the 94 already-known development-plus-confirmation questions and 162 necessary groups, A/B/C retain 160/160/162. Development remains 136/136 in all arms; confirmation moves 24/24/26. C appends exactly `runit-07073b1ecad0f6b8` (GB/T 19492-2020 clause 4.7) and `runit-117b5dcb7486b6ad` (clause 4.8), both already inside the frozen candidate union, and raises QA030 from 3/5 to 5/5. B appends two ordinary continuations and remains 3/5. C improves only QA030, regresses no case, adds no non-required row and keeps wrong-branch count at zero; all other 93 pools are identical. The targeted coverage, safety and selectivity gates pass, but because the rule was designed after observing QA030, generalization is not proven and the architecture is not activated.
- T060/T061 validation, T059/T058 validation, v4 database validation, 15 new focused tests and all 631 tests pass. Database integrity remains `ok`, foreign-key violations remain zero and corpus SHA-256 remains `9f23c72...1d97`. T061 result/report SHA-256 values are `bafd71f...e5af28` and `85754fe...6b0060`. No model or Embedding call, retrieval rerun, lexical/dense/RRF rewrite, corpus/retrieval/vector/index/final-Chunk/ANN/KG/v3/cloud/deployment/service mutation occurred; private outputs remain Git-ignored and `cloud_sync_required=false`. T060 and T061 are closed. The 17-question holdout remains sealed, and no production activation or next experiment is authorized without user discussion.

- T062 creates only a user-review draft for testing whether T061's classification-section completion generalizes. The 24 exact questions are absent from Gold v6 and are divided into eight broad positives, eight single-point guards and eight negative controls. Their 78 singleton evidence groups are bound to current, approved, searchable and citation-eligible v4 retrieval leaves; 77 leaves have not previously served as Gold-v6 required evidence, while the one overlap is DZ/T 0340-2020 clause 5.2.2 inside a ten-clause broad trial-level answer. Redundant GB/T 15218-2021 clause 5.3 was removed from the necessary-evidence contract before review handoff.
- At creation, T062 remained `review_needed`: the questions, answers and evidence boundaries were not Gold until user approval. No retrieval, candidate generation, scoring, model or Embedding call, holdout execution or architecture activation occurred during draft preparation. Six focused checks and all 637 tests passed; database integrity was `ok`, foreign-key violations were zero, counts remained 156 documents and 20,670 content units, and corpus/Gold-v6/retrieval-unit SHA-256 values remained `9f23c72...1d97`, `39f4ffa...c2d87` and `6d96def...99e72`. Draft JSON/Markdown SHA-256 values were `a95e192...8d717` and `a1ce4b4...048bd`; private outputs remained Git-ignored and `cloud_sync_required=false`.
- The user approved T062 without amendment for a controlled generalization test. T063 freezes the approved 24 questions, reference answers and 78 singleton necessary-evidence groups before retrieval; the frozen-contract SHA-256 is `aed9372...f63a`. Keyword and vector routes use only the exact frozen original questions, with no DeepSeek rewrite. The experiment reuses the immutable 23,250-row `qwen3.7-text-embedding` document-vector snapshot and creates only 24 private query vectors. A is the first 20 searchable and citable rows after lexical Top-50 plus exact dense Top-60, equal RRF and frozen lexical-1/dense-4 head admission; B appends the same number of unchanged-order continuation rows as C; C applies the T061 rule byte-for-byte. Gold is used only after selection.
- The fresh candidate union contains 74/78 necessary evidence groups (94.9%). The four absent groups are DZ/T 0340-2020 clauses 5.2.3, 5.2.4 and 5.2.5 for CBP002, and EJ/T 1157-2018 clause 8.2.3 for CBP008. A/B/C retain 41/41/42 groups, equal to 52.6%/52.6%/53.8% micro evidence-group recall; complete-question success remains 17/24 in every arm. C improves only CBP004 by one group, while the equal-size continuation improves none. Of the 36 groups C still misses, four never entered the union and 32 were already in the union but were truncated or not selected.
- T063 demonstrates that the unchanged T061 mechanism is selective but not general. All eight single-point guards and all eight negative controls remain complete; there are zero false triggers, zero non-required additions and zero regression on the prior 94 questions/162 groups. The strict fresh-boundary gate nevertheless fails because necessary-evidence recall is not complete. The failure reflects narrow surface-pattern and relation-text conditions plus four candidate-boundary misses: for example, `类型体系如何组成` is not the exact `组成体系` trigger, and many source-defined sibling clauses express a type's criterion rather than one of T061's narrow `分为/划分为/转换` relations.
- T063 is closed after seven focused tests, v4 `--validate-only` and all 644 tests pass. Database integrity is `ok`; counts remain 156 documents and 20,670 content units; corpus, retrieval-leaf and durable document-vector SHA-256 values remain `9f23c72...1d97`, `6d96def...99e72` and `8a5d0b3...644f`. Result/report SHA-256 values are `ffc2273...3787` and `abf9e56...1f09`. No corpus, Gold, retrieval leaf, durable document vector, production index, final Chunk, ANN, KG, v3 KB, cloud, deployment or service mutation occurred; the 17-question holdout remains sealed, no architecture is activated, private artifacts remain Git-ignored and `cloud_sync_required=false`. Any redesign or follow-on experiment requires a separate user decision.
- T064 tests only the user-approved repair of T063's four candidate-union misses. A is the frozen original-question lexical Top-50 plus exact dense Top-60 union. B appends the same number of searchable and citable candidates as C from the original retrieval routes extended to lexical Top-180 and dense Top-235, then ordered by unchanged equal RRF. C reads no case ID, Gold group, expected clause or retrieval rank: a broad family question must name a parent heading verbatim, and its document must be routed either by an explicit standard number or by at least two named parents in that same document. C adds only missing direct children, never a whole chapter and never recursively expands grandchildren.
- The deterministic route fires only on CBP002 and CBP008. CBP002 names DZ/T 0340-2020 parents 5.1 `工艺矿物学研究`, 5.2 `矿石加工选冶试验研究` and 5.3 `非金属矿物化性能测试研究`; their direct-child package adds only missing clauses 5.2.3, 5.2.4 and 5.2.5. CBP008 explicitly names EJ/T 1157-2018 and parent 8.2 `资源/储量分类`; its direct-child package adds only clause 8.2.3. C therefore adds exactly four necessary groups and no other row. B adds four equal-size ordinary candidates but none is required.
- Across the 24 fresh boundary questions and 78 necessary evidence groups, A/B/C candidate recall is 74/74/78, or 94.9%/94.9%/100.0%; complete candidate questions are 22/22/24. Broad positives rise from 57/61 to 61/61 only in C. All eight single-point guards and all eight negative controls remain identity cases, so C has zero false trigger and zero non-required addition. The candidate-boundary gate passes and equal-size ordinary growth does not explain the recovery.
- T064 does not activate the mechanism or claim that final retrieval is solved. It changes candidate generation only and deliberately leaves T063's 32 in-union-but-not-final evidence groups unresolved; no final Top-20/reranking experiment and no prior-94 final regression were run. The 17-question holdout remains sealed. Eight focused tests, v4 `--validate-only` and all 652 tests pass. Database integrity remains `ok`, counts remain 156 documents and 20,670 content units, and corpus/retrieval/document-vector hashes remain `9f23c72...1d97`, `6d96def...99e72` and `8a5d0b3...644f`. Result/report SHA-256 values are `c72b2cb...dda9` and `3c30f01...f222`; no new model or Embedding call, corpus/Gold/retrieval/vector/index/final-Chunk/ANN/KG/v3/cloud/deployment/service mutation occurred, private artifacts remain Git-ignored and `cloud_sync_required=false`.
- T065 fixes T064's 78/78 candidate union and tests only final-pool membership. Every arm is exactly 20 citable rows. A converts the existing T061 supplement into a fixed reservation common to all arms and otherwise preserves frozen fusion order. B promotes the same number of ordinary candidates as C and refills by that order. C reserves either all available direct children of question-named source parents, routed by an explicit standard number or at least two named parents in one document, or all two-field siblings when the question explicitly requests both `划分单元` and the corresponding resource/reserve-scale quantity field. C then fills unused slots in unchanged order. No case ID, Gold group, target rank or expected evidence leaf is read during selection.
- C triggers seven incomplete broad positives and leaves already-complete CBP001 plus all eight single-point guards and all eight negative controls unchanged. Direct-child package/promotion counts are CBP002 10/9, CBP004 7/5, CBP005 7/4, CBP006 8/6, CBP007 7/4 and CBP008 4/3. CBP003 uses the repeated two-field structure and reserves 12 source siblings, promoting five missing scale-field rows. Every package row is necessary under the frozen contract; the largest package is 12 and therefore remains within the fixed 20-row budget.
- Across 24 questions and 78 necessary groups, fixed20 A/B/C retain 42/45/78, or 53.8%/57.7%/100.0%, and complete 17/17/24 questions. Broad-positive evidence rises from 25/61 in A and 28/61 in B to 61/61 in C. B happens to improve CBP002, CBP006 and CBP007 but regresses CBP005, showing that equal-size ordinary promotion is unstable and does not explain C. C improves CBP002—CBP008, regresses no question, adds no non-required package row and has zero single-point/negative trigger. The fresh fixed-final-pool gate passes.
- T065 does not activate the architecture or establish general behavior outside the fresh boundary set. The prior 94 questions have not been rerun through the combined T064 candidate completion plus T065 final reservation, and the 17-question holdout remains sealed. Eight focused tests, v4 `--validate-only` and all 660 tests pass. Database integrity remains `ok`, counts remain 156 documents and 20,670 content units, and corpus/retrieval/document-vector hashes remain `9f23c72...1d97`, `6d96def...99e72` and `8a5d0b3...644f`. Result/report SHA-256 values are `5908fec...0fdd` and `f78890c...95dc`; no model/Embedding call, corpus/Gold/retrieval/vector/index/final-Chunk/ANN/KG/v3/cloud/deployment/service mutation occurred, private artifacts remain Git-ignored and `cloud_sync_required=false`.
- T066 is a separate regression/compatibility replay on the frozen 94-question known set, not a new confirmation or holdout test. A preserves the accepted T061 final pools of 20—27 rows. B fixes every pool at 20 while reserving all existing deterministic additions after the accepted clean base. C uses the same fixed budget and reservations, then applies the unchanged T064 named-parent direct-child candidate completion and unchanged T065 direct-child/repeated-pair source-structure reservation. The combined selector reads the original question, frozen candidate/pool identities and source structure only; case IDs and Gold evidence are not used for selection.
- A/B/C each retain all 162/162 necessary evidence groups and complete 94/94 questions, with zero wrong-branch row. The fixed conversion changes only QA015, QA020, QA026, QA034B and QA030 because their prior pools contained 21—27 rows; all 12 previously accepted specialized-rule additions remain reserved inside B and C, and no evidence regresses. T064 and T065 add nothing on the known set, so B and C are identical with zero new non-required structure row. This establishes compatibility and selectivity on the frozen 94 plus the separately tested fresh 24-question scope; it is not a universal-generalization or production-activation claim.
- T066 is closed after eight focused tests, v4 `--validate-only` and all 668 tests pass. Database integrity remains `ok`, foreign-key violations remain zero, counts remain 156 documents and 20,670 content units, and corpus/retrieval/document-vector hashes remain `9f23c72...1d97`, `6d96def...99e72` and `8a5d0b3...644f`. Result/report SHA-256 values are `82e85d3...01719` and `3202c68...b819`. No new model/Embedding call, retrieval rerun, corpus/Gold/retrieval/vector/index/final-Chunk/ANN/KG/v3/cloud/deployment/service mutation occurred; the 17-question holdout remains sealed, the architecture remains inactive, private artifacts remain Git-ignored and `cloud_sync_required=false`.
- T067 freezes the proposed one-time holdout acceptance contract before any execution. Its builder verifies pinned file hashes and reads only the T032 split identity list to recompute the 17-case identity hash; it does not load or emit holdout case IDs, question text, Gold answers or evidence rows. No holdout query embedding, ranking, model/reranker call or retrieval output is created. The contract remains `frozen_pending_user_review_and_runtime_closure`, with `holdout_execution_authorized=false`.
- The hard pass standard is deliberately evidence-complete: 100% candidate required-evidence-group recall, 100% final `Coverage@20`, 17/17 complete-evidence questions, zero wrong-branch/search-ineligible/citation-ineligible/duplicate final rows, exactly 20 final rows per question, zero input drift and zero database/index mutation. `Unit Hit@10`, `MRR@20`, `Doc Hit@5`, candidate coverage and structural-trigger traces must be reported but cannot compensate for a missing required group. There is no conditional pass. Once a complete holdout ranking exists, the set is observed and cannot be tuned on and reclaimed as blind; a changed architecture requires a new sealed confirmation set for a new blind claim.
- T067 records a pre-execution blocker rather than concealing it. T066 starts from frozen per-question T061 pools, T058 contains QA034/QA034B identity conditions and known-question cached concept routes, and no accepted generic end-to-end runner currently maps an unseen original question through retrieval and the accepted deterministic overlays into fixed20. Before holdout approval, a new runner must remove or generalize those dependencies and shadow-replay all known 94 questions to 162/162 groups plus the fresh 24 questions to 78/78, with exact20, zero wrong branch and zero regression, without reading holdout questions.
- T067 is handed off for user review after eight focused tests, v4 `--validate-only` and all 676 tests pass. Database integrity remains `ok`, counts remain 156 documents and 20,670 content units, and corpus/retrieval/document-vector hashes remain `9f23c72...1d97`, `6d96def...99e72` and `8a5d0b3...644f`. Contract/report SHA-256 values are `d40f149...c694c` and `7157786...3247`. No corpus/Gold/retrieval/vector/index/final-Chunk/ANN/KG/v3/cloud/deployment/service mutation occurred; `cloud_sync_required=false`.
- T068 distinguishes a generic final-pool selector from a genuinely executable end-to-end runner. The accepted runtime now starts from the original question, recomputes the frozen FTS document route and document-local lexical Top-50, performs exact cosine search over the frozen 23,250-row `qwen3.7-text-embedding` snapshot for dense Top-60, applies equal RRF with lexical-1/dense-4 head admission, and only then invokes generic structural reservation and unchanged-order fill to exactly 20 rows. Selection receives no case ID, dataset split, Gold answer/group, expected evidence ID, target rank or known-question route cache.
- T068 removes the remaining identity-specific behavior with question/source semantics. `不作为压覆` or `未压覆` plus investigation context quarantines DZ/T 0479-2024's assessment branch; an appendix-attachment list is routed by the most specific question domain, preserves the source cross-reference, appendix title and direct child rows, and uses material-specific noun phrases such as `互不影响协议` to recover same-document conditional context while still excluding assessment siblings. Complete application-list expansion is disabled for conditional substitutions such as `没有重大调整`. Detailed classification questions that explicitly request type conditions plus conversion/adjustment reserve the uniquely question-matched full classification section, while simple classification questions retain the narrower T061 relation completion. The governed `勘查实施方案/勘查方案` plus `评审/审查` authority protection, numbered-gap, T064 direct-child candidate completion and T065 source-structure reservation remain generic.
- End-to-end shadow replay retains 162/162 necessary groups on the known 94 and 78/78 on the fresh 24. Candidate and final evidence therefore both reach 240/240, all 118 questions are complete at exactly 20 rows, and wrong-branch and non-required structural selections are both zero. The runner reuses 116 existing known/fresh query vectors and creates only two known QA034 split-query vectors; no holdout vector is created. Independent runs with `PYTHONHASHSEED=1` and `2` have the same selection projection SHA-256 `76755c9...cd260`.
- T068 pins runner/result/report SHA-256 values `db1e761...0efc1`, `971dd49...39136` and `6f24b19...37ae2`; the 118-row shadow query-vector SHA-256 is `6444a52...3b789`, and the runtime-closure contract amendment SHA-256 is `5c53b9c...0510`. Eight focused tests, v4 `--validate-only` and all 684 tests pass. Corpus/retrieval/document-vector hashes remain `9f23c72...1d97`, `6d96def...99e72` and `8a5d0b3...644f`. The architecture remains inactive, the 17-question holdout remains unread/unembedded/unexecuted, a one-time run still requires explicit user approval, and `cloud_sync_required=false`.
- The user explicitly authorized T069 with “批准运行”. Before any query embedding or ranking, the new wrapper was compiled, reviewed and hash-pinned together with T068 and all 25 project script modules reachable by that runtime. The first preflight stopped safely because the handoff summary said 21 necessary groups while the byte-identical frozen Gold v6 actually contains 22: QA012 has three groups and QA023/QA040/QA041 each have two. Frozen split and Gold hashes still matched T067. The count assertion and authorization hash were corrected before execution; the successful second preflight found exactly 17 cases, 22 groups, zero prior rankings and no input drift.
- T069 then performed one logical run only. It persisted 17 new 1024-dimensional `qwen3.7-text-embedding` query vectors and wrote each fixed20 ranking atomically. Candidate recall is 22/22, but final `Coverage@20` is 21/22 and only 16/17 questions are evidence-complete. The sole failure is QA008, `沙金应该使用哪个标准？`: DZ/T 0208-2020 clause 1 (`runit-a028c124df2ff051`) is in the candidate union only through dense rank 39, is absent from document-local lexical Top-50, and falls to protected/equal-RRF order rank 71. No generic structural rule triggers, so the required clause is not reserved into the final 20. QA009's more explicit `砂金地质勘查适用哪一个规范？` retains the same clause at final rank 16, exposing a short-query semantic-alias and single-route-fusion vulnerability rather than a corpus gap.
- Every final pool remains exactly 20 unique, searchable and citation-eligible rows. Wrong-branch rows, input drift, database/index mutation and case-ID/Gold-dependent selection are all zero. The manual audit reviewed all 17 questions, the one displaced group, all structural traces, application/standard conflicts and authority precedence. Structural reservations and non-required structural additions are both zero; no harmful evidence, unresolved branch conflict or authority inversion was found. It separately records that frozen QA040 and QA041 ask only about internal-check quantities while also treating the neighboring external-check clause as necessary evidence; this did not cause T069's failure and was not changed after observation, but should be reconsidered when constructing a future blind set.
- T069 has no conditional pass. Its status is `failed_observed_diagnostic_only`; automatic hard gates fail, final sealed acceptance fails, production activation remains unauthorized, and these 17 questions may never be tuned on and reclaimed as blind. Wrapper/authorization/final-result/report SHA-256 values are `932d1b7...cf92`, `d21512b...7288`, `7946fde...fa8d2` and `d84e0db...2494`. Six focused tests and all 690 tests pass; T069 `--validate-only`, v4 `--validate-only`, database integrity and foreign-key checks pass. Corpus and experimental FTS hashes remain `9f23c72...1d97` and `4aa39d2...6c2f`; no corpus, FTS, document-vector, ANN, knowledge-graph, v3, cloud, deployment or service mutation occurred, and `cloud_sync_required=false`.
- T070 tests the user's preferred alias-normalization direction strictly as a post-holdout QA008 diagnostic. A is the frozen original question. B preserves A and adds the directed surface form `砂金应该使用哪个标准？`. C additionally adds `金属砂矿类应该使用哪个标准？`. The aliases form one lexical route: candidates take only their best rank across variants, so synonymous forms do not add independent weights. The persisted QA008 dense vector, Dense Top-60, equal RRF/head admission, T068 structural selector and fixed20 remain unchanged; no embedding or model call occurs.
- A exactly reproduces T069: the target scope clause is absent from lexical Top-50, dense rank 39, fused/protected order rank 71 and absent from final20. B leaves it outside lexical Top-50 and only raises fused order to 65. C correctly routes the DZ/T 0208-2020 document at document rank 1; inside the concept-only lexical variant the scope clause is rank 37, but merging the three variant rankings admits many table and subject-matter rows ahead of it and places the clause at merged lexical rank 82. It therefore still misses lexical Top-50, remains fused rank 65 and is absent from fixed20. The failure is not lack of a concept mapping; it is variant-union dilution plus the absence of an intent rule that prefers a standard's scope clause for “使用哪个标准” questions.
- T070 rejects both tested alias-union schemes as sufficient fixes. All 134 non-target questions across known94, fresh24 and the other observed T069 cases lack the exact `沙金` trigger and delegate to frozen T068, while all three QA008 pools remain exact20 with zero wrong branch. This proves local selectivity but not generalization. Script/result/report SHA-256 values are `515cfd1...c223`, `cc7acf9...de64` and `d884e64...2240`; six focused tests, v4 `--validate-only` and all 696 tests pass. Architecture activation remains false, T069 remains failed, and a follow-up must separately choose between canonical replacement and question-intent/scope-clause protection before any new experiment; `cloud_sync_required=false`.
- T071 removes T070's union as the only experimental change. A sends the original question to the lexical route. B sends only `砂金应该使用哪个标准？`. C sends only `金属砂矿类应该使用哪个标准？`. The original question remains recorded and still drives the generic structural selector; the durable original QA008 vector, Dense Top-60, equal RRF/head admission and fixed20 remain byte-identical. Thus this tests replacement rather than hidden weight accumulation, query-vector regeneration or a new ranking rule.
- A reproduces the frozen failure at lexical/dense/fused/final ranks `未进前50/39/71/未进前20`. B routes the correct document first but still cannot retrieve its scope clause lexically and only moves fused rank to 64. C also routes the correct document first, places DZ/T 0208-2020 clause 1 at lexical rank 37, combines it with unchanged dense rank 39, and raises it to fused and final rank 10. The required evidence is therefore recovered. C's final20 consists entirely of the correct standard but includes many appendix/table leaves unrelated to the narrow “which standard” answer, so clause-level precision is not yet optimized even though the dangerous cross-document distractors in A disappear.
- T071 establishes only that a directed standard-family replacement is sufficient for the observed QA008 failure. It does not justify globally replacing `沙金` in questions about industrial indicators, sampling or resource estimation, where loss of the specific gold subtype could be harmful. A production alias rule must therefore bind the mapping to a document-identification/applicability intent or pass a separate boundary test covering such negative controls. Across the current 134 non-target questions the exact trigger count is zero, all pools remain exact20, and wrong/non-required structural selections remain zero. Script/result/report SHA-256 values are `d47b557...064bb`, `9ab9d32...fb315` and `4853414...dcb4a`; six focused tests, v4 `--validate-only` and all 702 tests pass. T069 remains a failed observed set, architecture activation remains false and `cloud_sync_required=false`.
- T072 separates the alias proposal into two auditable layers before implementation. Orthographic normalization `沙金→砂金` and `沙金矿→砂金矿` is global, requires no user confirmation and must retain the original question for audit. The category mapping `砂金/砂金矿→金属砂矿类` is not treated as a synonym: it is allowed only for explicit standard/document applicability questions. Industrial indicators, sampling, analysis quality, resource estimation and other technical-detail questions retain the specific `砂金` wording. Questions such as “沙金怎么勘查” remain ambiguous and must ask the user to select a scope before retrieval.
- T072 creates only a review contract, not a retrieval result or architecture activation. Its 18 new questions comprise six category-mapping positives, eight technical-detail guards and four confirmation-first ambiguities; the 14 scored questions each bind one current searchable/citable DZ/T 0208-2020 evidence group, and none exactly duplicates Gold v6 or T063. The inner/outer quality-control answer preserves the source limitation that the external-inspection proportion may be reduced when the original analysis sample count used for resource estimation is large, but not below 3%. JSON/report SHA-256 values are `b3ce20a...32123` and `acd0e31...87f71`; ten focused tests, v4 `--validate-only` and all 712 tests pass. Until user approval, `retrieval_authorized=false`; no model, embedding, lexical/dense retrieval, fusion, corpus/index/vector/cloud/deployment or service mutation occurred, architecture remains inactive and `cloud_sync_required=false`.
- The user approved the T072 questions and authorized testing with “好的，审阅完毕，请执行测试。” T073 freezes all 18 questions, answers, evidence groups and routing expectations without changing the reviewed source artifact. Fourteen scored cases receive one private 1024-dimensional `qwen3.7-text-embedding` vector generated from the canonical question; the four ambiguous questions receive no vector and never enter lexical or dense retrieval. Existing 23,250 document vectors are reused. Both arms share that canonical dense vector and the unchanged T068 keyword Top-50, dense Top-60, equal-RRF/head admission, generic structure selector and fixed20 runtime. The only variable is the six positive cases' lexical query: A keeps `砂金/砂金矿`, while B uses `金属砂矿类`.
- T073 candidate evidence is already 14/14 in both arms, so the measured defect is final-pool ordering rather than recall. A retains 10/14 groups overall and only 2/6 standard-applicability scope groups. B retains 14/14 overall and 6/6 positive groups: AIP002—AIP005 are newly recovered, while AIP001/AIP006 stay correct and no positive case regresses. Every one of the eight technical-detail cases remains 8/8 and has byte-identical A/B candidate order and final pool; all four ambiguous cases stop before embedding/retrieval. Across 28 scored final pools, every pool has exactly 20 unique searchable/citable rows, with zero wrong branch, ineligible row or non-required structural selection. This passes the frozen boundary-safety and utility gates and demonstrates a contribution from intent-gated standard-family mapping on the approved boundary set.
- Passing T073 does not itself activate a production rule or prove universal mineral-family coverage. Frozen/result/report SHA-256 values are `0c22460...4dcb4`, `1e9dd40...6db25` and `d4be658...9d267`; the query-vector SHA-256 is `ad0e8c7...9a99`. Twenty-one T072/T073 focused tests, v4 `--validate-only` and all 723 tests pass. Database integrity remains `ok` at 156 documents and 20,670 units; corpus, FTS and document-vector hashes remain unchanged. No ANN, knowledge graph, v3, cloud sync, deployment or service mutation occurred; production activation remains a separate user decision and `cloud_sync_required=false`.

### 2026-07-29

- The user authorized local activation of the accepted T073 rule with “先落地吧。在使用中我们再随时优化。” T074 activates only the reviewed two-layer sand-gold routing contract in the local generic query stack. Orthographic `沙金→砂金` remains global. Category mapping `砂金/砂金矿→金属砂矿类` is allowed only for explicit standard-applicability questions and only changes the lexical route; the semantic route preserves `砂金`. Technical-detail terms such as industrial indicators, grade, thickness, sampling, analysis, splitting, internal/external inspection, spacing and resource estimation take precedence and prohibit category expansion. Broad questions such as `沙金怎么勘查` stop before retrieval and offer four deterministic confirmation scopes: applicable standard, industrial indicators, sampling/analysis and resource estimation.
- T074 removes the former coarse rule that injected `金属砂矿类` and `DZ/T 0208-2020` into a deterministic candidate scope. The governed route now preserves original, normalized, lexical and semantic questions; mapping ID/version, intent, allow/stop state and reason remain auditable. `QuestionResolver` enforces confirmation without requiring a model, `KnowledgeStore` separates the FTS and vector query texts and stops ambiguous questions before FTS/Embedding/ANN, and `QueryAnalysis` plus its exported JSON schema record the same governed state. New `QueryPlan` fields remain at the end of the dataclass so existing positional callers keep their historical meaning.
- The activation replay reads the frozen T072/T073 contract and reuses all fourteen persisted query vectors; it creates no new query or document Embedding. The actual production route reproduces every T073-B candidate order and fixed-20 final pool. Candidate necessary evidence is 14/14, final `Coverage@20` is 14/14, and all 14 scored questions are complete. Six standard-applicability cases map, all eight technical-detail controls retain byte-identical frozen pools, all four ambiguous cases stop, and no mapping creates a hard filename or standard-number scope. Corpus, retrieval-unit, FTS, document-vector and query-vector hashes are unchanged.
- T074 closes after 99 focused tests, the complete 734-test suite and v4 `--validate-only` pass. Database integrity remains `ok` at 156 documents and 20,670 units. Script/result/report SHA-256 values are `9bf9f14...024b5`, `e95e97f...6bc91` and `6aa1927...1f697`. The result is local activation only: it does not generalize the category mapping to other minerals, change T069's observed-failure status, create a knowledge graph, connect to cloud infrastructure, deploy or restart a service. `cloud_sync_required=false`.
- A knowledge graph is not automatically the next implementation. The next discussion should first define a small governed relation layer and a fixed A/B contract for relationship-heavy questions. Candidate relations include document hierarchy/supersession/citation, mineral-to-standard applicability, clause/appendix/table/list membership, general-versus-special rule, authority/condition/action/consequence and conditional material branches. Every edge must point back to current citation-safe evidence and may expand candidates only; final answers still require source clauses. Keyword and vector retrieval remain primary parallel routes. A broad graph or separate graph database is deferred until a bounded relation set proves evidence-coverage or precision gains without harmful expansion or unacceptable latency.
- The user authorized preparation of the small governed relation-layer review contract with “可以，请执行”. T075 defines sixteen allowed relation types and 24 new review questions: 16 development and 8 confirmation; 18 bounded-expansion positives, 5 explicit no-expansion negatives and 1 ambiguity-first stop. Its 62 proposed relations bind 94 necessary evidence groups to current searchable, citable and answerable v4 retrieval units. The review set does not directly repeat a Gold-v6, T062 or T072 question.
- T075 freezes the future comparison before graph construction. A is T074's unchanged keyword-plus-vector union and fixed20 selector. B may only add user-approved relation candidates, may traverse at most two hops, cannot delete A candidates or become a hard filter, and must still cite concrete clauses or table rows. Development tests compare relationship-evidence reservation caps 0/2/4/6 and select the smallest cap with no A regression; the confirmation split is run once after freezing. Accuracy and efficiency remain separate claims: only an end-to-end P95 latency reduction of at least 10% can support an efficiency claim.
- T075 remains `review_needed`. Eight focused tests and v4 validation pass; the database remains at 156 documents and 20,670 content units. `kg_entities`, `kg_relations`, `document_relations` and `clause_effects` remain absent. Script/test/JSON/report SHA-256 values are `75bd0ae...94b1`, `8999ed1...f431`, `cc749ed...ad88` and `e5e7c47...90a7`. No graph, graph database, relation table, index, Embedding, retrieval result, corpus mutation, cloud connection, deployment or service operation occurred. `graph_build_authorized=false`, `graph_retrieval_ab_authorized=false`, `knowledge_graph_created=false`, and `cloud_sync_required=false`.
- The user authorized building the local production layer with “可以好的，先建立本地生产层”. T076 adds a v4 adapter behind the existing `/knowledge/*` API contract and an explicit `KNOWLEDGE_RUNTIME_VERSION=v3|v4` switch. The switch defaults to v3; v4 has its own manifest and `qwen3.7-text-embedding` 1024-dimensional configuration, so the legacy `text-embedding-v4` configuration is neither overwritten nor reinterpreted. Parsed standard numbers remain retrieval hints rather than new hard filters; only explicit caller filters restrict document scope.
- The promoted local runtime is `v4-hybrid-fixed20-v1`. Its private manifest pins the unchanged v4 corpus (`9f23c72...1d97`), 23,250 retrieval leaves (`6d96def...99e72`), the independent 23,250-row FTS copy (`4aa39d2...f6c2f`), the 23,250×1024 durable Qwen vector matrix (`8a5d0b3...644f`) and its row mapping. Retrieval reproduces T068: FTS document Top-30, document-local lexical Top-50, exact-cosine dense Top-60, equal RRF with lexical-head 1/dense-head 4, governed structural reservation and unchanged-order fill to 20. T074 alone may use separate lexical and semantic query texts; ambiguous sand-gold questions stop before FTS or Embedding. Vector failure safely falls back to the keyword route.
- Frozen local shadow replay makes no live model or Embedding request and exactly reproduces all 118 T068 final pools and all 14 scored T074 final pools; four of four ambiguity cases stop before Embedding. A separate public-question smoke test using the configured Qwen API returned `query_embedding_ready=true`, `vector_route=exact_dense`, no vector error, and placed DZ/T 0208-2020 clause 1 for `沙金应该使用哪个标准？`. The 17 focused tests, v4 corpus validation, runtime promotion validation, shadow validation and all 759 project tests pass. Runtime-manifest/result/report SHA-256 values are `c66eb52...e92f9`, `a1c9daf...89d5` and `b58d14e...6c2b`.
- T076 closes only the local production-layer build. It does not activate v4 online, connect to the GeoWiki server, sync data, deploy code or restart a service. v3 remains the default and immediate rollback; `cloud_activation_authorized=false`, `deployment_authorized=false`, `service_restart_authorized=false`, `knowledge_graph_created=false`, `ann_used=false`, and `cloud_sync_required=false`. The next recommended gate is a local end-to-end KB-plus-QA shadow comparison before any separately approved online replacement.

### 2026-07-30

- The user authorized the first local end-to-end shadow with “可以，请开始测试”. T077 freezes the already-observed 22-question validation split rather than reclaiming the former sealed holdout as blind, adds the four user-reviewed T072 ambiguity-first cases and one synthetic nonexistent-standard guard, and starts separate local v4 Knowledge API and QA API processes. The test uses the configured real DeepSeek question-resolution/planning/evidence/answer chain and Qwen query Embedding, while app state, traces, usage and gap logs are isolated under the private T077 directory. Both processes are stopped after execution; no GeoWiki cloud connection, deployment, online restart, ANN or knowledge graph occurs.
- T077 grades final API behavior rather than retrieval alone. It maps every returned source back to the 23,250 searchable v4 leaves, requires each Gold necessary-evidence group in the final sources, and checks only case-specific key facts rather than prose style. The first mechanical score incorrectly rejected QA005—QA007's table cells written as `80~160` under an `/m` header and QA010's equivalent phrase `不能简单理解`; deterministic v1.1 rescoring accepts only those equivalent forms, changes no model output and raises strict pass from 10/22 to 14/22.
- Final observed metrics are: 17/22 answered, 16/22 with every Gold-required evidence group in final API sources, 16/22 with every key fact, and 14/22 strict full pass. Four ambiguity-first cases stop correctly in 3/4; the nonexistent-authority guard passes 0/1. Completed validation requests average 12,428 ms with P95 20,112 ms. The run status is `completed_with_findings`, not acceptance.
- P0 findings block online replacement. MISS001 explicitly requires nonexistent `DZ/T 9999-2099` article 7.7, but the system ignores that authority anchor and answers from three unrelated current standards. QA051 and QA064 eventually receive KB-side 200 responses only after the QA client's 60-second read timeout; `/api/ask` propagates HTTP 500 instead of returning a safe evidence-insufficient response. P1 findings are: domain-gate false negatives for `样品制备` and canonical `砂金` (QA061/AIA002); evidence-sufficiency false negatives despite direct Gold clauses (QA011/QA042); and wrong or lower-authority final sources (QA030/QA069). QA096 is P2: three current estimation standards directly support the same definition, but the Gold-preferred GB/T 19492-2020 article 5.1 is absent, so later work must decide between adding equivalent Gold alternatives and preferring the general classification standard.
- T077 script/test/result/report SHA-256 values are `9d91843...dc03a`, `e0a21d0...3c91`, `9815c15...c427c` and `9099556...ae326`. Twenty-three T076/T077 focused tests pass. During the complete suite, T075 exposed a pre-existing date-dependent validator: `date.today()` made the frozen 2026-07-29 review JSON fail on 2026-07-30. Its builder now pins the original approved preparation date; T075 JSON/Markdown hashes remain exactly `cc749ed...ad88` / `e5e7c47...90a7`, and its eight focused tests pass without rebuilding or changing the review contract. The rerun completes with all 765 tests passing.
- T077 does not authorize remediation or online activation. v3 remains the default and rollback, T076 remains local-shadow ready, `online_replacement_recommended=false`, `cloud_activation_authorized=false`, `deployment_authorized=false`, `service_restart_authorized=false`, `knowledge_graph_created=false`, and `cloud_sync_required=false`.
- The user accepted a deterministic explicit-authority guard for T077's first P0 and authorized testing. T078 classifies user-supplied standard anchors as strict authority, status lookup, comparison/relation or incidental mention. Only strict authority performs catalog/governance validation and hard standard scoping; missing, repealed, prohibited or unverified standards stop before model planning and body retrieval. Final answer evidence must match the named standard and, when supplied, the requested clause or an explicit descendant clause. Other standards may not silently substitute for the named authority.
- The frozen T077 MISS001 baseline remains unchanged for audit. Under T078 the same request returns `insufficient_evidence` with zero sources, zero retrieval rounds and zero body-search calls in 10.247 ms against the real v4 catalog, instead of answering from three unrelated standards. Six focused guard tests, eleven v4 runtime tests and the complete 773-test suite pass; the v4 corpus hash remains `9f23c72...1d97`. T078 does not address the separate 60-second timeout P0, activate v4 online, connect to cloud infrastructure, rebuild indexes/vectors, or create ANN/KG; v3 remains default and `cloud_sync_required=false`.
- The user authorized repair of T077's second P0 with “请修复吧”. T079 confirms that QA051 and QA064 are not intrinsically slow: frozen-vector local retrieval is about one second per search, while the failing path combined duplicate effective queries, a process-wide query-Embedding lock, an inner DashScope budget of up to three 60-second attempts plus backoff, and an outer QA-to-KB timeout of 60 seconds. Failure traces had no inner timing, so the historical T077 statement that those KB requests “eventually completed” is not treated as proven; T077 artifacts remain frozen rather than rewritten.
- T079 creates `v4-hybrid-fixed20-resilient-v2` without changing `hybrid_fixed20_v1`. The v2 manifest references the same hash-pinned corpus, 23,250-row FTS, retrieval leaves, document vectors and row mapping. Query Embedding now has a separate 3-second per-attempt budget with at most one retry, a 256-entry SHA-256-keyed in-memory cache and single-flight coalescing; identical concurrent effective searches are also computed once. The QA-to-KB client has a separate 20-second budget. Embedding failure is caught by the existing keyword-only route before the outer budget expires.
- Agent retrieval no longer rethrows a failed primary route when another route succeeded. If every route fails, `/api/ask` returns HTTP 200 with status `insufficient_evidence`, zero sources, no generated conclusion and no false corpus-gap task; the trace records phase, route index, primary/supplemental role and exception type. A forced Embedding outage still retrieves QA051 and QA064's required Gold evidence through keywords at ranks 2 and 1.
- With real Qwen, three concurrent QA064 searches invoke the provider once, coalesce two searches and complete in 1,482 ms with identical correct heads; a later call uses the query-vector cache. Separate local Knowledge and QA processes return HTTP 200 `answered` for QA051 and QA064 in 10,146/12,052 ms, with KB stages of 1,325/1,624 ms, one provider call per question and the expected evidence/facts. All 778 tests pass. Corpus SHA-256 remains `9f23c72...1d97`, integrity is `ok`, foreign-key violations are zero, and both v1 and v2 manifests validate. T079 does not authorize online replacement, cloud access, deployment, online service restart, ANN or knowledge graph; v3 remains default, P1 findings still block replacement, and `cloud_sync_required=false`.
- The user authorized continuation of the T077 P1 remediation with “那我们继续解决P1问题”. T080 closes the domain-gate false negatives by admitting geological `样品制备` while preserving explicit food, medicine, metallography and other-domain exclusions. Canonical `砂金有什么要求？` now follows the same ambiguity-first boundary as `沙金`, returns `clarification_required` with no source and stops before knowledge retrieval.
- T080 closes the two evidence-sufficiency false negatives without weakening the general evidence guard. QA011 no longer accepts a model-invented independent `详查报告申请材料` target; the current 自然资规〔2023〕4号 clause is sufficient and DZ/T 0430-2023's report-replacement note remains optional only when retrieved. QA042 no longer treats `基本分析样` plus “什么情况下” as a request for a basic-analysis project list and answers from DZ/T 0348-2020 clause 7.6.2.1.1 with the uniform/large-value/nonuniform/small-value relation.
- QA069's correct GB/T 13908-2020 clause 8.7.2.3 was already first in the retrieval candidates but had been discarded by an over-specific evidence selector. T080 gives the general solid-mineral sample-length question an explicit authority preference for the general standard. The final local E2E answer cites only that clause and covers wall rock, intercalation, thickness and industrial indicators.
- QA030's five normative clauses were all in the candidate union, but a DeepSeek paraphrase (`油气资源储量的分类划分标准是什么`) lost the broad-classification identity before deterministic same-section completion. T080 introduces a separately validated `structural_query`: lexical and dense routes continue using the canonical question, while only the deterministic structure selector receives the classification-system marker. Fabricated markers, semantic-target subqueries and bare work-stage questions are rejected. The final pool preserves GB/T 19492-2020 clauses 4.7/4.8 at positions 19/20, and the answer selector returns only clauses 4.1/4.2/4.3/4.7/4.8 rather than `300问` or DZ/T 0400-2022.
- A unified isolated local E2E replay under `v4-hybrid-fixed20-p1fix-v4` returns HTTP 200 for all six P1 cases: QA061, QA011, QA042, QA030 and QA069 are `answered`; AIA002 is `clarification_required`. Request times are 13,088/13,825/6,610/9,860/5,897/9,844 ms respectively. The complete 792-test suite passes. Corpus SHA-256 remains `9f23c72...1d97`, integrity is `ok`, foreign-key violations are zero, and counts remain 156 documents, 3,645 pages and 20,670 units. The v4 manifest is `4c080c2...a015e`; it reuses existing corpus/FTS/document vectors and creates no ANN/KG. Historical T074/T076 artifacts remain frozen, while their tests now distinguish historical replay validation from the current hash-pinned runtime. v3 remains default; online activation, cloud connection, deployment and service restart remain unauthorized, and `cloud_sync_required=false`.
- The user approved handling P2 before the full end-to-end validation. T081 finds that QA096's answer was already correct, while Gold v6's sole source contract was imprecise: the quoted `剩余控制经济可采储量` definition is GB/T 19492-2020 clause 5.2, but the current retrieval leaf spans clauses 5.1-5.4 and carries metadata `clause_no=5.1`. T081 does not silently relabel that leaf or mutate retrieval preprocessing. It makes the discrepancy explicit and accepts four current, precise clause 8.4 leaves from DZ/T 0217-2020, DZ/T 0252-2020, DZ/T 0216-2020 and DZ/T 0254-2020 as scope-specific alternatives for the same subtraction formula.
- Gold v7 preserves all 112 case identities, the QA096 question/reference answer, all other 111 cases and the count of 184 unconditional groups. Only QA096 citations, evidence claims, alternative IDs and review note change. JSON/report SHA-256 values are `84d2e95...88aa5` and `62c0520...9001`; six focused tests pass. Gold v6, corpus, retrieval leaves, FTS, embeddings and historical baselines remain unchanged, and `cloud_sync_required=false`.
- T082 reuses T077's already-observed final-API validation contract with Gold v7 and the current v4 runtime; it is a regression acceptance run, not a new blind holdout. All 22 validation questions return `answered`, all 22 contain every accepted necessary evidence group, all 22 contain every case-specific key fact, and all 22 pass jointly. The four sand-gold ambiguities stop before retrieval, and MISS001 returns `insufficient_evidence`. QA096 passes in 8,236 ms using three precise current clause 8.4 sources. Average/P95 validation latency is 10,892/17,766 ms.
- T082 result/report SHA-256 values are `ba5f6bc...ce1ec` and `84b1e94...ce2d`; all 803 project tests pass. Database SHA-256 remains `9f23c72...1d97`, integrity is `ok`, foreign-key violations are zero, and counts remain 156 documents, 3,645 pages and 20,670 units. All known T077 P0/P1/P2 findings are therefore locally closed. This establishes local acceptance readiness only: v3 remains default and rollback, and cloud connection, online replacement, deployment and service restart still require separate user authorization. No ANN, knowledge graph, index rebuild or vector rebuild occurred; `cloud_sync_required=false`.
- The user subsequently gave explicit authorization to replace v3 online while preserving the platform registration/account architecture. T083 therefore permits the necessary cloud connection, private v4 asset synchronization and ordered KB/API service restarts for this cutover only. The authorization does not include ANN or knowledge-graph construction, public exposure of the KB, raw-source upload, or replacement of the application account database.
- The cloud cutover uses `scripts/deploy_v4_cloud.sh`, not the legacy v3 `sync_cloud.sh`. It preserves the remote `.env` values and live `application.sqlite`, creates rollback point `20260730T080653Z`, verifies the eight v4 artifact hashes remotely, preloads the v4 store before activation, then restarts KB before API. The retained v3 database hash remains `dfdc347...453a0`; it is a rollback file only and the active KB process has no v3 database file descriptor.
- Production now runs `v4-hybrid-fixed20-p1fix-v4` with corpus/manifest SHA-256 `9f23c72...1d97` / `4c080c2...a015e`, 156 documents, 3,645 page records, 20,670 content units, 23,250 retrieval leaves, 23,250 persisted Qwen vectors and query Embedding ready. ANN count is zero and knowledge-graph entity/relation counts are zero. The mutable `/knowledge/candidates` contract is retained through a separate v4-only SQLite staging store containing no answer-retrieval tables; v3 is no longer used even for candidate administration.
- Registration, login, invitation, account, quota and frontend/API boundaries remain on the existing application layer. Seven users, six invitations and 27 sessions remain; application integrity is `ok`, registration/auth/email verification are ready, and Nginx still returns 404 for public `/knowledge/*`. The application file hash changed because the governed runtime lexicon moved from 26 to 28 rows; all user/invitation/session table counts and schemas remain unchanged.
- The first cloud QA030 smoke run exposed nondeterminism: a model rewrite produced only four of five final sources even though a repeat produced all five. T083 does not accept the lucky repeat as sufficient. Broad classification questions now preserve the user's clear original question as the primary lexical/dense route while retaining the model classification/plan. Five independent post-fix QA030 runs all return GB/T 19492-2020 clauses 4.1, 4.2, 4.3, 4.7 and 4.8 in 4.4-6.0 seconds; the sand-gold ambiguity stop and nonexistent-standard guard also pass.
- T083 closes after all 807 tests pass. v4 corpus integrity is `ok`, foreign-key violations are zero, v3 and v4 corpus hashes remain unchanged, all services are active, and there is no pending cloud synchronization. T075 remains `review_needed`; no ANN or knowledge graph was created.

## Pending

- Whether MVP needs a minimal admin account or upload review waits until V1.
- Whether standard catalog query needs Excel export.
- User upload file size limit.

### 2026-08-02

- T085 adopts Schema-prefix prompting and unified question-understanding/retrieval-planning with a deterministic fail-closed fallback. The raw unified arm was rejected after QA061 returned correct percentages from related mineral standards but omitted the accepted general-standard evidence. The adopted validator does not use Gold or case identity: multiple AND evidence groups with no independent query variant trigger the dedicated Planner. The guarded arm restores 22/22 necessary evidence, 22/22 key facts and 22/22 full pass while reducing Planner calls from 22 to 7, all model calls by 20.5%, total model Tokens by 9.4%, average latency by 12.5% and P95 by 10.8%. Fixed Schema prefixing separately reduces warm-run QuestionResolver/Planner cache-miss Tokens by 80.8% versus the legacy layout. Both legacy switches remain available for rollback; the bounded `0600` stage ledger stores no question, evidence or answer body. No corpus, Gold, FTS, vector, ANN, KG, cloud or service mutation occurred, and `cloud_sync_required=false`.
